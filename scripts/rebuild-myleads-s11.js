const fs = require("fs");
const lines = [];
const a = s => lines.push(s);

a(`/* eslint-disable */`);
a(`/*`);
a(`  DXB Analytics — MY LEADS TAB`);
a(`  Session 11 — 4-Level Role-Aware CRM`);
a(`  Owner / Director / Manager / Agent`);
a(`  April 2026`);
a(`*/`);
a(``);
a(`import React, { useState, useMemo, useCallback, useEffect } from "react";`);
a(`import { collection, doc, addDoc, updateDoc, serverTimestamp, query, where, getDocs, arrayUnion } from "firebase/firestore";`);
a(`import { db, auth } from "../firebase";`);
a(`import { T } from "../data";`);
a(`import { GOLDEN_VISA_THRESHOLD } from "../utils/constants";`);
a(`import Papa from "papaparse";`);
a(``);
a(`// ─── PIPELINE ────────────────────────────────────────────────`);
a(`const PIPELINE = [`);
a(`  { key:"Hot Case",        color:"#EF4444", bg:"rgba(239,68,68,0.1)" },`);
a(`  { key:"New Lead",        color:"#3B82F6", bg:"rgba(59,130,246,0.1)" },`);
a(`  { key:"Potential",       color:"#10B981", bg:"rgba(16,185,129,0.1)" },`);
a(`  { key:"No Answer",       color:"#F59E0B", bg:"rgba(245,158,11,0.1)" },`);
a(`  { key:"Low Budget",      color:"#8B5CF6", bg:"rgba(139,92,246,0.1)" },`);
a(`  { key:"Non Potential",   color:"#6B7280", bg:"rgba(107,114,128,0.1)" },`);
a(`  { key:"Whats app",       color:"#25D366", bg:"rgba(37,211,102,0.1)" },`);
a(`  { key:"Resale/buy/Rent", color:"#14B8A6", bg:"rgba(20,184,166,0.1)" },`);
a(`  { key:"EOI",             color:"#D4A843", bg:"rgba(212,168,67,0.1)" },`);
a(`  { key:"Closed Deal",     color:"#10B981", bg:"rgba(16,185,129,0.15)" },`);
a(`  { key:"Closed Outside",  color:"#EF4444", bg:"rgba(239,68,68,0.08)" },`);
a(`];`);
a(``);
a(`const SRC_COLOR = {`);
a(`  "Property Finder":"#00C08B","Bayut":"#FF6B35","Dubizzle":"#E8003D",`);
a(`  "Meta/Facebook":"#1877F2","Instagram":"#E1306C","WhatsApp":"#25D366",`);
a(`  "Google Ads":"#4285F4","Referral":"#8B5CF6","Website":"#14B8A6",`);
a(`  "Cold Call":"#F59E0B","TikTok":"#FF0050","LinkedIn":"#0A66C2",`);
a(`  "Email Campaign":"#EA4335","Walk In":"#6B7280","Manual":"#94A3B8",`);
a(`};`);
a(``);
a(`const LEAD_SOURCES   = Object.keys(SRC_COLOR);`);
a(`const SERVICE_TYPES  = ["Buyer","Seller","Tenant","Investor"];`);
a(`const REQUEST_TYPES  = ["Off-Plan","Ready","Resale","Rental","Investment","Commercial"];`);
a(`const CHANNELS       = ["WhatsApp","Phone Call","Email","In Person","Video Call","Social DM"];`);
a(`const NATS           = ["Indian","British","Russian","Chinese","French","Pakistani","Saudi","Egyptian","German","American","Italian","Canadian","Australian","Japanese","Korean","Emirati","Filipino","Lebanese","Jordanian","Other"];`);
a(`const TIMELINES      = ["Immediate","1-3 months","3-6 months","6-12 months","Just browsing"];`);
a(`const NOTE_TYPES     = ["Note","Call","Email","WhatsApp","Viewing","Offer","Follow Up","Status Update"];`);
a(`const GV_MIN         = typeof GOLDEN_VISA_THRESHOLD !== "undefined" ? GOLDEN_VISA_THRESHOLD : 2000000;`);
a(``);
a(`const WA_TEMPLATES = [`);
a(`  { label:"First Contact",   body:"Hello {name}, I came across your enquiry. I am {agent} from {org}. We have excellent options matching your requirements in Dubai. When would be a good time to connect?" },`);
a(`  { label:"Follow Up",       body:"Hello {name}, following up on your property search. We have new listings matching your budget. Would you like to schedule a viewing this week?" },`);
a(`  { label:"Viewing Confirm", body:"Hello {name}, confirming your property viewing. Please let me know if you need to reschedule. Looking forward to meeting you!" },`);
a(`  { label:"Offer Made",      body:"Hello {name}, great news! We have submitted your offer. I will update you as soon as we hear back." },`);
a(`  { label:"Golden Visa",     body:"Hello {name}, based on your budget you qualify for UAE Golden Visa (AED 2M+ investment). This gives you 10-year residency. Shall I send details?" },`);
a(`  { label:"Market Update",   body:"Hello {name}, prices in your preferred area have moved. I have great options within your budget. Shall we connect?" },`);
a(`  { label:"EOI Follow Up",   body:"Hello {name}, thank you for your Expression of Interest. We have reviewed your request and have excellent options. When can we connect?" },`);
a(`];`);
a(``);
a(`// ─── HELPERS ─────────────────────────────────────────────────`);
a(`const clnPhone  = p => String(p||"").replace(/[^0-9]/g,"");`);
a(`const maskPhone = p => { if(!p) return "—"; const c=clnPhone(p); if(c.length<6) return p; return c.slice(0,3)+"••••••"+c.slice(-2); };`);
a(`const fmtB      = b => { const n=parseFloat(b||0); if(!n) return "—"; return n>=1e6?"AED "+(n/1e6).toFixed(1)+"M":"AED "+n.toLocaleString(); };`);
a(`const fmtD      = d => { if(!d) return "—"; try { return new Date(d).toLocaleDateString("en-AE",{day:"2-digit",month:"short",year:"2-digit"}); } catch(e){return "—";} };`);
a(`const daysAgo   = d => !d ? 999 : Math.floor((Date.now()-new Date(d).getTime())/86400000);`);
a(`const escCSV    = v => '"'+String(v==null?"":v).replace(/"/g,'""')+'"';`);
a(``);
a(`function aiScore(l) {`);
a(`  let s=0;`);
a(`  if(l.phone&&l.email) s+=25; else if(l.phone||l.email) s+=10;`);
a(`  const b=parseFloat(l.budget||0);`);
a(`  if(b>=5000000) s+=20; else if(b>=GV_MIN) s+=15; else if(b>=1000000) s+=10;`);
a(`  const d=daysAgo(l.createdAt); if(d<1) s+=20; else if(d<3) s+=15; else if(d<7) s+=10;`);
a(`  const n=(l.notes_log||[]).length; if(n>=3) s+=10; else if(n>=1) s+=5;`);
a(`  if(l.timeline==="Immediate") s+=15; else if(l.timeline==="1-3 months") s+=10;`);
a(`  if(l.nationality) s+=5;`);
a(`  return { score:Math.min(100,s), color:s>=70?"#10B981":s>=40?"#D4A843":"#EF4444", label:s>=70?"Hot":s>=40?"Warm":"Cold" };`);
a(`}`);
a(``);
a(`// ─── ATOMS ───────────────────────────────────────────────────`);
a(`const PBadge = ({status}) => {`);
a(`  const p=PIPELINE.find(x=>x.key===status)||PIPELINE[1];`);
a(`  return <span style={{fontSize:10,padding:"2px 8px",borderRadius:10,background:p.bg,color:p.color,fontWeight:700,whiteSpace:"nowrap"}}>{status||"New Lead"}</span>;`);
a(`};`);
a(``);
a(`const ScoreBar = ({lead}) => {`);
a(`  const sc=aiScore(lead);`);
a(`  return <div style={{display:"flex",alignItems:"center",gap:4}}>`);
a(`    <div style={{width:32,height:3,borderRadius:2,background:"rgba(255,255,255,0.08)",overflow:"hidden"}}>`);
a(`      <div style={{height:"100%",width:sc.score+"%",background:sc.color,borderRadius:2}}/>`);
a(`    </div>`);
a(`    <span style={{fontSize:9,color:sc.color,fontWeight:700}}>{sc.label}</span>`);
a(`  </div>;`);
a(`};`);
a(``);
a(`const EyePhone = ({phone}) => {`);
a(`  const [show,setShow]=useState(false);`);
a(`  return <span style={{display:"inline-flex",alignItems:"center",gap:4}}>`);
a(`    <span style={{fontSize:11,color:T.textSecondary,fontFamily:"monospace"}}>{show?phone:maskPhone(phone)}</span>`);
a(`    <button type="button" onClick={e=>{e.stopPropagation();setShow(v=>!v);}} style={{background:"none",border:"none",cursor:"pointer",color:T.textMuted,padding:"1px 3px",display:"inline-flex"}}>`);
a(`      <svg width="10" height="10" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2">{show?<><path d="M17.94 17.94A10.07 10.07 0 0 1 12 20c-7 0-11-8-11-8a18.45 18.45 0 0 1 5.06-5.94M9.9 4.24A9.12 9.12 0 0 1 12 4c7 0 11 8 11 8a18.5 18.5 0 0 1-2.16 3.19m-6.72-1.07a3 3 0 1 1-4.24-4.24"/><line x1="1" y1="1" x2="23" y2="23"/></>:<><path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z"/><circle cx="12" cy="12" r="3"/></>}</svg>`);
a(`    </button>`);
a(`  </span>;`);
a(`};`);
a(``);
a(`const Lbl = ({children}) => <div style={{fontSize:10,color:T.textMuted,marginBottom:4,fontWeight:600,textTransform:"uppercase",letterSpacing:0.5}}>{children}</div>;`);
a(`const Inp = ({value,onChange,placeholder,type="text",disabled=false}) => (`);
a(`  <input type={type} value={value||""} onChange={e=>onChange(e.target.value)} placeholder={placeholder} disabled={disabled}`);
a(`    style={{width:"100%",padding:"8px 10px",background:disabled?"rgba(255,255,255,0.02)":"rgba(255,255,255,0.04)",border:"1px solid "+T.border,borderRadius:7,color:T.white,fontSize:12,outline:"none",boxSizing:"border-box",fontFamily:"'Outfit',sans-serif"}}/>`);
a(`);`);
a(`const Sel = ({value,onChange,children,disabled=false}) => (`);
a(`  <select value={value||""} onChange={e=>onChange(e.target.value)} disabled={disabled}`);
a(`    style={{width:"100%",padding:"8px 10px",background:disabled?"rgba(255,255,255,0.02)":"rgba(255,255,255,0.04)",border:"1px solid "+T.border,borderRadius:7,color:T.white,fontSize:12,outline:"none",fontFamily:"'Outfit',sans-serif"}}>`);
a(`    {children}`);
a(`  </select>`);
a(`);`);
a(``);
a(`const Section = ({icon,title,sub,color,open,onToggle,children}) => (`);
a(`  <div style={{background:"rgba(255,255,255,0.02)",border:"1px solid "+T.border,borderRadius:10,marginBottom:10,overflow:"hidden"}}>`);
a(`    <div style={{padding:"11px 14px",display:"flex",alignItems:"center",gap:10,cursor:"pointer",borderBottom:open?"1px solid "+T.border:"none"}} onClick={onToggle}>`);
a(`      <div style={{width:28,height:28,borderRadius:6,background:color+"18",display:"flex",alignItems:"center",justifyContent:"center",flexShrink:0}}>`);
a(`        <span style={{fontSize:14}}>{icon}</span>`);
a(`      </div>`);
a(`      <div style={{flex:1}}>`);
a(`        <div style={{fontSize:12,fontWeight:700,color:T.white}}>{title}</div>`);
a(`        {sub&&<div style={{fontSize:10,color:T.textMuted,marginTop:1}}>{sub}</div>}`);
a(`      </div>`);
a(`      <div style={{color:T.textMuted,fontSize:12,transition:"transform 0.2s",transform:open?"rotate(90deg)":"none"}}>▶</div>`);
a(`    </div>`);
a(`    {open&&<div style={{padding:"14px"}}>{children}</div>}`);
a(`  </div>`);
a(`);`);
a(``);
a(`// ═══════════════════════════════════════════════════════════════`);
a(`// MAIN COMPONENT`);
a(`// ═══════════════════════════════════════════════════════════════`);
a(`export default function MyLeadsTab({`);
a(`  myLeads=[], orgRole, userRole, orgId, orgName,`);
a(`  listings=[], teamMembers=[], firebaseUser,`);
a(`  leadSortBy, setLeadSortBy,`);
a(`  selectedLead, setSelectedLead,`);
a(`  handleTabChange,`);
a(`}) {`);
a(``);
a(`  // ── Role resolution ──────────────────────────────────────────`);
a(`  const isSuperAdmin = userRole==="superAdmin"||userRole==="admin";`);
a(`  const isOwner      = orgRole==="owner";`);
a(`  const isDirector   = orgRole==="director";`);
a(`  const isManager    = orgRole==="manager";`);
a(`  const isAgent      = orgRole==="agent";`);
a(`  const canManage    = isSuperAdmin||isOwner||isDirector||isManager;`);
a(`  const canSee       = canManage||isAgent;`);
a(`  const currentUid   = firebaseUser?.uid||auth?.currentUser?.uid||"";`);
a(`  const currentEmail = firebaseUser?.email||auth?.currentUser?.email||"";`);
a(``);
a(`  // ── State ────────────────────────────────────────────────────`);
a(`  const [smartView,    setSmartView]   = useState("all");`);
a(`  const [activeStage,  setActiveStage] = useState("all");`);
a(`  const [view,         setView]        = useState("table");`);
a(`  const [showAdd,      setShowAdd]     = useState(false);`);
a(`  const [showWA,       setShowWA]      = useState(false);`);
a(`  const [showAssign,   setShowAssign]  = useState(null);`);
a(`  const [drawerTab,    setDrawerTab]   = useState("profile");`);
a(`  const [aiSearch,     setAiSearch]    = useState("");`);
a(`  const [filterSource, setFilterSource]= useState("all");`);
a(`  const [filterBudget, setFilterBudget]= useState("all");`);
a(`  const [filterService,setFilterService]=useState("all");`);
a(`  const [filterAgent,  setFilterAgent] = useState("all");`);
a(`  const [filterManager,setFilterManager]=useState("all");`);
a(`  const [toast,        setToast]       = useState(null);`);
a(`  const [saving,       setSaving]      = useState(false);`);
a(`  const [noteText2,    setNoteText2]   = useState("");`);
a(`  const [noteType2,    setNoteType2]   = useState("Note");`);
a(`  const [savingNote,   setSavingNote]  = useState(false);`);
a(`  const [openSec,      setOpenSec]     = useState({contact:true,request:true,agent:false,extra:false});`);
a(``);
a(`  const EMPTY={name:"",phone:"",email:"",budget:"",requestType:"",serviceType:"Buyer",`);
a(`    project:"",source:"Manual",channel:"WhatsApp",campaign:"",status:"New Lead",`);
a(`    nationality:"",language:"",timeline:"",community:"",bedrooms:"",comment:"",`);
a(`    agentId:"",agentName:"",adName:"",adAccount:""};`);
a(`  const [form,setForm]=useState(EMPTY);`);
a(`  const F=(k,v)=>setForm(p=>({...p,[k]:v}));`);
a(``);
a(`  // ── Derived lists ────────────────────────────────────────────`);
a(`  const agents = useMemo(()=>(`);
a(`    (teamMembers||[]).filter(m=>m.orgRole==="agent"||m.role==="agent")`);
a(`  ),[teamMembers]);`);
a(``);
a(`  const managers = useMemo(()=>(`);
a(`    (teamMembers||[]).filter(m=>m.orgRole==="manager"||m.orgRole==="director"||m.orgRole==="owner")`);
a(`  ),[teamMembers]);`);
a(``);
a(`  // ── Toast ────────────────────────────────────────────────────`);
a(`  const notify = useCallback((msg,type="success")=>{`);
a(`    setToast({msg,type});`);
a(`    setTimeout(()=>setToast(null),3000);`);
a(`  },[]);`);
a(``);
a(`  // ── Counts ───────────────────────────────────────────────────`);
a(`  const stageCounts = useMemo(()=>{`);
a(`    const c={};`);
a(`    PIPELINE.forEach(p=>{c[p.key]=(myLeads||[]).filter(l=>(l.status||"New Lead")===p.key).length;});`);
a(`    return c;`);
a(`  },[myLeads]);`);
a(``);
a(`  // ── Smart view filter ────────────────────────────────────────`);
a(`  const smartFiltered = useMemo(()=>{`);
a(`    let a=[...(myLeads||[])];`);
a(`    if(smartView==="today")       a=a.filter(l=>daysAgo(l.createdAt)<1);`);
a(`    if(smartView==="my_leads")    a=a.filter(l=>l.assignedTo===currentUid||l.createdBy===currentUid);`);
a(`    if(smartView==="hot")         a=a.filter(l=>aiScore(l).score>=70);`);
a(`    if(smartView==="stale")       a=a.filter(l=>!["Closed Deal","Closed Outside"].includes(l.status)&&daysAgo(l.updatedAt||l.createdAt)>7);`);
a(`    if(smartView==="overdue")     a=a.filter(l=>l.followUpDate&&new Date(l.followUpDate)<new Date()&&!["Closed Deal","Closed Outside"].includes(l.status));`);
a(`    if(smartView==="uncontacted") a=a.filter(l=>!(l.notes_log||[]).some(n=>["Call","WhatsApp","Email"].includes(n.type)));`);
a(`    if(smartView==="unassigned")  a=a.filter(l=>!l.assignedTo||l.assignedTo==="");`);
a(`    if(smartView==="golden_visa") a=a.filter(l=>parseFloat(l.budget||0)>=GV_MIN);`);
a(`    return a;`);
a(`  },[myLeads,smartView,currentUid]);`);
a(``);
a(`  // ── Main filter ──────────────────────────────────────────────`);
a(`  const filtered = useMemo(()=>{`);
a(`    let a=[...smartFiltered];`);
a(`    if(activeStage!=="all")  a=a.filter(l=>(l.status||"New Lead")===activeStage);`);
a(`    if(filterAgent!=="all")  a=a.filter(l=>l.assignedTo===filterAgent);`);
a(`    if(filterManager!=="all") a=a.filter(l=>l.managerId===filterManager);`);
a(`    if(filterService!=="all") a=a.filter(l=>(l.serviceType||l.type||"")===filterService);`);
a(`    if(filterSource!=="all")  a=a.filter(l=>l.source===filterSource);`);
a(`    if(filterBudget!=="all"){`);
a(`      const b=l=>parseFloat(l.budget||0);`);
a(`      if(filterBudget==="under1m")  a=a.filter(l=>b(l)<1e6);`);
a(`      if(filterBudget==="1to3m")    a=a.filter(l=>b(l)>=1e6&&b(l)<3e6);`);
a(`      if(filterBudget==="3to5m")    a=a.filter(l=>b(l)>=3e6&&b(l)<5e6);`);
a(`      if(filterBudget==="5to10m")   a=a.filter(l=>b(l)>=5e6&&b(l)<1e7);`);
a(`      if(filterBudget==="above10m") a=a.filter(l=>b(l)>=1e7);`);
a(`    }`);
a(`    if(aiSearch.trim()){`);
a(`      const q=aiSearch.trim().toLowerCase();`);
a(`      a=a.filter(l=>(l.name||"").toLowerCase().includes(q)||(l.phone||"").includes(q)||(l.email||"").toLowerCase().includes(q)||(l.community||"").toLowerCase().includes(q)||(l.assignedToName||"").toLowerCase().includes(q));`);
a(`    }`);
a(`    const sorted=[...a];`);
a(`    if(leadSortBy==="score")   sorted.sort((a,b)=>aiScore(b).score-aiScore(a).score);`);
a(`    else if(leadSortBy==="budget") sorted.sort((a,b)=>parseFloat(b.budget||0)-parseFloat(a.budget||0));`);
a(`    else if(leadSortBy==="name")   sorted.sort((a,b)=>(a.name||"").localeCompare(b.name||""));`);
a(`    else sorted.sort((a,b)=>new Date(b.createdAt||0)-new Date(a.createdAt||0));`);
a(`    return sorted;`);
a(`  },[smartFiltered,activeStage,filterAgent,filterManager,filterService,filterSource,filterBudget,aiSearch,leadSortBy]);`);
a(``);
a(`  // ── KPIs ─────────────────────────────────────────────────────`);
a(`  const allLeads  = myLeads||[];`);
a(`  const kTotal    = allLeads.length;`);
a(`  const kToday    = allLeads.filter(l=>daysAgo(l.createdAt)<1).length;`);
a(`  const kHot      = allLeads.filter(l=>aiScore(l).score>=70&&!["Closed Deal","Closed Outside"].includes(l.status)).length;`);
a(`  const kClosed   = allLeads.filter(l=>l.status==="Closed Deal").length;`);
a(`  const kStale    = allLeads.filter(l=>!["Closed Deal","Closed Outside","Non Potential"].includes(l.status)&&daysAgo(l.updatedAt||l.createdAt)>7).length;`);
a(`  const kPipeline = allLeads.reduce((s,l)=>s+parseFloat(l.budget||0),0);`);
a(`  const kUnassigned = allLeads.filter(l=>!l.assignedTo).length;`);
a(`  const kConvRate = kTotal>0?Math.round((kClosed/kTotal)*100):0;`);
a(``);
a(`  // ── Agent performance (manager/owner/director) ───────────────`);
a(`  const agentPerf = useMemo(()=>{`);
a(`    if(!canManage) return [];`);
a(`    return agents.map(agent=>{`);
a(`      const uid=agent.uid||agent.id;`);
a(`      const aLeads=allLeads.filter(l=>l.assignedTo===uid);`);
a(`      const closed=aLeads.filter(l=>l.status==="Closed Deal").length;`);
a(`      const hot=aLeads.filter(l=>aiScore(l).score>=70).length;`);
a(`      const stale=aLeads.filter(l=>!["Closed Deal","Closed Outside"].includes(l.status)&&daysAgo(l.updatedAt||l.createdAt)>7).length;`);
a(`      const conv=aLeads.length>0?((closed/aLeads.length)*100).toFixed(1):"0.0";`);
a(`      const pipeline=aLeads.reduce((s,l)=>s+parseFloat(l.budget||0),0);`);
a(`      const lastAct=aLeads.reduce((latest,l)=>{const d=new Date(l.updatedAt||l.createdAt||0);return d>latest?d:latest;},new Date(0));`);
a(`      return {...agent,total:aLeads.length,closed,hot,stale,conv,pipeline,lastActive:lastAct.getTime()>0?lastAct:null};`);
a(`    }).sort((a,b)=>b.closed-a.closed);`);
a(`  },[agents,allLeads,canManage]);`);
a(``);
a(`  // ── Leaderboard (owner view) — ranked by combined score ──────`);
a(`  const leaderboard = useMemo(()=>{`);
a(`    if(!isOwner&&!isSuperAdmin) return [];`);
a(`    return [...agentPerf].map(a=>{`);
a(`      const convScore  = parseFloat(a.conv||0);`);
a(`      const pipeScore  = Math.min(a.pipeline/1000000,50); // cap at 50M`);
a(`      const combined   = (a.closed*10)+(convScore*2)+(pipeScore*0.5);`);
a(`      return {...a,combined:Math.round(combined)};`);
a(`    }).sort((a,b)=>b.combined-a.combined);`);
a(`  },[agentPerf,isOwner,isSuperAdmin]);`);
a(``);
a(`  // ── Source stats ─────────────────────────────────────────────`);
a(`  const srcStats = useMemo(()=>{`);
a(`    const m={};`);
a(`    allLeads.forEach(l=>{const s=l.source||"Manual";if(!m[s])m[s]={t:0,c:0};m[s].t++;if(l.status==="Closed Deal")m[s].c++;});`);
a(`    return Object.entries(m).map(([s,d])=>({src:s,total:d.t,closed:d.c,rate:d.t>0?Math.round(d.c*100/d.t):0})).sort((a,b)=>b.total-a.total);`);
a(`  },[allLeads]);`);
a(``);
a(`  // ── Property matching ────────────────────────────────────────`);
a(`  const matched = useMemo(()=>{`);
a(`    if(!selectedLead||!listings) return [];`);
a(`    const b=parseFloat(selectedLead.budget||0);`);
a(`    return (listings||[]).filter(l=>{`);
a(`      const lp=parseFloat(l.price||l.priceMin||0);`);
a(`      return (b>0?lp<=b*1.25&&lp>=b*0.6:true)&&(selectedLead.community?(l.community||"").toLowerCase().includes((selectedLead.community||"").toLowerCase()):true);`);
a(`    }).slice(0,4);`);
a(`  },[selectedLead,listings]);`);
a(``);
a(`  // ── Save lead ────────────────────────────────────────────────`);
a(`  const saveLead = useCallback(async()=>{`);
a(`    if(!form.name||!form.phone){notify("Name and phone are required","error");return;}`);
a(`    setSaving(true);`);
a(`    try{`);
a(`      const agentId   = form.agentId||(isAgent?currentUid:"");`);
a(`      const agentName = form.agentName||(isAgent?(firebaseUser?.displayName||currentEmail):"");`);
a(`      await addDoc(collection(db,"leads"),{`);
a(`        name:form.name.trim(),phone:form.phone.trim(),email:form.email.trim(),`);
a(`        budget:parseFloat(form.budget)||0,`);
a(`        requestType:form.requestType,serviceType:form.serviceType,type:form.serviceType,`);
a(`        project:form.project,source:form.source,channel:form.channel,`);
a(`        campaign:form.campaign,adName:form.adName,adAccount:form.adAccount,`);
a(`        status:form.status||"New Lead",`);
a(`        nationality:form.nationality,language:form.language,`);
a(`        timeline:form.timeline,community:form.community,`);
a(`        bedrooms:form.bedrooms,comment:form.comment,`);
a(`        assignedTo:agentId,assignedToName:agentName,`);
a(`        orgId:orgId||"",`);
a(`        createdBy:currentUid,`);
a(`        createdAt:new Date().toISOString(),`);
a(`        updatedAt:new Date().toISOString(),`);
a(`        tags:[],`);
a(`        notes_log:[{text:"Lead created",type:"Note",by:currentEmail,at:new Date().toISOString()}],`);
a(`      });`);
a(`      setForm(EMPTY);setShowAdd(false);notify("Lead saved successfully");`);
a(`    }catch(e){console.error(e);notify("Save failed: "+e.message,"error");}`);
a(`    setSaving(false);`);
a(`  },[form,isAgent,currentUid,currentEmail,orgId,notify]);`);
a(``);
a(`  // ── Update status ────────────────────────────────────────────`);
a(`  const updateStatus = useCallback(async(leadId,newStatus)=>{`);
a(`    try{`);
a(`      const entry={text:"Status changed to "+newStatus,type:"Status Update",by:currentEmail,at:new Date().toISOString()};`);
a(`      await updateDoc(doc(db,"leads",leadId),{status:newStatus,updatedAt:new Date().toISOString(),notes_log:arrayUnion(entry)});`);
a(`      if(selectedLead?.id===leadId) setSelectedLead(l=>({...l,status:newStatus,notes_log:[...(l.notes_log||[]),entry]}));`);
a(`      notify("Status updated");`);
a(`    }catch(e){notify("Update failed","error");}`);
a(`  },[currentEmail,selectedLead,setSelectedLead,notify]);`);
a(``);
a(`  // ── Assign lead ──────────────────────────────────────────────`);
a(`  const assignLead = useCallback(async(leadId,agent)=>{`);
a(`    try{`);
a(`      const entry={text:"Lead assigned to "+agent.name,type:"Note",by:currentEmail,at:new Date().toISOString()};`);
a(`      await updateDoc(doc(db,"leads",leadId),{`);
a(`        assignedTo:agent.uid||agent.id,assignedToName:agent.name,`);
a(`        updatedAt:new Date().toISOString(),notes_log:arrayUnion(entry),`);
a(`      });`);
a(`      if(selectedLead?.id===leadId) setSelectedLead(l=>({...l,assignedTo:agent.uid||agent.id,assignedToName:agent.name}));`);
a(`      setShowAssign(null);notify("Lead assigned to "+agent.name);`);
a(`    }catch(e){notify("Assign failed","error");}`);
a(`  },[currentEmail,selectedLead,setSelectedLead,notify]);`);
a(``);
a(`  // ── Add note ─────────────────────────────────────────────────`);
a(`  const addNote = useCallback(async(leadId)=>{`);
a(`    if(!noteText2.trim()) return;`);
a(`    setSavingNote(true);`);
a(`    try{`);
a(`      const entry={text:noteText2.trim(),type:noteType2,by:currentEmail,at:new Date().toISOString()};`);
a(`      await updateDoc(doc(db,"leads",leadId),{notes_log:arrayUnion(entry),updatedAt:new Date().toISOString()});`);
a(`      if(selectedLead?.id===leadId) setSelectedLead(l=>({...l,notes_log:[...(l.notes_log||[]),entry]}));`);
a(`      setNoteText2("");notify("Note added");`);
a(`    }catch(e){notify("Note failed","error");}`);
a(`    setSavingNote(false);`);
a(`  },[noteText2,noteType2,currentEmail,selectedLead,setSelectedLead,notify]);`);
a(``);
a(`  // ── Export CSV ───────────────────────────────────────────────`);
a(`  const exportCSV = useCallback(()=>{`);
a(`    const h=["ID","Name","Phone","Email","Budget","Service","Request","Status","Source","Channel","AD Name","AD Account","Nationality","Community","Timeline","Agent","Score","Created","Updated"];`);
a(`    const rows=filtered.map(l=>[l.id||"",l.name||"",l.phone||"",l.email||"",l.budget||"",l.serviceType||l.type||"",l.requestType||"",l.status||"",l.source||"",l.channel||"",l.adName||"",l.adAccount||"",l.nationality||"",l.community||"",l.timeline||"",l.assignedToName||"",aiScore(l).score,l.createdAt?new Date(l.createdAt).toLocaleDateString("en-GB"):"",l.updatedAt?new Date(l.updatedAt).toLocaleDateString("en-GB"):""].map(v=>escCSV(v)));`);
a(`    const csv=[h.join(","),...rows.map(r=>r.join(","))].join("\\n");`);
a(`    const blob=new Blob([csv],{type:"text/csv"});`);
a(`    const url=URL.createObjectURL(blob);`);
a(`    const a=document.createElement("a");`);
a(`    a.href=url;a.download="DXB_Leads_"+new Date().toISOString().slice(0,10)+".csv";a.click();URL.revokeObjectURL(url);`);
a(`  },[filtered]);`);
a(``);
a(`  // ── Send WA ──────────────────────────────────────────────────`);
a(`  const sendWA = useCallback((tmpl,lead)=>{`);
a(`    if(!lead?.phone) return;`);
a(`    const msg=tmpl.body`);
a(`      .replace("{name}",lead.name||"")`);
a(`      .replace("{agent}",firebaseUser?.displayName||"Agent")`);
a(`      .replace("{org}",orgName||"DXB Analytics");`);
a(`    window.open("https://wa.me/"+clnPhone(lead.phone)+"?text="+encodeURIComponent(msg),"_blank");`);
a(`    setShowWA(false);`);
a(`  },[orgName,firebaseUser]);`);
a(``);
a(`  // ── Access guard ─────────────────────────────────────────────`);
a(`  if(!canSee) return (`);
a(`    <div style={{display:"flex",flexDirection:"column",alignItems:"center",justifyContent:"center",height:400,gap:12,textAlign:"center"}}>`);
a(`      <div style={{fontSize:36}}>🔒</div>`);
a(`      <div style={{fontSize:16,fontWeight:700,color:T.white}}>CRM Access Required</div>`);
a(`      <div style={{fontSize:12,color:T.textMuted,maxWidth:300}}>Your account does not have CRM access. Contact your administrator.</div>`);
a(`    </div>`);
a(`  );`);
a(``);
a(`  return (`);
a(`    <div style={{paddingBottom:80}}>`);
a(``);
a(`      {/* ══ SMART VIEW TABS ══════════════════════════════════════ */}`);
a(`      <div style={{display:"flex",gap:0,borderBottom:"1px solid "+T.border,overflowX:"auto",paddingLeft:4}}>`);
a(`        {[`);
a(`          {k:"all",         l:"All"},`);
a(`          {k:"today",       l:"Today"},`);
a(`          {k:"my_leads",    l:"My Leads"},`);
a(`          {k:"uncontacted", l:"Uncontacted"},`);
a(`          {k:"hot",         l:"Hot"},`);
a(`          {k:"stale",       l:"Stale"},`);
a(`          {k:"overdue",     l:"Overdue"},`);
a(`          ...(canManage?[{k:"unassigned",l:"Unassigned"}]:[]),`);
a(`          {k:"golden_visa", l:"Golden Visa"},`);
a(`        ].map(v=>{`);
a(`          const active=smartView===v.k;`);
a(`          return <button key={v.k} type="button" onClick={()=>{setSmartView(v.k);setActiveStage("all");}}`);
a(`            style={{padding:"10px 14px",border:"none",borderBottom:active?"2px solid "+T.gold:"2px solid transparent",background:"transparent",color:active?T.white:T.textMuted,fontSize:12,fontWeight:active?600:400,cursor:"pointer",whiteSpace:"nowrap",fontFamily:"'Outfit',sans-serif",transition:"color 0.15s"}}>`);
a(`            {v.l}{v.k==="unassigned"&&kUnassigned>0?<span style={{marginLeft:5,fontSize:10,background:"rgba(239,68,68,0.15)",color:"#EF4444",padding:"1px 5px",borderRadius:8}}>{kUnassigned}</span>:null}`);
a(`          </button>;`);
a(`        })}`);
a(`      </div>`);
a(``);
a(`      {/* ══ TOOLBAR ══════════════════════════════════════════════ */}`);
a(`      <div style={{display:"flex",alignItems:"center",gap:10,padding:"10px 4px",borderBottom:"1px solid "+T.border,flexWrap:"wrap"}}>`);
a(`        <div style={{display:"flex",alignItems:"center",gap:8}}>`);
a(`          <span style={{fontSize:13,fontWeight:700,color:T.white}}>{orgName||"Leads"}</span>`);
a(`          <span style={{fontSize:13,color:T.textMuted,fontWeight:700}}>{kTotal.toLocaleString()}</span>`);
a(`        </div>`);
a(`        <div style={{flex:"1 1 240px",display:"flex",alignItems:"center",gap:8,padding:"6px 12px",background:"rgba(255,255,255,0.03)",border:"1px solid "+(aiSearch?T.gold:T.border),borderRadius:8,cursor:"text"}} onClick={()=>document.getElementById("crm-search")?.focus()}>`);
a(`          <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke={T.textMuted} strokeWidth="2"><circle cx="11" cy="11" r="8"/><line x1="21" y1="21" x2="16.65" y2="16.65"/></svg>`);
a(`          <input id="crm-search" value={aiSearch} onChange={e=>setAiSearch(e.target.value)} placeholder="Search name, phone, agent, area..."`);
a(`            style={{flex:1,background:"none",border:"none",outline:"none",color:T.white,fontSize:12,fontFamily:"'Outfit',sans-serif"}}/>`);
a(`          {aiSearch&&<button type="button" onClick={()=>setAiSearch("")} style={{background:"none",border:"none",color:T.textMuted,cursor:"pointer",fontSize:14}}>×</button>}`);
a(`        </div>`);
a(`        <div style={{display:"flex",gap:2,background:"rgba(255,255,255,0.03)",border:"1px solid "+T.border,borderRadius:7,padding:2,marginLeft:"auto"}}>`);
a(`          {[{k:"table",icon:"⊟"},{k:"kanban",icon:"⊞"},{k:"analytics",icon:"⊕"}].map(v=>(`);
a(`            <button key={v.k} type="button" onClick={()=>setView(v.k)} style={{padding:"5px 10px",borderRadius:5,border:view===v.k?"1px solid "+T.gold:"1px solid transparent",background:view===v.k?"rgba(212,168,67,0.15)":"transparent",color:view===v.k?T.gold:T.textMuted,cursor:"pointer",fontSize:13}}>{v.icon}</button>`);
a(`          ))}`);
a(`        </div>`);
a(`        <button type="button" onClick={exportCSV} style={{padding:"6px 11px",borderRadius:7,border:"1px solid "+T.border,background:"transparent",color:T.textSecondary,fontSize:11,fontWeight:600,cursor:"pointer",fontFamily:"'Outfit',sans-serif"}}>Export</button>`);
a(`        <button type="button" onClick={()=>setShowWA(true)} style={{padding:"6px 11px",borderRadius:7,border:"1px solid rgba(37,211,102,0.3)",background:"rgba(37,211,102,0.07)",color:"#25D366",fontSize:11,fontWeight:600,cursor:"pointer"}}>Bulk WA</button>`);
a(`        <button type="button" onClick={()=>setShowAdd(true)} style={{padding:"7px 16px",borderRadius:7,border:"none",background:"linear-gradient(135deg,#D4A843,#B8902E)",color:"#0A0E1A",fontSize:12,fontWeight:700,cursor:"pointer",display:"flex",alignItems:"center",gap:5}}>`);
a(`          <svg width="11" height="11" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2.5"><line x1="12" y1="5" x2="12" y2="19"/><line x1="5" y1="12" x2="19" y2="12"/></svg> Add`);
a(`        </button>`);
a(`      </div>`);
a(``);
a(`      {/* ══ PIPELINE PILLS ═══════════════════════════════════════ */}`);
a(`      <div style={{display:"flex",gap:0,borderBottom:"1px solid "+T.border,overflowX:"auto",paddingLeft:4,background:"rgba(255,255,255,0.01)"}}>`);
a(`        <button type="button" onClick={()=>setActiveStage("all")} style={{padding:"7px 12px",border:"none",borderBottom:activeStage==="all"?"2px solid "+T.gold:"2px solid transparent",background:"transparent",color:activeStage==="all"?T.white:T.textMuted,fontSize:11,fontWeight:activeStage==="all"?600:400,cursor:"pointer",whiteSpace:"nowrap",fontFamily:"'Outfit',sans-serif"}}>All</button>`);
a(`        {PIPELINE.map(p=>{`);
a(`          const count=stageCounts[p.key]||0;`);
a(`          const active=activeStage===p.key;`);
a(`          return <button key={p.key} type="button" onClick={()=>setActiveStage(active?"all":p.key)}`);
a(`            style={{padding:"7px 12px",border:"none",borderBottom:active?"2px solid "+p.color:"2px solid transparent",background:"transparent",color:active?p.color:T.textMuted,fontSize:11,fontWeight:active?600:400,cursor:"pointer",whiteSpace:"nowrap",fontFamily:"'Outfit',sans-serif",display:"flex",alignItems:"center",gap:5}}>`);
a(`            <span style={{width:6,height:6,borderRadius:"50%",background:p.color,display:"inline-block"}}/>`);
a(`            {p.key} <span style={{fontSize:10,color:active?p.color:T.textMuted}}>{count}</span>`);
a(`          </button>;`);
a(`        })}`);
a(`      </div>`);
a(``);
a(`      {/* ══ FILTER ROW ════════════════════════════════════════════ */}`);
a(`      <div style={{display:"flex",gap:8,padding:"8px 4px",borderBottom:"1px solid "+T.border,flexWrap:"wrap",alignItems:"center",background:"rgba(255,255,255,0.01)"}}>`);
a(`        {(isOwner||isDirector||isSuperAdmin)&&managers.length>0&&(`);
a(`          <select value={filterManager} onChange={e=>setFilterManager(e.target.value)} style={{padding:"5px 8px",background:"rgba(255,255,255,0.04)",border:"1px solid "+(filterManager!=="all"?T.gold:T.border),borderRadius:6,color:T.textSecondary,fontSize:11,outline:"none",fontFamily:"'Outfit',sans-serif"}}>`);
a(`            <option value="all">All Managers</option>`);
a(`            {managers.map(m=><option key={m.uid||m.id} value={m.uid||m.id}>{m.name}</option>)}`);
a(`          </select>`);
a(`        )}`);
a(`        {canManage&&agents.length>0&&(`);
a(`          <select value={filterAgent} onChange={e=>setFilterAgent(e.target.value)} style={{padding:"5px 8px",background:"rgba(255,255,255,0.04)",border:"1px solid "+(filterAgent!=="all"?T.gold:T.border),borderRadius:6,color:T.textSecondary,fontSize:11,outline:"none",fontFamily:"'Outfit',sans-serif"}}>`);
a(`            <option value="all">All Agents</option>`);
a(`            {agents.map(a=><option key={a.uid||a.id} value={a.uid||a.id}>{a.name}</option>)}`);
a(`          </select>`);
a(`        )}`);
a(`        <select value={filterService} onChange={e=>setFilterService(e.target.value)} style={{padding:"5px 8px",background:"rgba(255,255,255,0.04)",border:"1px solid "+T.border,borderRadius:6,color:T.textSecondary,fontSize:11,outline:"none",fontFamily:"'Outfit',sans-serif"}}>`);
a(`          <option value="all">All Types</option>`);
a(`          {SERVICE_TYPES.map(t=><option key={t} value={t}>{t}</option>)}`);
a(`        </select>`);
a(`        <select value={filterSource} onChange={e=>setFilterSource(e.target.value)} style={{padding:"5px 8px",background:"rgba(255,255,255,0.04)",border:"1px solid "+T.border,borderRadius:6,color:T.textSecondary,fontSize:11,outline:"none",fontFamily:"'Outfit',sans-serif"}}>`);
a(`          <option value="all">All Sources</option>`);
a(`          {LEAD_SOURCES.map(s=><option key={s} value={s}>{s}</option>)}`);
a(`        </select>`);
a(`        <select value={filterBudget} onChange={e=>setFilterBudget(e.target.value)} style={{padding:"5px 8px",background:"rgba(255,255,255,0.04)",border:"1px solid "+T.border,borderRadius:6,color:T.textSecondary,fontSize:11,outline:"none",fontFamily:"'Outfit',sans-serif"}}>`);
a(`          <option value="all">All Budgets</option>`);
a(`          <option value="under1m">Under AED 1M</option>`);
a(`          <option value="1to3m">AED 1M - 3M</option>`);
a(`          <option value="3to5m">AED 3M - 5M</option>`);
a(`          <option value="5to10m">AED 5M - 10M</option>`);
a(`          <option value="above10m">Above AED 10M</option>`);
a(`        </select>`);
a(`        <select value={leadSortBy||"date"} onChange={e=>setLeadSortBy&&setLeadSortBy(e.target.value)} style={{padding:"5px 8px",background:"rgba(255,255,255,0.04)",border:"1px solid "+T.border,borderRadius:6,color:T.textSecondary,fontSize:11,outline:"none",fontFamily:"'Outfit',sans-serif"}}>`);
a(`          <option value="date">Latest First</option>`);
a(`          <option value="score">AI Score</option>`);
a(`          <option value="budget">Budget High-Low</option>`);
a(`          <option value="name">Name A-Z</option>`);
a(`        </select>`);
a(`        {(aiSearch||activeStage!=="all"||filterAgent!=="all"||filterManager!=="all"||filterService!=="all"||filterSource!=="all"||filterBudget!=="all")&&(`);
a(`          <div style={{display:"flex",gap:6,alignItems:"center",marginLeft:"auto",flexWrap:"wrap"}}>`);
a(`            {activeStage!=="all"&&<span style={{fontSize:10,padding:"2px 8px",borderRadius:10,background:"rgba(212,168,67,0.15)",color:T.gold,cursor:"pointer"}} onClick={()=>setActiveStage("all")}>{activeStage} ×</span>}`);
a(`            {filterAgent!=="all"&&<span style={{fontSize:10,padding:"2px 8px",borderRadius:10,background:"rgba(139,92,246,0.15)",color:"#8B5CF6",cursor:"pointer"}} onClick={()=>setFilterAgent("all")}>{agents.find(a=>(a.uid||a.id)===filterAgent)?.name||"Agent"} ×</span>}`);
a(`            {filterManager!=="all"&&<span style={{fontSize:10,padding:"2px 8px",borderRadius:10,background:"rgba(59,130,246,0.15)",color:"#3B82F6",cursor:"pointer"}} onClick={()=>setFilterManager("all")}>{managers.find(m=>(m.uid||m.id)===filterManager)?.name||"Manager"} ×</span>}`);
a(`            <button type="button" onClick={()=>{setAiSearch("");setActiveStage("all");setFilterAgent("all");setFilterManager("all");setFilterService("all");setFilterSource("all");setFilterBudget("all");}} style={{fontSize:10,padding:"2px 10px",borderRadius:10,background:"rgba(252,129,129,0.1)",border:"1px solid rgba(252,129,129,0.3)",color:"#FC8181",cursor:"pointer",fontFamily:"'Outfit',sans-serif"}}>Clear all</button>`);
a(`            <span style={{fontSize:10,color:T.textMuted}}>{filtered.length} results</span>`);
a(`          </div>`);
a(`        )}`);
a(`      </div>`);
a(``);
a(`      {/* ══ MAIN CONTENT ══════════════════════════════════════════ */}`);
a(`      <div style={{display:"grid",gridTemplateColumns:selectedLead?"1fr 360px":"1fr",gap:0,alignItems:"start"}}>`);
a(``);
a(`        {/* ── TABLE VIEW ── */}`);
a(`        {view==="table"&&(`);
a(`          <div>`);
a(`            <div style={{display:"grid",gridTemplateColumns:"72px 2fr 110px 130px 110px 130px 140px 48px",padding:"8px 4px",borderBottom:"1px solid "+T.border,background:"rgba(255,255,255,0.02)"}}>`);
a(`              {["ID","NAME","STATUS","BUDGET","SERVICE","SOURCE","AGENT",""].map((h,i)=>(`);
a(`                <div key={i} style={{fontSize:9,fontWeight:700,color:T.textMuted,textTransform:"uppercase",letterSpacing:0.7,paddingLeft:i===0?4:0}}>{h}</div>`);
a(`              ))}`);
a(`            </div>`);
a(`            {allLeads.length===0&&(`);
a(`              <div style={{padding:"80px 20px",textAlign:"center"}}>`);
a(`                <div style={{fontSize:40,marginBottom:12}}>📋</div>`);
a(`                <div style={{fontSize:16,fontWeight:700,color:T.white,marginBottom:6}}>No leads yet</div>`);
a(`                <div style={{fontSize:12,color:T.textMuted,marginBottom:16}}>Add your first lead to start managing your pipeline</div>`);
a(`                <button type="button" onClick={()=>setShowAdd(true)} style={{padding:"10px 24px",borderRadius:8,border:"none",background:"linear-gradient(135deg,#D4A843,#B8902E)",color:"#0A0E1A",fontSize:13,fontWeight:700,cursor:"pointer"}}>+ Add First Lead</button>`);
a(`              </div>`);
a(`            )}`);
a(`            {filtered.length===0&&allLeads.length>0&&<div style={{padding:"40px 20px",textAlign:"center",color:T.textMuted,fontSize:13}}>No leads match your current filters</div>}`);
a(`            {filtered.map((lead,i)=>{`);
a(`              const sc=aiScore(lead);`);
a(`              const stale=!["Closed Deal","Closed Outside","Non Potential"].includes(lead.status)&&daysAgo(lead.updatedAt||lead.createdAt)>7;`);
a(`              const isSel=selectedLead?.id===lead.id;`);
a(`              const isGV=parseFloat(lead.budget||0)>=GV_MIN;`);
a(`              return (`);
a(`                <div key={lead.id||i} onClick={()=>setSelectedLead(isSel?null:lead)}`);
a(`                  style={{display:"grid",gridTemplateColumns:"72px 2fr 110px 130px 110px 130px 140px 48px",padding:"11px 4px",borderBottom:"1px solid "+T.border+"40",cursor:"pointer",background:isSel?"rgba(212,168,67,0.04)":"transparent",transition:"background 0.1s",borderLeft:isSel?"3px solid "+T.gold:"3px solid transparent"}}`);
a(`                  onMouseEnter={e=>!isSel&&(e.currentTarget.style.background="rgba(255,255,255,0.02)")}`);
a(`                  onMouseLeave={e=>!isSel&&(e.currentTarget.style.background="transparent")}`);
a(`                >`);
a(`                  <div style={{display:"flex",alignItems:"center",gap:4,paddingLeft:4}}>`);
a(`                    <div style={{width:7,height:7,borderRadius:"50%",background:sc.color,flexShrink:0}}/>`);
a(`                    <span style={{fontSize:10,color:T.textMuted}}>#{(lead.id||"").slice(-4)||String(i+1).padStart(4,"0")}</span>`);
a(`                  </div>`);
a(`                  <div style={{display:"flex",alignItems:"center",gap:6}}>`);
a(`                    <div style={{width:22,height:22,borderRadius:"50%",background:"rgba(255,255,255,0.07)",display:"flex",alignItems:"center",justifyContent:"center",flexShrink:0,fontSize:10,fontWeight:700,color:T.textMuted}}>{(lead.name||"?")[0].toUpperCase()}</div>`);
a(`                    <div>`);
a(`                      <div style={{display:"flex",alignItems:"center",gap:4}}>`);
a(`                        <span style={{fontSize:12,fontWeight:500,color:T.white}}>{lead.name||"Unnamed"}</span>`);
a(`                      </div>`);
a(`                      <div style={{display:"flex",gap:4,marginTop:1}}>`);
a(`                        <ScoreBar lead={lead}/>`);
a(`                        {isGV&&<span style={{fontSize:9,color:T.gold}}>🏆GV</span>}`);
a(`                        {stale&&<span style={{fontSize:9,color:"#EF4444"}}>⏰Stale</span>}`);
a(`                      </div>`);
a(`                    </div>`);
a(`                  </div>`);
a(`                  <div style={{alignSelf:"center"}}><PBadge status={lead.status||"New Lead"}/></div>`);
a(`                  <div style={{fontSize:12,fontWeight:700,color:T.gold,alignSelf:"center"}}>{fmtB(lead.budget)}</div>`);
a(`                  <div style={{fontSize:11,color:lead.serviceType||lead.type?"#A78BFA":T.textMuted,alignSelf:"center",fontWeight:lead.serviceType||lead.type?600:400}}>{lead.serviceType||lead.type||"—"}</div>`);
a(`                  <div style={{display:"flex",alignItems:"center",gap:4,alignSelf:"center"}}>`);
a(`                    <div style={{width:6,height:6,borderRadius:"50%",background:SRC_COLOR[lead.source]||"#94A3B8",flexShrink:0}}/>`);
a(`                    <span style={{fontSize:10,color:T.textMuted}}>{lead.source||"—"}</span>`);
a(`                  </div>`);
a(`                  <div style={{alignSelf:"center"}}>`);
a(`                    {lead.assignedToName`);
a(`                      ?<span style={{fontSize:10,color:"#8B5CF6",fontWeight:600,background:"rgba(139,92,246,0.1)",padding:"2px 7px",borderRadius:8}}>{lead.assignedToName}</span>`);
a(`                      :canManage`);
a(`                        ?<button type="button" onClick={e=>{e.stopPropagation();setShowAssign(lead);}} style={{fontSize:10,color:T.textMuted,background:"rgba(255,255,255,0.05)",border:"1px dashed "+T.border,padding:"2px 7px",borderRadius:8,cursor:"pointer",fontFamily:"'Outfit',sans-serif"}}>+ Assign</button>`);
a(`                        :<span style={{fontSize:10,color:T.textMuted}}>—</span>`);
a(`                    }`);
a(`                  </div>`);
a(`                  <div style={{display:"flex",alignItems:"center",justifyContent:"center"}}>`);
a(`                    {lead.phone&&<a href={"https://wa.me/"+clnPhone(lead.phone)} target="_blank" rel="noopener noreferrer" onClick={e=>e.stopPropagation()} style={{padding:"3px 6px",borderRadius:5,background:"rgba(37,211,102,0.1)",border:"1px solid rgba(37,211,102,0.2)",textDecoration:"none",fontSize:9,color:"#25D366",fontWeight:700}}>WA</a>}`);
a(`                  </div>`);
a(`                </div>`);
a(`              );`);
a(`            })}`);
a(`          </div>`);
a(`        )}`);
a(``);
a(`        {/* ── KANBAN VIEW ── */}`);
a(`        {view==="kanban"&&(`);
a(`          <div style={{overflowX:"auto",padding:"12px 0"}}>`);
a(`            <div style={{display:"flex",gap:10,minWidth:"max-content",alignItems:"start"}}>`);
a(`              {PIPELINE.map(p=>{`);
a(`                const cols=filtered.filter(l=>(l.status||"New Lead")===p.key);`);
a(`                return (`);
a(`                  <div key={p.key} style={{width:210,flexShrink:0}}>`);
a(`                    <div style={{display:"flex",justifyContent:"space-between",alignItems:"center",marginBottom:7,padding:"7px 10px",background:p.bg,borderRadius:7,border:"1px solid "+p.color+"25"}}>`);
a(`                      <div style={{display:"flex",alignItems:"center",gap:5}}>`);
a(`                        <div style={{width:7,height:7,borderRadius:"50%",background:p.color}}/>`);
a(`                        <span style={{fontSize:11,fontWeight:600,color:p.color}}>{p.key}</span>`);
a(`                      </div>`);
a(`                      <span style={{fontSize:10,color:p.color,background:p.color+"15",padding:"1px 6px",borderRadius:8}}>{cols.length}</span>`);
a(`                    </div>`);
a(`                    <div style={{display:"flex",flexDirection:"column",gap:5,maxHeight:540,overflowY:"auto"}}>`);
a(`                      {cols.map((lead,i)=>{`);
a(`                        const sc=aiScore(lead);`);
a(`                        const stale=daysAgo(lead.updatedAt||lead.createdAt)>7;`);
a(`                        return (`);
a(`                          <div key={lead.id||i} onClick={()=>setSelectedLead(selectedLead?.id===lead.id?null:lead)}`);
a(`                            style={{background:"rgba(255,255,255,0.03)",border:"1px solid "+(stale?"rgba(239,68,68,0.2)":T.border),borderRadius:8,padding:"9px 10px",cursor:"pointer"}}`);
a(`                            onMouseEnter={e=>(e.currentTarget.style.borderColor=p.color+"50")}`);
a(`                            onMouseLeave={e=>(e.currentTarget.style.borderColor=stale?"rgba(239,68,68,0.2)":T.border)}`);
a(`                          >`);
a(`                            <div style={{display:"flex",justifyContent:"space-between",marginBottom:4}}>`);
a(`                              <span style={{fontSize:11,fontWeight:500,color:T.white,overflow:"hidden",textOverflow:"ellipsis",whiteSpace:"nowrap",flex:1}}>{lead.name||"Unnamed"}</span>`);
a(`                              <div style={{width:6,height:6,borderRadius:"50%",background:sc.color,flexShrink:0,marginTop:3}}/>`);
a(`                            </div>`);
a(`                            <div style={{fontSize:11,fontWeight:700,color:T.gold,marginBottom:4}}>{fmtB(lead.budget)}</div>`);
a(`                            <div style={{display:"flex",justifyContent:"space-between",alignItems:"center"}}>`);
a(`                              <span style={{fontSize:9,color:SRC_COLOR[lead.source]||T.textMuted}}>{lead.source||"Manual"}</span>`);
a(`                              {stale&&<span style={{fontSize:9,color:"#EF4444"}}>Stale</span>}`);
a(`                            </div>`);
a(`                            {lead.assignedToName&&<div style={{fontSize:9,color:"#8B5CF6",marginTop:3,fontWeight:600}}>{lead.assignedToName}</div>}`);
a(`                          </div>`);
a(`                        );`);
a(`                      })}`);
a(`                      {cols.length===0&&<div style={{padding:"14px",textAlign:"center",color:T.textMuted,fontSize:10,border:"1px dashed "+T.border+"50",borderRadius:7}}>No leads</div>}`);
a(`                    </div>`);
a(`                  </div>`);
a(`                );`);
a(`              })}`);
a(`            </div>`);
a(`          </div>`);
a(`        )}`);
a(``);
a(`        {/* ── ANALYTICS VIEW ── */}`);
a(`        {view==="analytics"&&(`);
a(`          <div style={{padding:"14px 4px",display:"flex",flexDirection:"column",gap:14}}>`);
a(``);
a(`            {/* KPI Cards */}`);
a(`            <div style={{display:"grid",gridTemplateColumns:"repeat(auto-fill,minmax(130px,1fr))",gap:8}}>`);
a(`              {[`);
a(`                {l:"Total Leads",  v:kTotal,   c:T.gold},`);
a(`                {l:"New Today",    v:kToday,   c:"#0EA5E9"},`);
a(`                {l:"Hot Leads",    v:kHot,     c:"#10B981"},`);
a(`                {l:"Closed Deals", v:kClosed,  c:"#10B981"},`);
a(`                {l:"Stale 7d+",    v:kStale,   c:"#EF4444"},`);
a(`                {l:"Conv. Rate",   v:kConvRate+"%",c:kConvRate>=10?"#10B981":kConvRate>=5?"#D4A843":"#EF4444"},`);
a(`                {l:"Pipeline",     v:kPipeline>=1e9?"AED "+(kPipeline/1e9).toFixed(1)+"B":kPipeline>=1e6?"AED "+(kPipeline/1e6).toFixed(1)+"M":"AED "+kPipeline.toLocaleString(),c:T.gold},`);
a(`              ].map((k,i)=>(`);
a(`                <div key={i} style={{background:"rgba(255,255,255,0.03)",border:"1px solid "+T.border,borderRadius:9,padding:"10px 12px",position:"relative",overflow:"hidden"}}>`);
a(`                  <div style={{position:"absolute",top:0,left:0,right:0,height:2,background:k.c,opacity:0.8}}/>`);
a(`                  <div style={{fontSize:9,fontWeight:700,color:T.textMuted,textTransform:"uppercase",letterSpacing:0.7,marginBottom:3}}>{k.l}</div>`);
a(`                  <div style={{fontSize:20,fontWeight:900,color:k.c,fontFamily:"'Fraunces',serif"}}>{k.v}</div>`);
a(`                </div>`);
a(`              ))}`);
a(`            </div>`);
a(``);
a(`            {/* ── OWNER LEADERBOARD ── */}`);
a(`            {(isOwner||isSuperAdmin)&&leaderboard.length>0&&(`);
a(`              <div style={{background:"rgba(255,255,255,0.02)",border:"1px solid "+T.gold+"40",borderRadius:12,overflow:"hidden"}}>`);
a(`                <div style={{padding:"14px 16px",borderBottom:"1px solid "+T.border,background:"rgba(212,168,67,0.05)",display:"flex",justifyContent:"space-between",alignItems:"center"}}>`);
a(`                  <div>`);
a(`                    <div style={{fontSize:13,fontWeight:700,color:T.gold}}>🏆 Agent Leaderboard</div>`);
a(`                    <div style={{fontSize:11,color:T.textMuted,marginTop:2}}>Ranked by closed deals · conversion rate · pipeline value</div>`);
a(`                  </div>`);
a(`                  <div style={{fontSize:11,color:T.textMuted}}>{agents.length} agents</div>`);
a(`                </div>`);
a(`                <div style={{display:"grid",gridTemplateColumns:"32px 2fr 1fr 1fr 1fr 1fr 1fr",padding:"8px 16px",background:"rgba(255,255,255,0.02)",borderBottom:"1px solid "+T.border}}>`);
a(`                  {["#","Agent","Leads","Closed","Conv%","Pipeline","Score"].map((h,i)=>(`);
a(`                    <div key={i} style={{fontSize:9,fontWeight:700,color:T.textMuted,textTransform:"uppercase",letterSpacing:0.7}}>{h}</div>`);
a(`                  ))}`);
a(`                </div>`);
a(`                {leaderboard.slice(0,10).map((agent,i)=>(`);
a(`                  <div key={agent.uid||agent.id||i}`);
a(`                    style={{display:"grid",gridTemplateColumns:"32px 2fr 1fr 1fr 1fr 1fr 1fr",padding:"12px 16px",borderBottom:i<leaderboard.length-1?"1px solid "+T.border+"40":"none",background:i===0?"rgba(212,168,67,0.03)":i===1?"rgba(212,168,67,0.015)":"transparent"}}`);
a(`                    onMouseEnter={e=>(e.currentTarget.style.background="rgba(255,255,255,0.02)")}`);
a(`                    onMouseLeave={e=>(e.currentTarget.style.background=i===0?"rgba(212,168,67,0.03)":i===1?"rgba(212,168,67,0.015)":"transparent")}`);
a(`                  >`);
a(`                    <div style={{fontSize:12,fontWeight:700,color:i===0?"#F59E0B":i===1?"#94A3B8":i===2?"#B97333":T.textMuted,alignSelf:"center"}}>{i+1}</div>`);
a(`                    <div>`);
a(`                      <div style={{fontSize:12,fontWeight:600,color:T.white}}>{agent.name||"Agent"}</div>`);
a(`                      <div style={{fontSize:10,color:T.textMuted}}>{agent.email||""}</div>`);
a(`                    </div>`);
a(`                    <div style={{fontSize:13,fontWeight:700,color:T.gold,alignSelf:"center"}}>{agent.total}</div>`);
a(`                    <div style={{fontSize:13,color:"#10B981",fontWeight:700,alignSelf:"center"}}>{agent.closed}</div>`);
a(`                    <div style={{fontSize:13,color:parseFloat(agent.conv)>=10?"#10B981":parseFloat(agent.conv)>=5?"#D4A843":"#EF4444",fontWeight:700,alignSelf:"center"}}>{agent.conv}%</div>`);
a(`                    <div style={{fontSize:11,color:T.gold,fontWeight:600,alignSelf:"center"}}>{agent.pipeline>=1e6?"AED "+(agent.pipeline/1e6).toFixed(1)+"M":"AED "+agent.pipeline.toLocaleString()}</div>`);
a(`                    <div style={{fontSize:13,color:T.gold,fontWeight:900,alignSelf:"center",fontFamily:"'Fraunces',serif"}}>{agent.combined}</div>`);
a(`                  </div>`);
a(`                ))}`);
a(`              </div>`);
a(`            )}`);
a(``);
a(`            {/* ── AGENT PERFORMANCE TABLE (manager/director) ── */}`);
a(`            {(isManager||isDirector)&&agentPerf.length>0&&(`);
a(`              <div style={{background:"rgba(255,255,255,0.02)",border:"1px solid "+T.border,borderRadius:12,overflow:"hidden"}}>`);
a(`                <div style={{padding:"14px 16px",borderBottom:"1px solid "+T.border,display:"flex",justifyContent:"space-between",alignItems:"center"}}>`);
a(`                  <div style={{fontSize:13,fontWeight:700,color:T.white}}>Agent Performance</div>`);
a(`                  <div style={{fontSize:11,color:T.textMuted}}>{agents.length} agents · {allLeads.length} leads</div>`);
a(`                </div>`);
a(`                <div style={{display:"grid",gridTemplateColumns:"2fr 1fr 1fr 1fr 1fr 1fr 1fr",padding:"8px 16px",background:"rgba(255,255,255,0.02)",borderBottom:"1px solid "+T.border}}>`);
a(`                  {["Agent","Leads","Hot","Closed","Conv%","Stale","Last Active"].map((h,i)=>(`);
a(`                    <div key={i} style={{fontSize:9,fontWeight:700,color:T.textMuted,textTransform:"uppercase",letterSpacing:0.7}}>{h}</div>`);
a(`                  ))}`);
a(`                </div>`);
a(`                {agentPerf.map((agent,i)=>(`);
a(`                  <div key={agent.uid||agent.id||i}`);
a(`                    style={{display:"grid",gridTemplateColumns:"2fr 1fr 1fr 1fr 1fr 1fr 1fr",padding:"12px 16px",borderBottom:i<agentPerf.length-1?"1px solid "+T.border+"40":"none",cursor:"pointer"}}`);
a(`                    onMouseEnter={e=>(e.currentTarget.style.background="rgba(255,255,255,0.02)")}`);
a(`                    onMouseLeave={e=>(e.currentTarget.style.background="transparent")}`);
a(`                    onClick={()=>setFilterAgent(agent.uid||agent.id||"all")}`);
a(`                  >`);
a(`                    <div>`);
a(`                      <div style={{fontSize:12,fontWeight:600,color:T.white}}>{agent.name||"Agent"}</div>`);
a(`                      <div style={{fontSize:10,color:T.textMuted}}>{agent.email||""}</div>`);
a(`                    </div>`);
a(`                    <div style={{fontSize:13,fontWeight:700,color:T.gold,alignSelf:"center"}}>{agent.total}</div>`);
a(`                    <div style={{fontSize:13,color:"#10B981",fontWeight:600,alignSelf:"center"}}>{agent.hot}</div>`);
a(`                    <div style={{fontSize:13,color:"#10B981",fontWeight:700,alignSelf:"center"}}>{agent.closed}</div>`);
a(`                    <div style={{fontSize:13,color:parseFloat(agent.conv)>=10?"#10B981":parseFloat(agent.conv)>=5?"#D4A843":"#EF4444",fontWeight:700,alignSelf:"center"}}>{agent.conv}%</div>`);
a(`                    <div style={{fontSize:13,color:agent.stale>0?"#EF4444":T.textMuted,fontWeight:agent.stale>0?700:400,alignSelf:"center"}}>{agent.stale}</div>`);
a(`                    <div style={{fontSize:11,color:T.textMuted,alignSelf:"center"}}>{agent.lastActive?fmtD(agent.lastActive):"Never"}</div>`);
a(`                  </div>`);
a(`                ))}`);
a(`                {agentPerf.length===0&&<div style={{padding:"20px",textAlign:"center",color:T.textMuted,fontSize:12}}>No agents in your team yet</div>}`);
a(`              </div>`);
a(`            )}`);
a(``);
a(`            {/* Source Performance */}`);
a(`            <div style={{background:"rgba(255,255,255,0.02)",border:"1px solid "+T.border,borderRadius:12,padding:"16px"}}>`);
a(`              <div style={{fontSize:13,fontWeight:700,color:T.white,marginBottom:14}}>Source Performance</div>`);
a(`              <div style={{display:"grid",gridTemplateColumns:"repeat(auto-fill,minmax(130px,1fr))",gap:8}}>`);
a(`                {srcStats.slice(0,8).map((s,i)=>(`);
a(`                  <div key={i} style={{background:"rgba(255,255,255,0.03)",borderRadius:8,padding:"10px"}}>`);
a(`                    <div style={{fontSize:11,fontWeight:700,color:SRC_COLOR[s.src]||T.textMuted,marginBottom:5,overflow:"hidden",textOverflow:"ellipsis",whiteSpace:"nowrap"}}>{s.src}</div>`);
a(`                    <div style={{display:"flex",justifyContent:"space-between",alignItems:"flex-end"}}>`);
a(`                      <div><div style={{fontSize:18,fontWeight:900,color:T.white,fontFamily:"'Fraunces',serif",lineHeight:1}}>{s.total}</div><div style={{fontSize:9,color:T.textMuted}}>leads</div></div>`);
a(`                      <div style={{textAlign:"right"}}><div style={{fontSize:14,fontWeight:900,color:s.rate>=10?"#10B981":s.rate>=5?"#D4A843":"#EF4444",fontFamily:"'Fraunces',serif",lineHeight:1}}>{s.rate}%</div><div style={{fontSize:9,color:T.textMuted}}>closed</div></div>`);
a(`                    </div>`);
a(`                    <div style={{height:2,background:T.border,borderRadius:1,marginTop:7}}><div style={{height:"100%",width:Math.min(s.total,100)+"%",background:SRC_COLOR[s.src]||T.gold,borderRadius:1,opacity:0.7}}/></div>`);
a(`                  </div>`);
a(`                ))}`);
a(`                {srcStats.length===0&&<div style={{color:T.textMuted,fontSize:12,padding:"20px",textAlign:"center"}}>No source data yet</div>}`);
a(`              </div>`);
a(`            </div>`);
a(``);
a(`            {/* Pipeline Distribution */}`);
a(`            <div style={{background:"rgba(255,255,255,0.02)",border:"1px solid "+T.border,borderRadius:12,padding:"16px"}}>`);
a(`              <div style={{fontSize:13,fontWeight:700,color:T.white,marginBottom:14}}>Pipeline Distribution</div>`);
a(`              {PIPELINE.map(p=>{`);
a(`                const count=allLeads.filter(l=>(l.status||"New Lead")===p.key).length;`);
a(`                const pct=allLeads.length>0?Math.round(count*100/allLeads.length):0;`);
a(`                return(`);
a(`                  <div key={p.key} style={{marginBottom:8}}>`);
a(`                    <div style={{display:"flex",justifyContent:"space-between",marginBottom:3}}>`);
a(`                      <div style={{display:"flex",alignItems:"center",gap:6}}><div style={{width:6,height:6,borderRadius:"50%",background:p.color}}/><span style={{fontSize:11,color:T.textSecondary}}>{p.key}</span></div>`);
a(`                      <span style={{fontSize:11,fontWeight:700,color:p.color}}>{count} <span style={{color:T.textMuted,fontWeight:400}}>({pct}%)</span></span>`);
a(`                    </div>`);
a(`                    <div style={{height:4,borderRadius:2,background:"rgba(255,255,255,0.05)"}}><div style={{height:"100%",width:pct+"%",borderRadius:2,background:p.color,opacity:0.85,transition:"width 0.8s"}}/></div>`);
a(`                  </div>`);
a(`                );`);
a(`              })}`);
a(`            </div>`);
a(``);
a(`          </div>`);
a(`        )}`);
a(``);
a(`        {/* ══ LEAD DETAIL DRAWER ════════════════════════════════════ */}`);
a(`        {selectedLead&&(`);
a(`          <div style={{background:"rgba(255,255,255,0.02)",borderLeft:"1px solid "+T.border,display:"flex",flexDirection:"column",maxHeight:"calc(100vh - 160px)",position:"sticky",top:0,overflowY:"auto"}}>`);
a(`            <div style={{padding:"13px 15px",borderBottom:"1px solid "+T.border,background:"rgba(255,255,255,0.02)"}}>`);
a(`              <div style={{display:"flex",justifyContent:"space-between",alignItems:"flex-start",marginBottom:7}}>`);
a(`                <div style={{display:"flex",alignItems:"center",gap:8}}>`);
a(`                  <div style={{width:30,height:30,borderRadius:"50%",background:"rgba(255,255,255,0.07)",display:"flex",alignItems:"center",justifyContent:"center",flexShrink:0,fontSize:12,fontWeight:700,color:T.textMuted}}>{(selectedLead.name||"?")[0].toUpperCase()}</div>`);
a(`                  <div>`);
a(`                    <div style={{fontSize:14,fontWeight:700,color:T.white}}>{selectedLead.name||"Unnamed"}</div>`);
a(`                    <PBadge status={selectedLead.status||"New Lead"}/>`);
a(`                  </div>`);
a(`                </div>`);
a(`                <button type="button" onClick={()=>setSelectedLead(null)} style={{background:"none",border:"none",color:T.textMuted,cursor:"pointer",fontSize:18}}>×</button>`);
a(`              </div>`);
a(`              <div style={{display:"flex",gap:6,flexWrap:"wrap"}}>`);
a(`                {selectedLead.phone&&<a href={"https://wa.me/"+clnPhone(selectedLead.phone)} target="_blank" rel="noopener noreferrer" style={{fontSize:10,padding:"4px 10px",borderRadius:7,background:"rgba(37,211,102,0.1)",border:"1px solid rgba(37,211,102,0.25)",color:"#25D366",textDecoration:"none",fontWeight:600}}>WhatsApp</a>}`);
a(`                {selectedLead.phone&&<a href={"tel:"+selectedLead.phone} style={{fontSize:10,padding:"4px 10px",borderRadius:7,background:"rgba(99,179,237,0.1)",border:"1px solid rgba(99,179,237,0.25)",color:"#63B3ED",textDecoration:"none",fontWeight:600}}>Call</a>}`);
a(`                {canManage&&<button type="button" onClick={()=>setShowAssign(selectedLead)} style={{fontSize:10,padding:"4px 10px",borderRadius:7,background:"rgba(139,92,246,0.1)",border:"1px solid rgba(139,92,246,0.3)",color:"#8B5CF6",cursor:"pointer",fontFamily:"'Outfit',sans-serif",fontWeight:600}}>Reassign</button>}`);
a(`                {parseFloat(selectedLead.budget||0)>=GV_MIN&&<span style={{fontSize:10,padding:"4px 8px",borderRadius:7,background:"rgba(212,168,67,0.15)",color:T.gold,fontWeight:700}}>🏆 Golden Visa</span>}`);
a(`              </div>`);
a(`              <div style={{marginTop:8}}>`);
a(`                <div style={{fontSize:9,color:T.textMuted,marginBottom:4,fontWeight:700,textTransform:"uppercase",letterSpacing:0.5}}>Change Status</div>`);
a(`                <div style={{display:"flex",gap:4,flexWrap:"wrap"}}>`);
a(`                  {PIPELINE.map(p=>(`);
a(`                    <button key={p.key} type="button" onClick={()=>updateStatus(selectedLead.id,p.key)}`);
a(`                      style={{fontSize:9,padding:"3px 7px",borderRadius:8,border:"1px solid "+((selectedLead.status||"New Lead")===p.key?p.color:T.border),background:(selectedLead.status||"New Lead")===p.key?p.bg:"transparent",color:(selectedLead.status||"New Lead")===p.key?p.color:T.textMuted,cursor:"pointer",fontWeight:(selectedLead.status||"New Lead")===p.key?700:400,fontFamily:"'Outfit',sans-serif"}}>`);
a(`                      {p.key}`);
a(`                    </button>`);
a(`                  ))}`);
a(`                </div>`);
a(`              </div>`);
a(`            </div>`);
a(`            <div style={{display:"flex",borderBottom:"1px solid "+T.border}}>`);
a(`              {[{k:"profile",l:"Profile"},{k:"activity",l:"Activity"},{k:"match",l:"Matches"}].map(t=>(`);
a(`                <button key={t.k} type="button" onClick={()=>setDrawerTab(t.k)} style={{flex:1,padding:"8px 0",border:"none",borderBottom:drawerTab===t.k?"2px solid "+T.gold:"2px solid transparent",background:"transparent",color:drawerTab===t.k?T.gold:T.textMuted,fontSize:11,fontWeight:drawerTab===t.k?600:400,cursor:"pointer",fontFamily:"'Outfit',sans-serif"}}>{t.l}</button>`);
a(`              ))}`);
a(`            </div>`);
a(`            <div style={{padding:"13px 15px",flex:1}}>`);
a(`              {drawerTab==="profile"&&(()=>{`);
a(`                const sc=aiScore(selectedLead);`);
a(`                return (`);
a(`                  <div>`);
a(`                    <div style={{padding:"9px 12px",borderRadius:8,background:sc.color+"0E",border:"1px solid "+sc.color+"25",marginBottom:12,display:"flex",justifyContent:"space-between",alignItems:"center"}}>`);
a(`                      <div>`);
a(`                        <div style={{fontSize:9,color:T.textMuted,fontWeight:700,textTransform:"uppercase",letterSpacing:0.7,marginBottom:2}}>AI LEAD SCORE</div>`);
a(`                        <div style={{fontFamily:"'Fraunces',serif",fontSize:18,fontWeight:800,color:sc.color}}>{sc.score}/100 — {sc.label}</div>`);
a(`                      </div>`);
a(`                      <div style={{width:38,height:38,borderRadius:"50%",border:"2px solid "+sc.color,display:"flex",alignItems:"center",justifyContent:"center",background:sc.color+"12",flexShrink:0}}>`);
a(`                        <span style={{fontSize:12,fontWeight:800,color:sc.color,fontFamily:"'Fraunces',serif"}}>{sc.score}</span>`);
a(`                      </div>`);
a(`                    </div>`);
a(`                    {[`);
a(`                      {label:"Phone",       value:selectedLead.phone,   link:"tel:"+selectedLead.phone},`);
a(`                      {label:"WhatsApp",    value:selectedLead.phone?"Open":null, link:selectedLead.phone?"https://wa.me/"+clnPhone(selectedLead.phone):null, ext:true},`);
a(`                      {label:"Email",       value:selectedLead.email,   link:selectedLead.email?"mailto:"+selectedLead.email:null},`);
a(`                      {label:"Budget",      value:fmtB(selectedLead.budget)},`);
a(`                      {label:"Service",     value:selectedLead.serviceType||selectedLead.type||"—"},`);
a(`                      {label:"Request",     value:selectedLead.requestType||"—"},`);
a(`                      {label:"Project",     value:selectedLead.project||"—"},`);
a(`                      {label:"Source",      value:selectedLead.source||"—"},`);
a(`                      {label:"Channel",     value:selectedLead.channel||"—"},`);
a(`                      {label:"Campaign",    value:selectedLead.campaign||"—"},`);
a(`                      {label:"AD Name",     value:selectedLead.adName||"—"},`);
a(`                      {label:"AD Account",  value:selectedLead.adAccount||"—"},`);
a(`                      {label:"Nationality", value:selectedLead.nationality||"—"},`);
a(`                      {label:"Community",   value:selectedLead.community||"—"},`);
a(`                      {label:"Timeline",    value:selectedLead.timeline||"—"},`);
a(`                      {label:"Assigned To", value:selectedLead.assignedToName||"Unassigned",color:"#8B5CF6"},`);
a(`                      {label:"Added",       value:fmtD(selectedLead.createdAt)},`);
a(`                    ].map((f,i)=>(`);
a(`                      <div key={i} style={{display:"flex",justifyContent:"space-between",alignItems:"center",padding:"5px 0",borderBottom:"1px solid "+T.border+"30"}}>`);
a(`                        <span style={{fontSize:11,color:T.textMuted,flexShrink:0}}>{f.label}</span>`);
a(`                        {f.link`);
a(`                          ?<a href={f.link} target={f.ext?"_blank":"_self"} rel="noopener noreferrer" style={{fontSize:11,color:T.gold,fontWeight:600,textDecoration:"none"}}>{f.value}</a>`);
a(`                          :<span style={{fontSize:11,color:f.color||T.white,fontWeight:600,textAlign:"right",maxWidth:160,overflow:"hidden",textOverflow:"ellipsis"}}>{f.value||"—"}</span>`);
a(`                        }`);
a(`                      </div>`);
a(`                    ))}`);
a(`                    {selectedLead.comment&&<div style={{marginTop:9,padding:"8px 10px",background:"rgba(255,255,255,0.03)",borderRadius:7,fontSize:11,color:T.textSecondary,lineHeight:1.6}}><div style={{fontSize:9,color:T.textMuted,marginBottom:3,fontWeight:700,textTransform:"uppercase",letterSpacing:0.5}}>Comment</div>{selectedLead.comment}</div>}`);
a(`                  </div>`);
a(`                );`);
a(`              })()}`);
a(`              {drawerTab==="activity"&&(`);
a(`                <div>`);
a(`                  <div style={{display:"flex",flexDirection:"column",gap:6,marginBottom:12,maxHeight:260,overflowY:"auto"}}>`);
a(`                    {(selectedLead.notes_log||[]).length===0&&<div style={{padding:"20px",textAlign:"center",color:T.textMuted,fontSize:11}}>No activity yet</div>}`);
a(`                    {[...(selectedLead.notes_log||[])].reverse().map((n,i)=>(`);
a(`                      <div key={i} style={{padding:"7px 10px",background:"rgba(255,255,255,0.03)",borderRadius:7,borderLeft:"2px solid "+T.gold}}>`);
a(`                        <div style={{fontSize:11,color:T.white,marginBottom:2}}>{n.text}</div>`);
a(`                        <div style={{fontSize:9,color:T.textMuted}}>{n.type||"Note"} · {n.by||"Agent"} · {fmtD(n.at)}</div>`);
a(`                      </div>`);
a(`                    ))}`);
a(`                  </div>`);
a(`                  <div style={{borderTop:"1px solid "+T.border,paddingTop:10}}>`);
a(`                    <select value={noteType2} onChange={e=>setNoteType2(e.target.value)} style={{width:"100%",padding:"6px 8px",background:"rgba(255,255,255,0.04)",border:"1px solid "+T.border,borderRadius:6,color:T.textSecondary,fontSize:11,outline:"none",marginBottom:6,fontFamily:"'Outfit',sans-serif"}}>`);
a(`                      {NOTE_TYPES.map(t=><option key={t} value={t}>{t}</option>)}`);
a(`                    </select>`);
a(`                    <textarea value={noteText2} onChange={e=>setNoteText2(e.target.value)} placeholder="Add activity note..." rows={3} style={{width:"100%",padding:"7px 9px",background:"rgba(255,255,255,0.04)",border:"1px solid "+T.border,borderRadius:6,color:T.white,fontSize:12,outline:"none",resize:"none",boxSizing:"border-box",marginBottom:7,fontFamily:"'Outfit',sans-serif"}}/>`);
a(`                    <button type="button" onClick={()=>addNote(selectedLead.id)} disabled={!noteText2.trim()||savingNote} style={{width:"100%",padding:"7px",borderRadius:6,border:"none",background:!noteText2.trim()?"rgba(212,168,67,0.2)":"rgba(212,168,67,0.9)",color:"#0A0E1A",fontSize:12,fontWeight:700,cursor:!noteText2.trim()?"not-allowed":"pointer",fontFamily:"'Outfit',sans-serif"}}>{savingNote?"Saving...":"Save Note"}</button>`);
a(`                  </div>`);
a(`                </div>`);
a(`              )}`);
a(`              {drawerTab==="match"&&(`);
a(`                <div>`);
a(`                  <div style={{fontSize:11,color:T.textMuted,marginBottom:10}}>{matched.length>0?"Properties matching budget + area:":"No listings match yet."}</div>`);
a(`                  {matched.map((l,i)=>(`);
a(`                    <div key={i} style={{padding:"9px 11px",background:"rgba(255,255,255,0.03)",borderRadius:7,marginBottom:7,border:"1px solid "+T.border}}>`);
a(`                      <div style={{fontSize:12,fontWeight:600,color:T.white,marginBottom:3}}>{l.title||l.name||"Property"}</div>`);
a(`                      <div style={{display:"flex",justifyContent:"space-between"}}><span style={{fontSize:11,color:T.textMuted}}>{l.community||""}</span><span style={{fontSize:12,fontWeight:700,color:T.gold}}>{fmtB(l.price||l.priceMin)}</span></div>`);
a(`                    </div>`);
a(`                  ))}`);
a(`                  {matched.length===0&&<button type="button" onClick={()=>handleTabChange?.("Projects")} style={{width:"100%",padding:"8px",borderRadius:7,border:"1px solid "+T.border,background:"rgba(212,168,67,0.06)",color:T.gold,fontSize:11,cursor:"pointer",fontFamily:"'Outfit',sans-serif"}}>Browse Projects →</button>}`);
a(`                </div>`);
a(`              )}`);
a(`            </div>`);
a(`          </div>`);
a(`        )}`);
a(`      </div>`);
a(``);
a(`      {/* ══ ADD LEAD MODAL ═══════════════════════════════════════ */}`);
a(`      {showAdd&&(`);
a(`        <div style={{position:"fixed",inset:0,background:"rgba(4,9,15,0.9)",zIndex:2000,display:"flex",alignItems:"center",justifyContent:"center",padding:20}} onClick={e=>{if(e.target===e.currentTarget)setShowAdd(false);}}>`);
a(`          <div style={{background:"#0D1117",borderRadius:14,border:"1px solid "+T.border,width:"100%",maxWidth:620,maxHeight:"92vh",overflowY:"auto"}} onClick={e=>e.stopPropagation()}>`);
a(`            <div style={{padding:"15px 20px",borderBottom:"1px solid "+T.border,display:"flex",justifyContent:"space-between",alignItems:"center",position:"sticky",top:0,background:"#0D1117",zIndex:1}}>`);
a(`              <div style={{fontFamily:"'Fraunces',serif",fontSize:15,fontWeight:900,color:T.white}}>Add New Lead</div>`);
a(`              <button type="button" onClick={()=>setShowAdd(false)} style={{background:"rgba(255,255,255,0.06)",border:"1px solid "+T.border,borderRadius:7,color:T.textMuted,width:28,height:28,cursor:"pointer",fontSize:16,display:"flex",alignItems:"center",justifyContent:"center"}}>×</button>`);
a(`            </div>`);
a(`            <div style={{padding:20}}>`);
a(`              <div style={{marginBottom:10}}>`);
a(`                <div style={{fontSize:11,fontWeight:700,color:T.white,marginBottom:10,paddingBottom:6,borderBottom:"1px solid "+T.border}}>Contact Information</div>`);
a(`                <div style={{display:"grid",gridTemplateColumns:"1fr 1fr",gap:10}}>`);
a(`                  <div><Lbl>Full Name *</Lbl><Inp value={form.name} onChange={v=>F("name",v)} placeholder="Client full name"/></div>`);
a(`                  <div><Lbl>Phone *</Lbl><Inp value={form.phone} onChange={v=>F("phone",v)} placeholder="+971 50 XXX XXXX" type="tel"/></div>`);
a(`                  <div><Lbl>Email</Lbl><Inp value={form.email} onChange={v=>F("email",v)} placeholder="email@example.com" type="email"/></div>`);
a(`                  <div><Lbl>Budget (AED)</Lbl><Inp value={form.budget} onChange={v=>F("budget",v)} placeholder="e.g. 2000000" type="number"/></div>`);
a(`                </div>`);
a(`              </div>`);
a(`              <Section icon="📋" title="Request Information" sub="Project and classification details" color="#818CF8" open={openSec.request} onToggle={()=>setOpenSec(s=>({...s,request:!s.request}))}>`);
a(`                <div style={{display:"grid",gridTemplateColumns:"1fr 1fr",gap:10}}>`);
a(`                  <div><Lbl>Request Type</Lbl><Sel value={form.requestType} onChange={v=>F("requestType",v)}><option value="">Select...</option>{REQUEST_TYPES.map(t=><option key={t} value={t}>{t}</option>)}</Sel></div>`);
a(`                  <div><Lbl>Service Type</Lbl><Sel value={form.serviceType} onChange={v=>F("serviceType",v)}><option value="">Select...</option>{SERVICE_TYPES.map(t=><option key={t} value={t}>{t}</option>)}</Sel></div>`);
a(`                  <div style={{gridColumn:"1/-1"}}><Lbl>Project</Lbl><Inp value={form.project} onChange={v=>F("project",v)} placeholder="Project name"/></div>`);
a(`                  <div><Lbl>Lead Source</Lbl><Sel value={form.source} onChange={v=>F("source",v)}><option value="">Select...</option>{LEAD_SOURCES.map(s=><option key={s} value={s}>{s}</option>)}</Sel></div>`);
a(`                  <div><Lbl>Channel</Lbl><Sel value={form.channel} onChange={v=>F("channel",v)}><option value="">Select...</option>{CHANNELS.map(c=><option key={c} value={c}>{c}</option>)}</Sel></div>`);
a(`                  <div style={{gridColumn:"1/-1"}}><Lbl>Campaign</Lbl><Inp value={form.campaign} onChange={v=>F("campaign",v)} placeholder="Campaign name"/></div>`);
a(`                  <div><Lbl>AD Name</Lbl><Inp value={form.adName} onChange={v=>F("adName",v)} placeholder="Ad name"/></div>`);
a(`                  <div><Lbl>AD Account</Lbl><Inp value={form.adAccount} onChange={v=>F("adAccount",v)} placeholder="Ad account"/></div>`);
a(`                  <div style={{gridColumn:"1/-1"}}><Lbl>Comment</Lbl><textarea value={form.comment} onChange={e=>F("comment",e.target.value)} placeholder="Additional notes..." rows={3} style={{width:"100%",padding:"8px 10px",background:"rgba(255,255,255,0.04)",border:"1px solid "+T.border,borderRadius:7,color:T.white,fontSize:12,outline:"none",resize:"none",boxSizing:"border-box",fontFamily:"'Outfit',sans-serif"}}/></div>`);
a(`                </div>`);
a(`              </Section>`);
a(`              <Section icon="👤" title="Agent Information" sub="Assign to a sales agent" color="#FB923C" open={openSec.agent} onToggle={()=>setOpenSec(s=>({...s,agent:!s.agent}))}>`);
a(`                <div style={{display:"grid",gridTemplateColumns:"1fr 1fr",gap:10}}>`);
a(`                  <div>`);
a(`                    <Lbl>Assign To Agent</Lbl>`);
a(`                    <Sel value={form.agentId} onChange={v=>{const agent=agents.find(a=>(a.uid||a.id)===v);F("agentId",v);if(agent)F("agentName",agent.name);}}>`);
a(`                      <option value="">Unassigned</option>`);
a(`                      {isAgent&&<option value={currentUid}>Myself</option>}`);
a(`                      {agents.filter(a=>(a.uid||a.id)!==currentUid).map(a=><option key={a.uid||a.id} value={a.uid||a.id}>{a.name}</option>)}`);
a(`                    </Sel>`);
a(`                  </div>`);
a(`                  <div><Lbl>Pipeline Status</Lbl><Sel value={form.status} onChange={v=>F("status",v)}>{PIPELINE.map(p=><option key={p.key} value={p.key}>{p.key}</option>)}</Sel></div>`);
a(`                </div>`);
a(`              </Section>`);
a(`              <Section icon="📍" title="Additional Details" sub="Community, nationality, timeline" color="#10B981" open={openSec.extra} onToggle={()=>setOpenSec(s=>({...s,extra:!s.extra}))}>`);
a(`                <div style={{display:"grid",gridTemplateColumns:"1fr 1fr 1fr",gap:10}}>`);
a(`                  <div><Lbl>Nationality</Lbl><Sel value={form.nationality} onChange={v=>F("nationality",v)}><option value="">Select...</option>{NATS.map(n=><option key={n} value={n}>{n}</option>)}</Sel></div>`);
a(`                  <div><Lbl>Community</Lbl><Inp value={form.community} onChange={v=>F("community",v)} placeholder="e.g. Dubai Marina"/></div>`);
a(`                  <div><Lbl>Timeline</Lbl><Sel value={form.timeline} onChange={v=>F("timeline",v)}><option value="">Select...</option>{TIMELINES.map(t=><option key={t} value={t}>{t}</option>)}</Sel></div>`);
a(`                </div>`);
a(`              </Section>`);
a(`              <button type="button" disabled={!form.name||!form.phone||saving} onClick={saveLead}`);
a(`                style={{width:"100%",padding:"12px",borderRadius:8,border:"none",background:(!form.name||!form.phone)?"rgba(212,168,67,0.3)":"linear-gradient(135deg,#D4A843,#B8902E)",color:"#0A0E1A",fontSize:14,fontWeight:700,cursor:(!form.name||!form.phone)?"not-allowed":"pointer",fontFamily:"'Outfit',sans-serif"}}>`);
a(`                {saving?"Saving...":"Save Lead"}`);
a(`              </button>`);
a(`            </div>`);
a(`          </div>`);
a(`        </div>`);
a(`      )}`);
a(``);
a(`      {/* ══ ASSIGN MODAL ════════════════════════════════════════ */}`);
a(`      {showAssign&&(`);
a(`        <div style={{position:"fixed",inset:0,background:"rgba(4,9,15,0.88)",zIndex:2100,display:"flex",alignItems:"center",justifyContent:"center",padding:20}} onClick={e=>{if(e.target===e.currentTarget)setShowAssign(null);}}>`);
a(`          <div style={{background:"#0D1117",borderRadius:14,border:"1px solid "+T.border,width:"100%",maxWidth:400,padding:22}} onClick={e=>e.stopPropagation()}>`);
a(`            <div style={{display:"flex",justifyContent:"space-between",alignItems:"center",marginBottom:16}}>`);
a(`              <div>`);
a(`                <div style={{fontFamily:"'Fraunces',serif",fontSize:15,fontWeight:900,color:T.white}}>Assign Lead</div>`);
a(`                <div style={{fontSize:11,color:T.textMuted,marginTop:2}}>{showAssign.name||"Lead"}</div>`);
a(`              </div>`);
a(`              <button type="button" onClick={()=>setShowAssign(null)} style={{background:"rgba(255,255,255,0.06)",border:"1px solid "+T.border,borderRadius:7,color:T.textMuted,width:28,height:28,cursor:"pointer",display:"flex",alignItems:"center",justifyContent:"center",fontSize:16}}>×</button>`);
a(`            </div>`);
a(`            {agents.length===0&&<div style={{padding:"20px",textAlign:"center",color:T.textMuted,fontSize:12}}>No agents in your team yet. Create agents from the Team tab.</div>}`);
a(`            <div style={{display:"flex",flexDirection:"column",gap:8}}>`);
a(`              {agents.map(agent=>(`);
a(`                <button key={agent.uid||agent.id} type="button" onClick={()=>assignLead(showAssign.id,agent)}`);
a(`                  style={{padding:"12px 14px",borderRadius:9,border:"1px solid "+(showAssign.assignedTo===(agent.uid||agent.id)?"#8B5CF6":T.border),background:showAssign.assignedTo===(agent.uid||agent.id)?"rgba(139,92,246,0.1)":"rgba(255,255,255,0.03)",textAlign:"left",cursor:"pointer",display:"flex",alignItems:"center",gap:12}}`);
a(`                  onMouseEnter={e=>(e.currentTarget.style.background="rgba(139,92,246,0.1)")}`);
a(`                  onMouseLeave={e=>(e.currentTarget.style.background=showAssign.assignedTo===(agent.uid||agent.id)?"rgba(139,92,246,0.1)":"rgba(255,255,255,0.03)")}`);
a(`                >`);
a(`                  <div style={{width:34,height:34,borderRadius:"50%",background:"rgba(139,92,246,0.15)",display:"flex",alignItems:"center",justifyContent:"center",fontSize:14,fontWeight:700,color:"#8B5CF6",flexShrink:0}}>{(agent.name||"A")[0].toUpperCase()}</div>`);
a(`                  <div>`);
a(`                    <div style={{fontSize:13,fontWeight:600,color:T.white}}>{agent.name||"Agent"}</div>`);
a(`                    <div style={{fontSize:10,color:T.textMuted}}>{agent.email||""}</div>`);
a(`                  </div>`);
a(`                  {showAssign.assignedTo===(agent.uid||agent.id)&&<span style={{marginLeft:"auto",fontSize:10,color:"#8B5CF6",fontWeight:700}}>Current</span>}`);
a(`                </button>`);
a(`              ))}`);
a(`            </div>`);
a(`          </div>`);
a(`        </div>`);
a(`      )}`);
a(``);
a(`      {/* ══ WA TEMPLATES MODAL ══════════════════════════════════ */}`);
a(`      {showWA&&(`);
a(`        <div style={{position:"fixed",inset:0,background:"rgba(4,9,15,0.9)",zIndex:2100,display:"flex",alignItems:"center",justifyContent:"center",padding:20}} onClick={e=>{if(e.target===e.currentTarget)setShowWA(false);}}>`);
a(`          <div style={{background:"#0D1117",borderRadius:14,border:"1px solid "+T.border,width:"100%",maxWidth:480,padding:22}} onClick={e=>e.stopPropagation()}>`);
a(`            <div style={{display:"flex",justifyContent:"space-between",alignItems:"center",marginBottom:16}}>`);
a(`              <div>`);
a(`                <div style={{fontFamily:"'Fraunces',serif",fontSize:15,fontWeight:900,color:T.white}}>WhatsApp Templates</div>`);
a(`                <div style={{fontSize:11,color:T.textMuted,marginTop:2}}>{filtered.filter(l=>l.phone).length} leads with phone</div>`);
a(`              </div>`);
a(`              <button type="button" onClick={()=>setShowWA(false)} style={{background:"rgba(255,255,255,0.06)",border:"1px solid "+T.border,borderRadius:7,color:T.textMuted,width:28,height:28,cursor:"pointer",display:"flex",alignItems:"center",justifyContent:"center",fontSize:16}}>×</button>`);
a(`            </div>`);
a(`            <div style={{display:"flex",flexDirection:"column",gap:7}}>`);
a(`              {WA_TEMPLATES.map((tmpl,i)=>(`);
a(`                <button key={i} type="button" onClick={()=>{const lead=selectedLead||filtered.find(l=>l.phone);if(lead)sendWA(tmpl,lead);else{notify("No leads with phone","error");setShowWA(false);}}} style={{padding:"11px 13px",borderRadius:8,border:"1px solid rgba(37,211,102,0.2)",background:"rgba(37,211,102,0.04)",textAlign:"left",cursor:"pointer"}} onMouseEnter={e=>(e.currentTarget.style.background="rgba(37,211,102,0.1)")} onMouseLeave={e=>(e.currentTarget.style.background="rgba(37,211,102,0.04)")}>`);
a(`                  <div style={{fontSize:12,fontWeight:700,color:"#25D366",marginBottom:3}}>{tmpl.label}</div>`);
a(`                  <div style={{fontSize:10,color:T.textMuted,lineHeight:1.5}}>{tmpl.body.slice(0,100)}...</div>`);
a(`                </button>`);
a(`              ))}`);
a(`            </div>`);
a(`          </div>`);
a(`        </div>`);
a(`      )}`);
a(``);
a(`      {/* ══ TOAST ══════════════════════════════════════════════════ */}`);
a(`      {toast&&<div style={{position:"fixed",bottom:24,right:24,padding:"11px 18px",background:toast.type==="error"?"rgba(239,68,68,0.15)":"rgba(16,185,129,0.15)",border:"1px solid "+(toast.type==="error"?"#EF4444":"#10B981"),borderRadius:9,color:toast.type==="error"?"#EF4444":"#10B981",fontSize:12,fontWeight:600,zIndex:9999,boxShadow:"0 8px 32px rgba(0,0,0,0.4)"}}>{toast.msg}</div>}`);
a(``);
a(`    </div>`);
a(`  );`);
a(`}`);

fs.writeFileSync("src/tabs/MyLeadsTab.jsx", lines.join("\n"), "utf8");
console.log("Done. Lines:", lines.length);