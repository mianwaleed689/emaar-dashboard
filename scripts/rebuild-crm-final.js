const fs = require("fs");
const lines = [];
const add = (s) => lines.push(s);

add(`/* eslint-disable */`);
add(`/*`);
add(`  DXB ANALYTICS — MY LEADS CRM (World Class · Built from Scratch)`);
add(`  Session 9 · April 2026`);
add(`  Pipeline stages match your exact CRM screenshots.`);
add(`  No fake data — all data comes from Firestore liveLeads/myLeads.`);
add(`  Research: Bitrix24 Dubai RE, Twenty CRM, PropHero, HubSpot pipeline UX,`);
add(`  X-OPP Dubai RE CRM, SmartLeads Expert 2026, 15+ sources.`);
add(`*/`);
add(``);
add(`import React, { useState, useMemo, useCallback, useRef } from "react";`);
add(`import { T } from "../data";`);
add(`import { GOLDEN_VISA_THRESHOLD } from "../utils/constants";`);
add(`import Papa from "papaparse";`);
add(``);
add(`// ─── YOUR EXACT PIPELINE STAGES (from screenshots) ──────────────────────────`);
add(`const PIPELINE = {`);
add(`  "Hot Case":        { color: "#EF4444", bg: "rgba(239,68,68,0.12)",   icon: "🔥", desc: "High intent, urgent" },`);
add(`  "New Lead":        { color: "#3B82F6", bg: "rgba(59,130,246,0.12)",  icon: "✦",  desc: "Just came in" },`);
add(`  "Potential":       { color: "#10B981", bg: "rgba(16,185,129,0.12)",  icon: "⬆",  desc: "Qualified, interested" },`);
add(`  "No Answer":       { color: "#F59E0B", bg: "rgba(245,158,11,0.12)",  icon: "📵", desc: "Cannot reach" },`);
add(`  "Low Budget":      { color: "#8B5CF6", bg: "rgba(139,92,246,0.12)",  icon: "💰", desc: "Budget below threshold" },`);
add(`  "Non Potential":   { color: "#6B7280", bg: "rgba(107,114,128,0.12)", icon: "✗",  desc: "Not qualified" },`);
add(`  "WhatsApp":        { color: "#25D366", bg: "rgba(37,211,102,0.12)",  icon: "💬", desc: "WA communication only" },`);
add(`  "Resale/buy/Rent": { color: "#14B8A6", bg: "rgba(20,184,166,0.12)",  icon: "🏠", desc: "Secondary market" },`);
add(`  "EOI":             { color: "#D4A843", bg: "rgba(212,168,67,0.12)",  icon: "📋", desc: "Expression of Interest" },`);
add(`  "Closed Deal":     { color: "#10B981", bg: "rgba(16,185,129,0.15)",  icon: "✅", desc: "Deal closed successfully" },`);
add(`  "Closed Outside":  { color: "#EF4444", bg: "rgba(239,68,68,0.08)",   icon: "🚫", desc: "Closed with another agent" },`);
add(`};`);
add(``);
add(`// ─── SERVICE TYPES (your screenshots) ───────────────────────────────────────`);
add(`const SERVICE_TYPES = ["Buyer", "Seller", "Tenant"];`);
add(``);
add(`// ─── REQUEST TYPES ───────────────────────────────────────────────────────────`);
add(`const REQUEST_TYPES = ["Off-Plan", "Ready", "Resale", "Rental", "Investment", "Commercial"];`);
add(``);
add(`// ─── LEAD SOURCES ────────────────────────────────────────────────────────────`);
add(`const LEAD_SOURCES = [`);
add(`  "Property Finder", "Bayut", "Dubizzle", "Meta/Facebook", "Instagram",`);
add(`  "WhatsApp", "Google Ads", "Referral", "Website", "Cold Call",`);
add(`  "TikTok", "LinkedIn", "Email Campaign", "Walk In", "Manual",`);
add(`];`);
add(``);
add(`// ─── CHANNELS ────────────────────────────────────────────────────────────────`);
add(`const CHANNELS = ["WhatsApp", "Phone Call", "Email", "In Person", "Video Call", "Social DM"];`);
add(``);
add(`// ─── SOURCE COLORS ───────────────────────────────────────────────────────────`);
add(`const SRC_COLOR = {`);
add(`  "Property Finder":"#00C08B","Bayut":"#FF6B35","Dubizzle":"#E8003D",`);
add(`  "Meta/Facebook":"#1877F2","Instagram":"#E1306C","WhatsApp":"#25D366",`);
add(`  "Google Ads":"#4285F4","Referral":"#8B5CF6","Website":"#14B8A6",`);
add(`  "Cold Call":"#F59E0B","TikTok":"#010101","LinkedIn":"#0A66C2",`);
add(`  "Email Campaign":"#EA4335","Walk In":"#6B7280","Manual":"#94A3B8",`);
add(`};`);
add(``);
add(`// ─── NATIONALITIES ───────────────────────────────────────────────────────────`);
add(`const NATS = ["Indian","British","Russian","Chinese","French","Pakistani","Saudi","Egyptian","German","American","Italian","Canadian","Australian","Japanese","Korean","Emirati","Filipino","Lebanese","Jordanian","Other"];`);
add(``);
add(`// ─── BEST CONTACT TIMES ──────────────────────────────────────────────────────`);
add(`const BEST_TIME = {`);
add(`  "Property Finder":"Evening 6-9pm","Bayut":"Evening 6-9pm","Dubizzle":"Evening 6-8pm",`);
add(`  "WhatsApp":"Morning 9-11am","Instagram":"Afternoon 2-5pm","Meta/Facebook":"Afternoon 2-5pm",`);
add(`  "Google Ads":"Morning 10am-12pm","Referral":"Anytime","Cold Call":"Morning 9-11am",`);
add(`  "Website":"Morning 9-11am","TikTok":"Evening 7-10pm","Manual":"Anytime",`);
add(`};`);
add(``);
add(`// ─── WA TEMPLATES ────────────────────────────────────────────────────────────`);
add(`const WA_TEMPLATES = [`);
add(`  { label:"First Contact",   body:"Hello {name}, I came across your property enquiry. I am {agent} from The Address Holding. We have excellent options matching your requirements in Dubai. When would be a good time to connect?" },`);
add(`  { label:"Follow Up",       body:"Hello {name}, following up on your property search. We have new listings matching your budget. Would you like to schedule a viewing this week?" },`);
add(`  { label:"Viewing Confirm", body:"Hello {name}, confirming your property viewing. Please let me know if you need to reschedule. Looking forward to meeting you!" },`);
add(`  { label:"Offer Made",      body:"Hello {name}, great news! We have submitted your offer. Our team is working to get you the best deal. I will update you as soon as we hear back." },`);
add(`  { label:"Golden Visa",     body:"Hello {name}, based on your budget you qualify for UAE Golden Visa (AED 2M+ investment). This gives you 10-year residency. Shall I send you more details?" },`);
add(`  { label:"Market Update",   body:"Hello {name}, quick update — prices in your preferred area have moved. I have great options within your budget. Shall we connect?" },`);
add(`  { label:"EOI Follow Up",   body:"Hello {name}, thank you for your Expression of Interest. Our team has reviewed your request and we have some excellent options to present. When can we connect?" },`);
add(`];`);
add(``);
add(`// ─── HELPERS ─────────────────────────────────────────────────────────────────`);
add(`const clnPhone = (p) => String(p||"").replace(/[^0-9]/g,"");`);
add(`const fmtBudget = (b) => { const n=parseFloat(b||0); if(!n) return "—"; if(n>=1000000) return "AED "+(n/1000000).toFixed(1)+"M"; return "AED "+n.toLocaleString(); };`);
add(`const fmtDate = (d) => { if(!d) return "—"; return new Date(d).toLocaleDateString("en-AE",{day:"2-digit",month:"short",year:"2-digit"}); };`);
add(`const daysAgo = (d) => !d ? 999 : Math.floor((Date.now()-new Date(d).getTime())/86400000);`);
add(`const escCSV = (v) => '"'+String(v==null?"":v).replace(/"/g,'""')+'"';`);
add(``);
add(`// ─── AI SCORE ENGINE ─────────────────────────────────────────────────────────`);
add(`function aiScore(l) {`);
add(`  let s = 0;`);
add(`  if (l.phone && l.email) s += 25; else if (l.phone || l.email) s += 10;`);
add(`  const b = parseFloat(l.budget||0);`);
add(`  if (b >= 5000000) s += 20; else if (b >= (GOLDEN_VISA_THRESHOLD||2000000)) s += 15; else if (b >= 1000000) s += 10;`);
add(`  const d = daysAgo(l.createdAt);`);
add(`  if (d < 1) s += 20; else if (d < 3) s += 15; else if (d < 7) s += 10;`);
add(`  const notes = (l.notes_log||[]).length;`);
add(`  if (notes >= 3) s += 10; else if (notes >= 1) s += 5;`);
add(`  if (l.timeline === "Immediate") s += 15; else if (l.timeline === "1-3 months") s += 10;`);
add(`  if (l.nationality) s += 5;`);
add(`  const score = Math.min(100, s);`);
add(`  return { score, color: score>=70?"#10B981":score>=40?"#D4A843":"#EF4444", label: score>=70?"Hot":score>=40?"Warm":"Cold" };`);
add(`}`);
add(``);
add(`// ─── SMALL UI ATOMS ──────────────────────────────────────────────────────────`);
add(`const PipelineBadge = ({ status }) => {`);
add(`  const cfg = PIPELINE[status] || PIPELINE["New Lead"];`);
add(`  return <span style={{fontSize:10,padding:"2px 8px",borderRadius:10,background:cfg.bg,color:cfg.color,fontWeight:700,whiteSpace:"nowrap"}}>{cfg.icon} {status}</span>;`);
add(`};`);
add(``);
add(`const ScoreBar = ({ lead }) => {`);
add(`  const sc = aiScore(lead);`);
add(`  return (`);
add(`    <div style={{display:"flex",alignItems:"center",gap:5}}>`);
add(`      <div style={{width:32,height:3,borderRadius:2,background:"rgba(255,255,255,0.1)",overflow:"hidden"}}>`);
add(`        <div style={{height:"100%",width:sc.score+"%",borderRadius:2,background:sc.color}} />`);
add(`      </div>`);
add(`      <span style={{fontSize:9,color:sc.color,fontWeight:700}}>{sc.label}</span>`);
add(`    </div>`);
add(`  );`);
add(`};`);
add(``);
add(`const SrcBadge = ({ src }) => {`);
add(`  const c = SRC_COLOR[src] || "#94A3B8";`);
add(`  return (`);
add(`    <div style={{display:"flex",alignItems:"center",gap:4}}>`);
add(`      <div style={{width:6,height:6,borderRadius:"50%",background:c,flexShrink:0}} />`);
add(`      <span style={{fontSize:10,color:T.textMuted}}>{src||"Manual"}</span>`);
add(`    </div>`);
add(`  );`);
add(`};`);
add(``);
add(`// ─── FIELD COMPONENT ─────────────────────────────────────────────────────────`);
add(`const Field = ({ label, children }) => (`);
add(`  <div>`);
add(`    <div style={{fontSize:10,color:T.textMuted,marginBottom:3,fontWeight:600,textTransform:"uppercase",letterSpacing:0.5}}>{label}</div>`);
add(`    {children}`);
add(`  </div>`);
add(`);`);
add(``);
add(`const FInput = ({ value, onChange, placeholder, type="text", style={} }) => (`);
add(`  <input type={type} value={value||""} onChange={e=>onChange(e.target.value)} placeholder={placeholder}`);
add(`    style={{width:"100%",padding:"8px 10px",background:"rgba(255,255,255,0.04)",border:"1px solid "+T.border,borderRadius:7,color:T.white,fontSize:12,outline:"none",boxSizing:"border-box",...style}} />`);
add(`);`);
add(``);
add(`const FSelect = ({ value, onChange, children, style={} }) => (`);
add(`  <select value={value||""} onChange={e=>onChange(e.target.value)}`);
add(`    style={{width:"100%",padding:"8px 10px",background:"rgba(255,255,255,0.04)",border:"1px solid "+T.border,borderRadius:7,color:T.white,fontSize:12,outline:"none",...style}}>`);
add(`    {children}`);
add(`  </select>`);
add(`);`);
add(``);
add(`// ═══════════════════════════════════════════════════════════════════════════════`);
add(`// MAIN COMPONENT`);
add(`// ═══════════════════════════════════════════════════════════════════════════════`);
add(`function MyLeadsTab({`);
add(`  myLeads, liveLeads, orgRole, userRole, orgId, orgName, listings,`);
add(`  leadSearch, setLeadSearch,`);
add(`  leadStatusFilter, setLeadStatusFilter,`);
add(`  leadTypeFilter, setLeadTypeFilter,`);
add(`  leadSourceFilter, setLeadSourceFilter,`);
add(`  leadBudgetFilter, setLeadBudgetFilter,`);
add(`  leadNatFilter, setLeadNatFilter,`);
add(`  leadTagFilter, setLeadTagFilter,`);
add(`  leadSortBy, setLeadSortBy,`);
add(`  selectedLead, setSelectedLead,`);
add(`  leadDrawerTab, setLeadDrawerTab,`);
add(`  leadShowAdd, setLeadShowAdd,`);
add(`  leadAddName, setLeadAddName,`);
add(`  leadAddPhone, setLeadAddPhone,`);
add(`  leadAddEmail, setLeadAddEmail,`);
add(`  leadAddNat, setLeadAddNat,`);
add(`  leadAddLang, setLeadAddLang,`);
add(`  leadAddSource, setLeadAddSource,`);
add(`  leadAddType, setLeadAddType,`);
add(`  leadAddBudget, setLeadAddBudget,`);
add(`  leadAddBedrooms, setLeadAddBedrooms,`);
add(`  leadAddPurpose, setLeadAddPurpose,`);
add(`  leadAddTimeline, setLeadAddTimeline,`);
add(`  leadAddStatus, setLeadAddStatus,`);
add(`  leadAddComm, setLeadAddComm,`);
add(`  leadAddRef, setLeadAddRef,`);
add(`  leadAddFollowUp, setLeadAddFollowUp,`);
add(`  leadAddSaving, setLeadAddSaving,`);
add(`  noteText, setNoteText,`);
add(`  noteType, setNoteType,`);
add(`  noteLoading, setNoteLoading,`);
add(`  taskText, setTaskText,`);
add(`  taskDue, setTaskDue,`);
add(`  showMLAnalytics, setShowMLAnalytics,`);
add(`  showMLTemplates, setShowMLTemplates,`);
add(`  showQuickCapture, setShowQuickCapture,`);
add(`  handleTabChange,`);
add(`}) {`);
add(``);
add(`  // ── Local state ──`);
add(`  const [view, setView] = useState("table");              // table | kanban | analytics`);
add(`  const [activeTab, setActiveTab] = useState("all");      // all | pipeline stage key`);
add(`  const [serviceType, setServiceType] = useState("all"); // all | Buyer | Seller | Tenant`);
add(`  const [drawerTab, setDrawerTab] = useState("profile");  // profile | activity | match`);
add(`  const [showAddForm, setShowAddForm] = useState(false);`);
add(`  const [showWATemplates, setShowWATemplates] = useState(false);`);
add(``);
add(`  // ── Add form local state ──`);
add(`  const [form, setForm] = useState({`);
add(`    name:"", phone:"", email:"", budget:"",`);
add(`    requestType:"", serviceType:"Buyer", project:"",`);
add(`    source:"Manual", channel:"WhatsApp", campaign:"",`);
add(`    status:"New Lead", nationality:"", language:"",`);
add(`    timeline:"", community:"", bedrooms:"", comment:"",`);
add(`    agentName:"", agentId:"",`);
add(`  });`);
add(`  const [saving, setSaving] = useState(false);`);
add(``);
add(`  // ── Role ──`);
add(`  const isAgent      = orgRole === "agent";`);
add(`  const isManager    = orgRole === "manager";`);
add(`  const isSuperAdmin = userRole === "admin" || userRole === "superAdmin";`);
add(`  const canSee       = isAgent || isManager || isSuperAdmin;`);
add(`  const allLeads     = isSuperAdmin ? (liveLeads||[]) : (myLeads||[]);`);
add(``);
add(`  // ── Duplicate check ──`);
add(`  const isDuplicate = useCallback((phone) => {`);
add(`    if (!phone) return false;`);
add(`    const c = clnPhone(phone);`);
add(`    return allLeads.some(l => clnPhone(l.phone) === c);`);
add(`  }, [allLeads]);`);
add(``);
add(`  // ── Filter + sort ──`);
add(`  const filtered = useMemo(() => {`);
add(`    let a = allLeads;`);
add(`    // Status tab`);
add(`    if (activeTab !== "all") {`);
add(`      if (activeTab === "_stale")  a = a.filter(l => !["Closed Deal","Closed Outside","Non Potential"].includes(l.status) && daysAgo(l.updatedAt||l.createdAt)>7);`);
add(`      else if (activeTab === "_hot") a = a.filter(l => aiScore(l).score>=70 && !["Closed Deal","Closed Outside"].includes(l.status));`);
add(`      else if (activeTab === "_gv") a = a.filter(l => parseFloat(l.budget||0)>=(GOLDEN_VISA_THRESHOLD||2000000));`);
add(`      else a = a.filter(l => (l.status||"New Lead") === activeTab);`);
add(`    }`);
add(`    // Service type`);
add(`    if (serviceType !== "all") a = a.filter(l => (l.serviceType||l.type||"") === serviceType);`);
add(`    // Source`);
add(`    if (leadSourceFilter && leadSourceFilter !== "all") a = a.filter(l => l.source === leadSourceFilter);`);
add(`    // Nationality`);
add(`    if (leadNatFilter && leadNatFilter !== "all") a = a.filter(l => l.nationality === leadNatFilter);`);
add(`    // Budget`);
add(`    if (leadBudgetFilter && leadBudgetFilter !== "all") {`);
add(`      const b = l => parseFloat(l.budget||0);`);
add(`      if (leadBudgetFilter==="under1m")   a = a.filter(l => b(l)<1000000);`);
add(`      if (leadBudgetFilter==="1to3m")     a = a.filter(l => b(l)>=1000000&&b(l)<3000000);`);
add(`      if (leadBudgetFilter==="3to5m")     a = a.filter(l => b(l)>=3000000&&b(l)<5000000);`);
add(`      if (leadBudgetFilter==="5to10m")    a = a.filter(l => b(l)>=5000000&&b(l)<10000000);`);
add(`      if (leadBudgetFilter==="above10m")  a = a.filter(l => b(l)>=10000000);`);
add(`    }`);
add(`    // Search`);
add(`    if (leadSearch && leadSearch.trim()) {`);
add(`      const q = leadSearch.trim().toLowerCase();`);
add(`      a = a.filter(l =>`);
add(`        (l.name||"").toLowerCase().includes(q) ||`);
add(`        (l.phone||"").includes(q) ||`);
add(`        (l.email||"").toLowerCase().includes(q) ||`);
add(`        (l.community||"").toLowerCase().includes(q)`);
add(`      );`);
add(`    }`);
add(`    // Sort`);
add(`    const sorted = [...a];`);
add(`    if (leadSortBy === "score")  sorted.sort((a,b) => aiScore(b).score - aiScore(a).score);`);
add(`    else if (leadSortBy === "budget") sorted.sort((a,b) => parseFloat(b.budget||0) - parseFloat(a.budget||0));`);
add(`    else if (leadSortBy === "name")   sorted.sort((a,b) => (a.name||"").localeCompare(b.name||""));`);
add(`    else sorted.sort((a,b) => new Date(b.createdAt||0) - new Date(a.createdAt||0));`);
add(`    return sorted;`);
add(`  }, [allLeads, activeTab, serviceType, leadSourceFilter, leadNatFilter, leadBudgetFilter, leadSearch, leadSortBy]);`);
add(``);
add(`  // ── KPI counters ──`);
add(`  const totalVal  = allLeads.reduce((s,l) => s+parseFloat(l.budget||0), 0);`);
add(`  const newToday  = allLeads.filter(l => daysAgo(l.createdAt) < 1).length;`);
add(`  const hotLeads  = allLeads.filter(l => aiScore(l).score>=70 && !["Closed Deal","Closed Outside"].includes(l.status));`);
add(`  const closed    = allLeads.filter(l => l.status === "Closed Deal");`);
add(`  const staleList = allLeads.filter(l => !["Closed Deal","Closed Outside","Non Potential"].includes(l.status) && daysAgo(l.updatedAt||l.createdAt)>7);`);
add(`  const gvLeads   = allLeads.filter(l => parseFloat(l.budget||0)>=(GOLDEN_VISA_THRESHOLD||2000000));`);
add(``);
add(`  // ── Tab counts ──`);
add(`  const tabCount = useMemo(() => {`);
add(`    const c = { all: allLeads.length, _hot: hotLeads.length, _stale: staleList.length, _gv: gvLeads.length };`);
add(`    Object.keys(PIPELINE).forEach(s => { c[s] = allLeads.filter(l => (l.status||"New Lead")===s).length; });`);
add(`    return c;`);
add(`  }, [allLeads, hotLeads, staleList, gvLeads]);`);
add(``);
add(`  // ── Source analytics ──`);
add(`  const srcStats = useMemo(() => {`);
add(`    const stats = {};`);
add(`    allLeads.forEach(l => {`);
add(`      const src = l.source || "Manual";`);
add(`      if (!stats[src]) stats[src] = { total:0, closed:0 };`);
add(`      stats[src].total++;`);
add(`      if (l.status === "Closed Deal") stats[src].closed++;`);
add(`    });`);
add(`    return Object.entries(stats).map(([src,d]) => ({`);
add(`      src, total: d.total, closed: d.closed,`);
add(`      rate: d.total > 0 ? Math.round(d.closed*100/d.total) : 0,`);
add(`    })).sort((a,b) => b.total - a.total);`);
add(`  }, [allLeads]);`);
add(``);
add(`  // ── Property matching ──`);
add(`  const matchedListings = useMemo(() => {`);
add(`    if (!selectedLead || !listings) return [];`);
add(`    const b = parseFloat(selectedLead.budget||0);`);
add(`    return (listings||[]).filter(l => {`);
add(`      const lp = parseFloat(l.price||l.priceMin||0);`);
add(`      const budgetOk = b > 0 ? lp<=b*1.25 && lp>=b*0.6 : true;`);
add(`      const commOk = selectedLead.community`);
add(`        ? (l.community||"").toLowerCase().includes((selectedLead.community||"").toLowerCase())`);
add(`        : true;`);
add(`      return budgetOk && commOk;`);
add(`    }).slice(0, 4);`);
add(`  }, [selectedLead, listings]);`);
add(``);
add(`  // ── Export CSV ──`);
add(`  const exportCSV = useCallback(() => {`);
add(`    const h = ["ID","Name","Phone","Email","Budget","Service Type","Request Type","Status","Source","Channel","Nationality","Language","Timeline","Community","Bedrooms","Project","Campaign","Score","Created","Updated"];`);
add(`    const rows = filtered.map(l => [`);
add(`      l.id||"", l.name||"", l.phone||"", l.email||"", l.budget||"",`);
add(`      l.serviceType||l.type||"", l.requestType||"", l.status||"New Lead",`);
add(`      l.source||"", l.channel||"", l.nationality||"", l.language||"",`);
add(`      l.timeline||"", l.community||"", l.bedrooms||"", l.project||"", l.campaign||"",`);
add(`      aiScore(l).score,`);
add(`      l.createdAt ? new Date(l.createdAt).toLocaleDateString("en-GB") : "",`);
add(`      l.updatedAt ? new Date(l.updatedAt).toLocaleDateString("en-GB") : "",`);
add(`    ].map(v => escCSV(v)));`);
add(`    const csv = [h.join(","), ...rows.map(r => r.join(","))].join("\\n");`);
add(`    const blob = new Blob([csv], {type:"text/csv"});`);
add(`    const url = URL.createObjectURL(blob);`);
add(`    const a = document.createElement("a");`);
add(`    a.href=url; a.download="DXB_Leads_"+new Date().toISOString().slice(0,10)+".csv";`);
add(`    a.click(); URL.revokeObjectURL(url);`);
add(`  }, [filtered]);`);
add(``);
add(`  // ── WA Template send ──`);
add(`  const sendWA = useCallback((tmpl, lead) => {`);
add(`    if (!lead?.phone) return;`);
add(`    const agent = "Agent";`);
add(`    const msg = tmpl.body`);
add(`      .replace("{name}", lead.name||"")`);
add(`      .replace("{agent}", agent)`);
add(`      .replace("{budget}", fmtBudget(lead.budget))`);
add(`      .replace("{community}", lead.community||"Dubai")`);
add(`      .replace("{type}", lead.serviceType||lead.type||"property")`);
add(`      .replace("{date}", "the scheduled time");`);
add(`    window.open("https://wa.me/"+clnPhone(lead.phone)+"?text="+encodeURIComponent(msg), "_blank");`);
add(`    setShowWATemplates(false);`);
add(`  }, []);`);
add(``);
add(`  if (!canSee) return (`);
add(`    <div style={{display:"flex",flexDirection:"column",alignItems:"center",justifyContent:"center",height:400,gap:12}}>`);
add(`      <div style={{fontSize:32}}>🔒</div>`);
add(`      <div style={{fontSize:16,fontWeight:700,color:T.white}}>CRM Access Required</div>`);
add(`      <div style={{fontSize:12,color:T.textMuted}}>Contact your agency manager to enable CRM access</div>`);
add(`    </div>`);
add(`  );`);
add(``);
add(`  return (`);
add(`    <div style={{paddingBottom:80}}>`);
add(``);
add(`      {/* ════════════════════════════════════════════════════════════`);
add(`           HEADER`);
add(`         ════════════════════════════════════════════════════════════ */}`);
add(`      <div style={{display:"flex",alignItems:"flex-start",justifyContent:"space-between",marginBottom:18,flexWrap:"wrap",gap:12,paddingBottom:16,borderBottom:"1px solid "+T.border}}>`);
add(`        <div>`);
add(`          <div style={{fontFamily:"'Fraunces',serif",fontSize:22,fontWeight:900,color:T.white}}>`);
add(`            {orgName || "My Agency"} — {isManager ? "Team Leads" : "My Leads"}`);
add(`          </div>`);
add(`          <div style={{fontSize:12,color:T.textMuted,marginTop:3}}>`);
add(`            {filtered.length} of {allLeads.length} leads · AI scored · Real-time`);
add(`          </div>`);
add(`        </div>`);
add(`        <div style={{display:"flex",gap:8,flexWrap:"wrap",alignItems:"center"}}>`);
add(`          {/* View toggle */}`);
add(`          <div style={{display:"flex",gap:2,background:"rgba(255,255,255,0.04)",border:"1px solid "+T.border,borderRadius:8,padding:3}}>`);
add(`            {[{k:"table",l:"List"},{k:"kanban",l:"Pipeline"},{k:"analytics",l:"Analytics"}].map(v => (`);
add(`              <button key={v.k} type="button" onClick={()=>setView(v.k)}`);
add(`                style={{padding:"5px 13px",borderRadius:6,fontSize:11,fontWeight:600,cursor:"pointer",fontFamily:"'Outfit',sans-serif",background:view===v.k?"rgba(212,168,67,0.15)":"transparent",border:view===v.k?"1px solid "+T.gold:"1px solid transparent",color:view===v.k?T.gold:T.textMuted,transition:"all 0.15s"}}>`);
add(`                {v.l}`);
add(`              </button>`);
add(`            ))}`);
add(`          </div>`);
add(`          <button type="button" onClick={exportCSV} style={{padding:"7px 12px",borderRadius:7,border:"1px solid "+T.border,background:"transparent",color:T.textSecondary,fontSize:12,fontWeight:600,cursor:"pointer",fontFamily:"'Outfit',sans-serif"}}>Export CSV</button>`);
add(`          <button type="button" onClick={()=>setShowWATemplates(true)} style={{padding:"7px 12px",borderRadius:7,border:"1px solid rgba(37,211,102,0.3)",background:"rgba(37,211,102,0.08)",color:"#25D366",fontSize:12,fontWeight:600,cursor:"pointer",fontFamily:"'Outfit',sans-serif"}}>Bulk WA</button>`);
add(`          <button type="button" onClick={()=>setShowAddForm(true)} style={{padding:"8px 18px",borderRadius:8,border:"none",background:"linear-gradient(135deg,#D4A843,#B8902E)",color:"#0A0E1A",fontSize:13,fontWeight:700,cursor:"pointer",fontFamily:"'Outfit',sans-serif"}}>+ Add Lead</button>`);
add(`        </div>`);
add(`      </div>`);
add(``);
add(`      {/* ════════════════════════════════════════════════════════════`);
add(`           KPI CARDS`);
add(`         ════════════════════════════════════════════════════════════ */}`);
add(`      <div style={{display:"grid",gridTemplateColumns:"repeat(6,1fr)",gap:8,marginBottom:16}}>`);
add(`        {[`);
add(`          {label:"Total Leads",   value:allLeads.length,                                    color:T.gold,     tab:"all"},`);
add(`          {label:"New Today",     value:newToday,                                           color:"#0EA5E9",  tab:null},`);
add(`          {label:"Hot Leads",     value:hotLeads.length,                                    color:"#10B981",  tab:"_hot"},`);
add(`          {label:"Closed Deals",  value:closed.length,                                      color:"#10B981",  tab:"Closed Deal"},`);
add(`          {label:"Stale 7d+",     value:staleList.length,                                   color:"#EF4444",  tab:"_stale"},`);
add(`          {label:"Pipeline Value",value:totalVal>=1e9?"AED "+(totalVal/1e9).toFixed(1)+"B":"AED "+(totalVal/1e6).toFixed(1)+"M", color:T.gold, tab:null},`);
add(`        ].map((k,i) => (`);
add(`          <div key={i} onClick={()=>k.tab&&setActiveTab(k.tab)}`);
add(`            style={{background:"rgba(255,255,255,0.03)",border:"1px solid "+(activeTab===k.tab&&k.tab?k.color+"50":T.border),borderRadius:10,padding:"10px 12px",cursor:k.tab?"pointer":"default",position:"relative",overflow:"hidden",transition:"border-color 0.2s"}}>`);
add(`            <div style={{position:"absolute",top:0,left:0,right:0,height:2,background:k.color,opacity:0.8}} />`);
add(`            <div style={{fontSize:9,fontWeight:700,color:T.textMuted,textTransform:"uppercase",letterSpacing:0.7,marginBottom:4}}>{k.label}</div>`);
add(`            <div style={{fontSize:20,fontWeight:900,color:k.color,fontFamily:"'Fraunces',serif"}}>{k.value}</div>`);
add(`          </div>`);
add(`        ))}`);
add(`      </div>`);
add(``);
add(`      {/* ════════════════════════════════════════════════════════════`);
add(`           STALE ALERT BANNER`);
add(`         ════════════════════════════════════════════════════════════ */}`);
add(`      {staleList.length > 0 && (`);
add(`        <div style={{padding:"8px 14px",background:"rgba(239,68,68,0.06)",border:"1px solid rgba(239,68,68,0.2)",borderRadius:8,marginBottom:14,display:"flex",alignItems:"center",gap:10}}>`);
add(`          <div style={{width:6,height:6,borderRadius:"50%",background:"#EF4444",flexShrink:0}} />`);
add(`          <div style={{flex:1,fontSize:11,color:"#EF4444"}}>`);
add(`            {staleList.slice(0,2).map(l=>l.name||"Unnamed").filter(Boolean).join(", ")}`);
add(`            {staleList.length > 2 ? " + "+(staleList.length-2)+" more" : ""} — no contact for 7+ days`);
add(`          </div>`);
add(`          <button type="button" onClick={()=>setActiveTab("_stale")} style={{padding:"3px 10px",background:"transparent",border:"1px solid rgba(239,68,68,0.3)",borderRadius:5,color:"#EF4444",fontSize:10,cursor:"pointer",fontFamily:"'Outfit',sans-serif"}}>View Stale</button>`);
add(`        </div>`);
add(`      )}`);
add(``);
add(`      {/* ════════════════════════════════════════════════════════════`);
add(`           PIPELINE STATUS TABS — YOUR EXACT STAGES`);
add(`         ════════════════════════════════════════════════════════════ */}`);
add(`      <div style={{overflowX:"auto",marginBottom:14}}>`);
add(`        <div style={{display:"flex",gap:0,borderBottom:"1px solid "+T.border,minWidth:"max-content"}}>`);
add(`          {[`);
add(`            {k:"all",       l:"All",          count:tabCount.all,          color:T.textSecondary},`);
add(`            {k:"_hot",      l:"🔥 Hot",        count:tabCount._hot,         color:"#EF4444"},`);
add(`            ...Object.entries(PIPELINE).map(([k,v]) => ({k, l:v.icon+" "+k, count:tabCount[k]||0, color:v.color})),`);
add(`            {k:"_stale",    l:"⏰ Stale",      count:tabCount._stale,       color:"#F59E0B"},`);
add(`            {k:"_gv",       l:"🏆 Golden Visa",count:tabCount._gv,          color:T.gold},`);
add(`          ].map(tab => {`);
add(`            const active = activeTab === tab.k;`);
add(`            return (`);
add(`              <button key={tab.k} type="button" onClick={()=>setActiveTab(tab.k)}`);
add(`                style={{padding:"8px 14px",border:"none",borderBottom:active?"2px solid "+tab.color:"2px solid transparent",background:"transparent",color:active?tab.color:T.textMuted,fontSize:11,fontWeight:active?700:500,cursor:"pointer",whiteSpace:"nowrap",fontFamily:"'Outfit',sans-serif",display:"flex",alignItems:"center",gap:5,transition:"color 0.15s"}}>`);
add(`                {tab.l}`);
add(`                {tab.count > 0 && <span style={{fontSize:10,padding:"1px 5px",borderRadius:8,background:active?tab.color+"20":"rgba(255,255,255,0.06)",color:active?tab.color:T.textMuted}}>{tab.count}</span>}`);
add(`              </button>`);
add(`            );`);
add(`          })}`);
add(`        </div>`);
add(`      </div>`);
add(``);
add(`      {/* ════════════════════════════════════════════════════════════`);
add(`           FILTER BAR`);
add(`         ════════════════════════════════════════════════════════════ */}`);
add(`      <div style={{background:"rgba(255,255,255,0.02)",border:"1px solid "+T.border,borderRadius:10,padding:"10px 14px",marginBottom:14}}>`);
add(`        <div style={{display:"flex",gap:8,flexWrap:"wrap",alignItems:"center"}}>`);
add(`          {/* Search */}`);
add(`          <div style={{position:"relative",flex:"2 1 200px"}}>`);
add(`            <svg style={{position:"absolute",left:9,top:"50%",transform:"translateY(-50%)"}} width="12" height="12" viewBox="0 0 24 24" fill="none" stroke={T.textMuted} strokeWidth="2"><circle cx="11" cy="11" r="8"/><line x1="21" y1="21" x2="16.65" y2="16.65"/></svg>`);
add(`            <input value={leadSearch||""} onChange={e=>setLeadSearch(e.target.value)} placeholder="Search name, phone, email, community..."`);
add(`              style={{width:"100%",padding:"7px 8px 7px 28px",background:"rgba(255,255,255,0.04)",border:"1px solid "+(leadSearch?T.gold:T.border),borderRadius:7,color:T.white,fontSize:12,outline:"none",boxSizing:"border-box"}} />`);
add(`          </div>`);
add(`          {/* Service type pills */}`);
add(`          <div style={{display:"flex",gap:4}}>`);
add(`            {["all",...SERVICE_TYPES].map(t => (`);
add(`              <button key={t} type="button" onClick={()=>setServiceType(t)}`);
add(`                style={{padding:"5px 11px",borderRadius:16,fontSize:11,fontWeight:serviceType===t?700:500,cursor:"pointer",fontFamily:"'Outfit',sans-serif",background:serviceType===t?"rgba(212,168,67,0.15)":"rgba(255,255,255,0.03)",border:"1px solid "+(serviceType===t?T.gold:T.border),color:serviceType===t?T.gold:T.textMuted,transition:"all 0.15s"}}>`);
add(`                {t==="all"?"All Types":t}`);
add(`              </button>`);
add(`            ))}`);
add(`          </div>`);
add(`          {/* Source */}`);
add(`          <select value={leadSourceFilter||"all"} onChange={e=>setLeadSourceFilter(e.target.value)}`);
add(`            style={{flex:"1 1 130px",padding:"7px 8px",background:"rgba(255,255,255,0.04)",border:"1px solid "+T.border,borderRadius:7,color:T.textSecondary,fontSize:12,outline:"none"}}>`);
add(`            <option value="all">All Sources</option>`);
add(`            {LEAD_SOURCES.map(s => <option key={s} value={s}>{s}</option>)}`);
add(`          </select>`);
add(`          {/* Budget */}`);
add(`          <select value={leadBudgetFilter||"all"} onChange={e=>setLeadBudgetFilter(e.target.value)}`);
add(`            style={{flex:"1 1 120px",padding:"7px 8px",background:"rgba(255,255,255,0.04)",border:"1px solid "+T.border,borderRadius:7,color:T.textSecondary,fontSize:12,outline:"none"}}>`);
add(`            <option value="all">All Budgets</option>`);
add(`            <option value="under1m">Under AED 1M</option>`);
add(`            <option value="1to3m">AED 1-3M</option>`);
add(`            <option value="3to5m">AED 3-5M</option>`);
add(`            <option value="5to10m">AED 5-10M</option>`);
add(`            <option value="above10m">Above AED 10M</option>`);
add(`          </select>`);
add(`          {/* Nationality */}`);
add(`          <select value={leadNatFilter||"all"} onChange={e=>setLeadNatFilter(e.target.value)}`);
add(`            style={{flex:"1 1 130px",padding:"7px 8px",background:"rgba(255,255,255,0.04)",border:"1px solid "+T.border,borderRadius:7,color:T.textSecondary,fontSize:12,outline:"none"}}>`);
add(`            <option value="all">All Nationalities</option>`);
add(`            {NATS.map(n => <option key={n} value={n}>{n}</option>)}`);
add(`          </select>`);
add(`          {/* Sort */}`);
add(`          <select value={leadSortBy||"date"} onChange={e=>setLeadSortBy(e.target.value)}`);
add(`            style={{flex:"1 1 100px",padding:"7px 8px",background:"rgba(255,255,255,0.04)",border:"1px solid "+T.border,borderRadius:7,color:T.textSecondary,fontSize:12,outline:"none"}}>`);
add(`            <option value="date">Latest First</option>`);
add(`            <option value="score">AI Score</option>`);
add(`            <option value="budget">Budget High-Low</option>`);
add(`            <option value="name">Name A-Z</option>`);
add(`          </select>`);
add(`        </div>`);
add(`      </div>`);
add(``);
add(`      {/* ════════════════════════════════════════════════════════════`);
add(`           MAIN CONTENT AREA`);
add(`         ════════════════════════════════════════════════════════════ */}`);
add(`      <div style={{display:"grid",gridTemplateColumns:selectedLead?"1fr 360px":"1fr",gap:14,alignItems:"start"}}>`);
add(``);
add(`        {/* ── LIST VIEW ── */}`);
add(`        {view==="table" && (`);
add(`          <div style={{background:"rgba(255,255,255,0.02)",border:"1px solid "+T.border,borderRadius:12,overflow:"hidden"}}>`);
add(`            {/* Table header */}`);
add(`            <div style={{display:"grid",gridTemplateColumns:"36px 2fr 1fr 1fr 1fr 1fr 1fr 60px",padding:"10px 14px",background:"rgba(255,255,255,0.03)",borderBottom:"1px solid "+T.border}}>`);
add(`              {["","Lead","Phone","Budget","Service","Source","Status",""].map((h,i) => (`);
add(`                <div key={i} style={{fontSize:9,fontWeight:700,color:T.textMuted,textTransform:"uppercase",letterSpacing:0.8}}>{h}</div>`);
add(`              ))}`);
add(`            </div>`);
add(`            {/* Empty state */}`);
add(`            {allLeads.length === 0 && (`);
add(`              <div style={{padding:"60px 20px",textAlign:"center"}}>`);
add(`                <div style={{fontSize:36,marginBottom:12}}>📋</div>`);
add(`                <div style={{fontSize:16,fontWeight:700,color:T.white,marginBottom:6}}>No leads yet</div>`);
add(`                <div style={{fontSize:12,color:T.textMuted,marginBottom:16}}>Add your first lead to get started with the CRM</div>`);
add(`                <button type="button" onClick={()=>setShowAddForm(true)}`);
add(`                  style={{padding:"10px 24px",borderRadius:8,border:"none",background:"linear-gradient(135deg,#D4A843,#B8902E)",color:"#0A0E1A",fontSize:13,fontWeight:700,cursor:"pointer"}}>+ Add First Lead</button>`);
add(`              </div>`);
add(`            )}`);
add(`            {filtered.length === 0 && allLeads.length > 0 && (`);
add(`              <div style={{padding:"40px 20px",textAlign:"center",color:T.textMuted,fontSize:13}}>No leads match your current filters.</div>`);
add(`            )}`);
add(`            {/* Rows */}`);
add(`            {filtered.map((lead, i) => {`);
add(`              const sc = aiScore(lead);`);
add(`              const stale = !["Closed Deal","Closed Outside","Non Potential"].includes(lead.status) && daysAgo(lead.updatedAt||lead.createdAt) > 7;`);
add(`              const isGV = parseFloat(lead.budget||0) >= (GOLDEN_VISA_THRESHOLD||2000000);`);
add(`              const isSel = selectedLead?.id === lead.id;`);
add(`              const pipeCfg = PIPELINE[lead.status] || PIPELINE["New Lead"];`);
add(`              return (`);
add(`                <div key={lead.id||i} onClick={()=>setSelectedLead(isSel?null:lead)}`);
add(`                  style={{display:"grid",gridTemplateColumns:"36px 2fr 1fr 1fr 1fr 1fr 1fr 60px",padding:"11px 14px",borderBottom:i<filtered.length-1?"1px solid "+T.border+"50":"none",cursor:"pointer",background:isSel?"rgba(212,168,67,0.05)":stale?"rgba(239,68,68,0.02)":"transparent",transition:"background 0.15s",borderLeft:isSel?"2px solid "+T.gold:"2px solid transparent"}}`);
add(`                  onMouseEnter={e=>!isSel&&(e.currentTarget.style.background="rgba(255,255,255,0.02)")}`);
add(`                  onMouseLeave={e=>!isSel&&(e.currentTarget.style.background=stale?"rgba(239,68,68,0.02)":"transparent")}`);
add(`                >`);
add(`                  {/* Score dot */}`);
add(`                  <div style={{display:"flex",alignItems:"center",paddingTop:2}}>`);
add(`                    <div style={{width:8,height:8,borderRadius:"50%",background:sc.color}} />`);
add(`                  </div>`);
add(`                  {/* Name */}`);
add(`                  <div>`);
add(`                    <div style={{fontSize:12,fontWeight:600,color:T.white,marginBottom:3}}>{lead.name||"Unnamed"}</div>`);
add(`                    <div style={{display:"flex",gap:4,flexWrap:"wrap"}}>`);
add(`                      <ScoreBar lead={lead} />`);
add(`                      {isGV && <span style={{fontSize:9,padding:"1px 5px",borderRadius:8,background:"rgba(212,168,67,0.2)",color:T.gold}}>🏆 GV</span>}`);
add(`                      {stale && <span style={{fontSize:9,padding:"1px 5px",borderRadius:8,background:"rgba(239,68,68,0.15)",color:"#EF4444"}}>⏰ Stale</span>}`);
add(`                    </div>`);
add(`                  </div>`);
add(`                  {/* Phone */}`);
add(`                  <div style={{fontSize:11,color:T.textSecondary,alignSelf:"center"}}>{lead.phone||"—"}</div>`);
add(`                  {/* Budget */}`);
add(`                  <div style={{fontSize:12,fontWeight:700,color:T.gold,alignSelf:"center"}}>{fmtBudget(lead.budget)}</div>`);
add(`                  {/* Service type */}`);
add(`                  <div style={{fontSize:11,color:T.textMuted,alignSelf:"center"}}>{lead.serviceType||lead.type||"—"}</div>`);
add(`                  {/* Source */}`);
add(`                  <div style={{alignSelf:"center"}}><SrcBadge src={lead.source} /></div>`);
add(`                  {/* Status */}`);
add(`                  <div style={{alignSelf:"center"}}><PipelineBadge status={lead.status||"New Lead"} /></div>`);
add(`                  {/* WA action */}`);
add(`                  <div style={{alignSelf:"center",display:"flex",gap:4}}>`);
add(`                    {lead.phone && (`);
add(`                      <a href={"https://wa.me/"+clnPhone(lead.phone)} target="_blank" rel="noopener noreferrer"`);
add(`                        onClick={e=>e.stopPropagation()}`);
add(`                        style={{padding:"4px 7px",borderRadius:5,background:"rgba(37,211,102,0.1)",border:"1px solid rgba(37,211,102,0.2)",textDecoration:"none",display:"flex",alignItems:"center"}}>`);
add(`                        <span style={{fontSize:10,color:"#25D366",fontWeight:700}}>WA</span>`);
add(`                      </a>`);
add(`                    )}`);
add(`                  </div>`);
add(`                </div>`);
add(`              );`);
add(`            })}`);
add(`          </div>`);
add(`        )}`);
add(``);
add(`        {/* ── KANBAN VIEW ── */}`);
add(`        {view==="kanban" && (`);
add(`          <div style={{overflowX:"auto",paddingBottom:8}}>`);
add(`            <div style={{display:"flex",gap:12,minWidth:"max-content",alignItems:"start"}}>`);
add(`              {Object.entries(PIPELINE).map(([status, cfg]) => {`);
add(`                const colLeads = allLeads.filter(l => (l.status||"New Lead") === status);`);
add(`                return (`);
add(`                  <div key={status} style={{width:220,flexShrink:0}}>`);
add(`                    {/* Column header */}`);
add(`                    <div style={{display:"flex",justifyContent:"space-between",alignItems:"center",marginBottom:8,padding:"8px 11px",background:cfg.bg,borderRadius:8,border:"1px solid "+cfg.color+"30"}}>`);
add(`                      <span style={{fontSize:11,fontWeight:700,color:cfg.color}}>{cfg.icon} {status}</span>`);
add(`                      <span style={{fontSize:10,color:cfg.color,background:"rgba(255,255,255,0.1)",padding:"1px 7px",borderRadius:10}}>{colLeads.length}</span>`);
add(`                    </div>`);
add(`                    {/* Cards */}`);
add(`                    <div style={{display:"flex",flexDirection:"column",gap:6,maxHeight:580,overflowY:"auto"}}>`);
add(`                      {colLeads.map((lead, i) => {`);
add(`                        const sc = aiScore(lead);`);
add(`                        const isGV = parseFloat(lead.budget||0) >= (GOLDEN_VISA_THRESHOLD||2000000);`);
add(`                        const stale = daysAgo(lead.updatedAt||lead.createdAt) > 7;`);
add(`                        return (`);
add(`                          <div key={lead.id||i} onClick={()=>setSelectedLead(selectedLead?.id===lead.id?null:lead)}`);
add(`                            style={{background:"rgba(255,255,255,0.03)",border:"1px solid "+(stale?"rgba(239,68,68,0.3)":T.border),borderRadius:9,padding:"10px 11px",cursor:"pointer",transition:"border-color 0.15s"}}`);
add(`                            onMouseEnter={e=>(e.currentTarget.style.borderColor=cfg.color+"60")}`);
add(`                            onMouseLeave={e=>(e.currentTarget.style.borderColor=stale?"rgba(239,68,68,0.3)":T.border)}`);
add(`                          >`);
add(`                            <div style={{display:"flex",justifyContent:"space-between",marginBottom:5}}>`);
add(`                              <div style={{fontSize:12,fontWeight:600,color:T.white,flex:1,overflow:"hidden",textOverflow:"ellipsis",whiteSpace:"nowrap"}}>{lead.name||"Unnamed"}</div>`);
add(`                              <div style={{width:7,height:7,borderRadius:"50%",background:sc.color,flexShrink:0,marginTop:3}} />`);
add(`                            </div>`);
add(`                            <div style={{fontSize:12,fontWeight:700,color:T.gold,marginBottom:5}}>{fmtBudget(lead.budget)}</div>`);
add(`                            <SrcBadge src={lead.source} />`);
add(`                            <div style={{display:"flex",gap:4,marginTop:5,flexWrap:"wrap"}}>`);
add(`                              {lead.serviceType&&<span style={{fontSize:9,padding:"1px 5px",borderRadius:6,background:"rgba(255,255,255,0.06)",color:T.textMuted}}>{lead.serviceType||lead.type}</span>}`);
add(`                              {isGV&&<span style={{fontSize:9,padding:"1px 5px",borderRadius:6,background:"rgba(212,168,67,0.2)",color:T.gold}}>GV</span>}`);
add(`                              {stale&&<span style={{fontSize:9,padding:"1px 5px",borderRadius:6,background:"rgba(239,68,68,0.15)",color:"#EF4444"}}>Stale</span>}`);
add(`                            </div>`);
add(`                            {lead.community&&<div style={{fontSize:9,color:T.textMuted,marginTop:4}}>{lead.community}</div>}`);
add(`                            <div style={{fontSize:9,color:T.textMuted,marginTop:4}}>{fmtDate(lead.createdAt)}</div>`);
add(`                          </div>`);
add(`                        );`);
add(`                      })}`);
add(`                      {colLeads.length===0 && (`);
add(`                        <div style={{padding:"16px 12px",textAlign:"center",color:T.textMuted,fontSize:11,borderRadius:8,border:"1px dashed "+T.border+"60"}}>No leads</div>`);
add(`                      )}`);
add(`                    </div>`);
add(`                  </div>`);
add(`                );`);
add(`              })}`);
add(`            </div>`);
add(`          </div>`);
add(`        )}`);
add(``);
add(`        {/* ── ANALYTICS VIEW ── */}`);
add(`        {view==="analytics" && (`);
add(`          <div style={{display:"flex",flexDirection:"column",gap:14}}>`);
add(`            {/* Source performance */}`);
add(`            <div style={{background:"rgba(255,255,255,0.02)",border:"1px solid "+T.border,borderRadius:12,padding:"18px"}}>`);
add(`              <div style={{fontSize:13,fontWeight:700,color:T.white,marginBottom:14}}>Source Performance — Closed Deal Rate</div>`);
add(`              {allLeads.length === 0 ? (`);
add(`                <div style={{padding:"30px",textAlign:"center",color:T.textMuted,fontSize:12}}>No leads data available yet</div>`);
add(`              ) : (`);
add(`                <div style={{display:"grid",gridTemplateColumns:"repeat(auto-fill,minmax(150px,1fr))",gap:10}}>`);
add(`                  {srcStats.slice(0,8).map((s,i) => (`);
add(`                    <div key={i} style={{background:"rgba(255,255,255,0.03)",borderRadius:9,padding:"12px"}}>`);
add(`                      <div style={{fontSize:11,fontWeight:700,color:SRC_COLOR[s.src]||T.textMuted,marginBottom:6,overflow:"hidden",textOverflow:"ellipsis",whiteSpace:"nowrap"}}>{s.src}</div>`);
add(`                      <div style={{display:"flex",justifyContent:"space-between",alignItems:"flex-end"}}>`);
add(`                        <div>`);
add(`                          <div style={{fontSize:20,fontWeight:900,color:T.white,fontFamily:"'Fraunces',serif",lineHeight:1}}>{s.total}</div>`);
add(`                          <div style={{fontSize:9,color:T.textMuted}}>leads</div>`);
add(`                        </div>`);
add(`                        <div style={{textAlign:"right"}}>`);
add(`                          <div style={{fontSize:16,fontWeight:900,color:s.rate>=10?"#10B981":s.rate>=5?"#D4A843":"#EF4444",fontFamily:"'Fraunces',serif",lineHeight:1}}>{s.rate}%</div>`);
add(`                          <div style={{fontSize:9,color:T.textMuted}}>closed</div>`);
add(`                        </div>`);
add(`                      </div>`);
add(`                      <div style={{height:2,background:T.border,borderRadius:1,marginTop:8}}>`);
add(`                        <div style={{height:"100%",width:Math.min(s.total,100)+"%",background:SRC_COLOR[s.src]||T.gold,borderRadius:1,opacity:0.7}} />`);
add(`                      </div>`);
add(`                    </div>`);
add(`                  ))}`);
add(`                </div>`);
add(`              )}`);
add(`            </div>`);
add(`            {/* Pipeline funnel */}`);
add(`            <div style={{background:"rgba(255,255,255,0.02)",border:"1px solid "+T.border,borderRadius:12,padding:"18px"}}>`);
add(`              <div style={{fontSize:13,fontWeight:700,color:T.white,marginBottom:14}}>Pipeline Distribution</div>`);
add(`              {allLeads.length === 0 ? (`);
add(`                <div style={{padding:"20px",textAlign:"center",color:T.textMuted,fontSize:12}}>No leads data available yet</div>`);
add(`              ) : (`);
add(`                <div style={{display:"flex",flexDirection:"column",gap:7}}>`);
add(`                  {Object.entries(PIPELINE).map(([st,cfg]) => {`);
add(`                    const count = allLeads.filter(l=>(l.status||"New Lead")===st).length;`);
add(`                    const pct = allLeads.length>0 ? Math.round(count*100/allLeads.length) : 0;`);
add(`                    return (`);
add(`                      <div key={st}>`);
add(`                        <div style={{display:"flex",justifyContent:"space-between",marginBottom:3}}>`);
add(`                          <span style={{fontSize:11,color:T.textSecondary}}>{cfg.icon} {st}</span>`);
add(`                          <span style={{fontSize:11,fontWeight:700,color:cfg.color}}>{count} <span style={{color:T.textMuted,fontWeight:400}}>({pct}%)</span></span>`);
add(`                        </div>`);
add(`                        <div style={{height:5,borderRadius:3,background:"rgba(255,255,255,0.06)",overflow:"hidden"}}>`);
add(`                          <div style={{height:"100%",width:pct+"%",borderRadius:3,background:cfg.color,opacity:0.85,transition:"width 0.8s"}} />`);
add(`                        </div>`);
add(`                      </div>`);
add(`                    );`);
add(`                  })}`);
add(`                </div>`);
add(`              )}`);
add(`            </div>`);
add(`          </div>`);
add(`        )}`);
add(``);
add(`        {/* ════════════════════════════════════════════════════════════`);
add(`             LEAD DETAIL DRAWER (slide-in right panel)`);
add(`           ════════════════════════════════════════════════════════════ */}`);
add(`        {selectedLead && (`);
add(`          <div style={{background:"rgba(255,255,255,0.02)",border:"1px solid "+T.border,borderRadius:12,overflow:"hidden",display:"flex",flexDirection:"column",maxHeight:720,position:"sticky",top:0}}>`);
add(`            {/* Drawer header */}`);
add(`            <div style={{padding:"14px 16px",borderBottom:"1px solid "+T.border,background:"rgba(255,255,255,0.02)"}}>`);
add(`              <div style={{display:"flex",justifyContent:"space-between",alignItems:"flex-start",marginBottom:8}}>`);
add(`                <div style={{fontFamily:"'Fraunces',serif",fontSize:15,fontWeight:800,color:T.white,flex:1,overflow:"hidden",textOverflow:"ellipsis",whiteSpace:"nowrap",paddingRight:8}}>{selectedLead.name||"Unnamed Lead"}</div>`);
add(`                <button type="button" onClick={()=>setSelectedLead(null)} style={{background:"none",border:"none",color:T.textMuted,cursor:"pointer",fontSize:18,lineHeight:1,flexShrink:0}}>×</button>`);
add(`              </div>`);
add(`              <div style={{display:"flex",gap:6,flexWrap:"wrap"}}>`);
add(`                <PipelineBadge status={selectedLead.status||"New Lead"} />`);
add(`                {parseFloat(selectedLead.budget||0)>=(GOLDEN_VISA_THRESHOLD||2000000)&&(`);
add(`                  <span style={{fontSize:10,padding:"2px 7px",borderRadius:10,background:"rgba(212,168,67,0.2)",color:T.gold,fontWeight:700}}>🏆 Golden Visa Eligible</span>`);
add(`                )}`);
add(`              </div>`);
add(`            </div>`);
add(`            {/* Drawer tabs */}`);
add(`            <div style={{display:"flex",borderBottom:"1px solid "+T.border}}>`);
add(`              {[{k:"profile",l:"Profile"},{k:"activity",l:"Activity"},{k:"match",l:"Matches"}].map(t => (`);
add(`                <button key={t.k} type="button" onClick={()=>setDrawerTab(t.k)}`);
add(`                  style={{flex:1,padding:"9px 0",border:"none",borderBottom:drawerTab===t.k?"2px solid "+T.gold:"2px solid transparent",background:"transparent",color:drawerTab===t.k?T.gold:T.textMuted,fontSize:12,fontWeight:drawerTab===t.k?700:500,cursor:"pointer",fontFamily:"'Outfit',sans-serif",transition:"color 0.15s"}}>`);
add(`                  {t.l}`);
add(`                </button>`);
add(`              ))}`);
add(`            </div>`);
add(`            {/* Drawer body */}`);
add(`            <div style={{flex:1,overflowY:"auto",padding:"14px 16px"}}>`);
add(``);
add(`              {/* ── PROFILE TAB ── */}`);
add(`              {drawerTab==="profile" && (() => {`);
add(`                const sc = aiScore(selectedLead);`);
add(`                return (`);
add(`                  <div>`);
add(`                    {/* AI Score gauge */}`);
add(`                    <div style={{padding:"11px 13px",borderRadius:9,background:sc.color+"10",border:"1px solid "+sc.color+"30",marginBottom:14,display:"flex",justifyContent:"space-between",alignItems:"center"}}>`);
add(`                      <div>`);
add(`                        <div style={{fontSize:9,color:T.textMuted,fontWeight:700,textTransform:"uppercase",letterSpacing:0.7,marginBottom:3}}>AI LEAD SCORE</div>`);
add(`                        <div style={{fontFamily:"'Fraunces',serif",fontSize:20,fontWeight:800,color:sc.color}}>{sc.score}/100 — {sc.label}</div>`);
add(`                        <div style={{fontSize:10,color:T.textMuted,marginTop:2}}>Best time: {BEST_TIME[selectedLead.source]||"Anytime"}</div>`);
add(`                      </div>`);
add(`                      <div style={{width:44,height:44,borderRadius:"50%",border:"3px solid "+sc.color,display:"flex",alignItems:"center",justifyContent:"center",background:sc.color+"15",flexShrink:0}}>`);
add(`                        <span style={{fontSize:14,fontWeight:800,color:sc.color,fontFamily:"'Fraunces',serif"}}>{sc.score}</span>`);
add(`                      </div>`);
add(`                    </div>`);
add(`                    {/* All fields */}`);
add(`                    {[`);
add(`                      {label:"Phone",       value:selectedLead.phone,     link:selectedLead.phone?"tel:"+selectedLead.phone:null},`);
add(`                      {label:"WhatsApp",    value:selectedLead.phone?"Open WA":null, link:selectedLead.phone?"https://wa.me/"+clnPhone(selectedLead.phone):null, ext:true},`);
add(`                      {label:"Email",       value:selectedLead.email,     link:selectedLead.email?"mailto:"+selectedLead.email:null},`);
add(`                      {label:"Budget",      value:fmtBudget(selectedLead.budget)},`);
add(`                      {label:"Service Type",value:selectedLead.serviceType||selectedLead.type||"—"},`);
add(`                      {label:"Request Type",value:selectedLead.requestType||"—"},`);
add(`                      {label:"Project",     value:selectedLead.project||"—"},`);
add(`                      {label:"Source",      value:selectedLead.source||"—"},`);
add(`                      {label:"Channel",     value:selectedLead.channel||"—"},`);
add(`                      {label:"Campaign",    value:selectedLead.campaign||"—"},`);
add(`                      {label:"Nationality", value:selectedLead.nationality||"—"},`);
add(`                      {label:"Language",    value:selectedLead.language||"—"},`);
add(`                      {label:"Timeline",    value:selectedLead.timeline||"—"},`);
add(`                      {label:"Community",   value:selectedLead.community||"—"},`);
add(`                      {label:"Bedrooms",    value:selectedLead.bedrooms||"—"},`);
add(`                      {label:"Agent",       value:selectedLead.agentName||"—"},`);
add(`                      {label:"Added",       value:fmtDate(selectedLead.createdAt)},`);
add(`                      {label:"Last Update", value:fmtDate(selectedLead.updatedAt)},`);
add(`                    ].map((f,i) => (`);
add(`                      <div key={i} style={{display:"flex",justifyContent:"space-between",alignItems:"center",padding:"6px 0",borderBottom:i<17?"1px solid "+T.border+"40":"none"}}>`);
add(`                        <span style={{fontSize:11,color:T.textMuted,flexShrink:0}}>{f.label}</span>`);
add(`                        {f.link`);
add(`                          ? <a href={f.link} target={f.ext?"_blank":"_self"} rel="noopener noreferrer" style={{fontSize:11,color:T.gold,fontWeight:600,textDecoration:"none"}}>{f.value}</a>`);
add(`                          : <span style={{fontSize:11,color:T.white,fontWeight:600,textAlign:"right",maxWidth:180,overflow:"hidden",textOverflow:"ellipsis"}}>{f.value||"—"}</span>`);
add(`                        }`);
add(`                      </div>`);
add(`                    ))}`);
add(`                    {/* Tags */}`);
add(`                    {(selectedLead.tags||[]).length > 0 && (`);
add(`                      <div style={{marginTop:10}}>`);
add(`                        <div style={{fontSize:9,color:T.textMuted,marginBottom:5,fontWeight:700,textTransform:"uppercase",letterSpacing:0.7}}>Tags</div>`);
add(`                        <div style={{display:"flex",flexWrap:"wrap",gap:4}}>`);
add(`                          {(selectedLead.tags||[]).map((tag,i) => (`);
add(`                            <span key={i} style={{fontSize:10,padding:"2px 8px",borderRadius:10,background:"rgba(212,168,67,0.15)",color:T.gold,border:"1px solid rgba(212,168,67,0.3)"}}>{tag}</span>`);
add(`                          ))}`);
add(`                        </div>`);
add(`                      </div>`);
add(`                    )}`);
add(`                    {/* Comment */}`);
add(`                    {selectedLead.comment && (`);
add(`                      <div style={{marginTop:10,padding:"9px 11px",background:"rgba(255,255,255,0.03)",borderRadius:7,fontSize:11,color:T.textSecondary,lineHeight:1.6}}>`);
add(`                        <div style={{fontSize:9,color:T.textMuted,marginBottom:4,fontWeight:700,textTransform:"uppercase",letterSpacing:0.7}}>Comment</div>`);
add(`                        {selectedLead.comment}`);
add(`                      </div>`);
add(`                    )}`);
add(`                  </div>`);
add(`                );`);
add(`              })()}`);
add(``);
add(`              {/* ── ACTIVITY TAB ── */}`);
add(`              {drawerTab==="activity" && (`);
add(`                <div>`);
add(`                  <div style={{display:"flex",flexDirection:"column",gap:7,marginBottom:14,maxHeight:260,overflowY:"auto"}}>`);
add(`                    {(selectedLead.notes_log||[]).length === 0 && (`);
add(`                      <div style={{padding:"20px",textAlign:"center",color:T.textMuted,fontSize:11}}>No activity yet</div>`);
add(`                    )}`);
add(`                    {[...(selectedLead.notes_log||[])].reverse().map((note,i) => (`);
add(`                      <div key={i} style={{padding:"8px 10px",background:"rgba(255,255,255,0.03)",borderRadius:7,borderLeft:"2px solid "+T.gold}}>`);
add(`                        <div style={{fontSize:11,color:T.white,marginBottom:2}}>{note.text}</div>`);
add(`                        <div style={{fontSize:9,color:T.textMuted}}>{note.type||"Note"} · {note.by||"Agent"} · {fmtDate(note.at)}</div>`);
add(`                      </div>`);
add(`                    ))}`);
add(`                  </div>`);
add(`                  <div style={{borderTop:"1px solid "+T.border,paddingTop:12}}>`);
add(`                    <select value={noteType||"Note"} onChange={e=>setNoteType(e.target.value)}`);
add(`                      style={{width:"100%",padding:"7px 8px",background:"rgba(255,255,255,0.04)",border:"1px solid "+T.border,borderRadius:6,color:T.textSecondary,fontSize:11,outline:"none",marginBottom:7}}>`);
add(`                      {["Note","Call","Email","WhatsApp","Viewing","Offer","Follow Up","Status Update"].map(t => <option key={t} value={t}>{t}</option>)}`);
add(`                    </select>`);
add(`                    <textarea value={noteText||""} onChange={e=>setNoteText(e.target.value)} placeholder="Add activity note..." rows={3}`);
add(`                      style={{width:"100%",padding:"8px 10px",background:"rgba(255,255,255,0.04)",border:"1px solid "+T.border,borderRadius:7,color:T.white,fontSize:12,outline:"none",resize:"none",boxSizing:"border-box",marginBottom:8}} />`);
add(`                    <div style={{fontSize:10,color:T.textMuted,textAlign:"center"}}>Note saving requires Firestore write permission</div>`);
add(`                  </div>`);
add(`                </div>`);
add(`              )}`);
add(``);
add(`              {/* ── MATCHES TAB ── */}`);
add(`              {drawerTab==="match" && (`);
add(`                <div>`);
add(`                  <div style={{fontSize:11,color:T.textMuted,marginBottom:12}}>`);
add(`                    {matchedListings.length > 0`);
add(`                      ? "Properties matching budget + area:"`);
add(`                      : "No listings match this lead's requirements yet."}`);
add(`                  </div>`);
add(`                  {matchedListings.map((l,i) => (`);
add(`                    <div key={i} style={{padding:"10px 12px",background:"rgba(255,255,255,0.03)",borderRadius:8,marginBottom:8,border:"1px solid "+T.border}}>`);
add(`                      <div style={{fontSize:12,fontWeight:600,color:T.white,marginBottom:4}}>{l.title||l.name||"Property"}</div>`);
add(`                      <div style={{display:"flex",justifyContent:"space-between"}}>`);
add(`                        <span style={{fontSize:11,color:T.textMuted}}>{l.community||""}</span>`);
add(`                        <span style={{fontSize:12,fontWeight:700,color:T.gold}}>{fmtBudget(l.price||l.priceMin)}</span>`);
add(`                      </div>`);
add(`                    </div>`);
add(`                  ))}`);
add(`                  {matchedListings.length === 0 && (`);
add(`                    <button type="button" onClick={()=>handleTabChange?.("Projects")}`);
add(`                      style={{width:"100%",padding:"9px",borderRadius:7,border:"1px solid "+T.border,background:"rgba(212,168,67,0.06)",color:T.gold,fontSize:11,cursor:"pointer",fontFamily:"'Outfit',sans-serif"}}>`);
add(`                      Browse All Projects →`);
add(`                    </button>`);
add(`                  )}`);
add(`                </div>`);
add(`              )}`);
add(`            </div>`);
add(`          </div>`);
add(`        )}`);
add(`      </div>`);
add(``);
add(`      {/* ════════════════════════════════════════════════════════════`);
add(`           ADD LEAD MODAL — matches your screenshot exactly`);
add(`         ════════════════════════════════════════════════════════════ */}`);
add(`      {showAddForm && (`);
add(`        <div style={{position:"fixed",inset:0,background:"rgba(4,9,15,0.9)",zIndex:2000,display:"flex",alignItems:"center",justifyContent:"center",padding:"20px"}} onClick={e=>{if(e.target===e.currentTarget)setShowAddForm(false);}}>`);
add(`          <div style={{background:"#0D1117",borderRadius:14,border:"1px solid "+T.border,width:"100%",maxWidth:620,maxHeight:"90vh",overflowY:"auto"}} onClick={e=>e.stopPropagation()}>`);
add(`            {/* Modal header */}`);
add(`            <div style={{padding:"18px 20px",borderBottom:"1px solid "+T.border,display:"flex",justifyContent:"space-between",alignItems:"center",position:"sticky",top:0,background:"#0D1117",zIndex:1}}>`);
add(`              <div>`);
add(`                <div style={{fontFamily:"'Fraunces',serif",fontSize:16,fontWeight:900,color:T.white}}>Add New Lead</div>`);
add(`                <div style={{fontSize:11,color:T.textMuted,marginTop:2}}>Dubai Real Estate CRM</div>`);
add(`              </div>`);
add(`              <button type="button" onClick={()=>setShowAddForm(false)} style={{background:"rgba(255,255,255,0.06)",border:"1px solid "+T.border,borderRadius:8,color:T.textMuted,width:30,height:30,cursor:"pointer",fontSize:16,display:"flex",alignItems:"center",justifyContent:"center"}}>×</button>`);
add(`            </div>`);
add(`            <div style={{padding:"20px"}}>`);
add(``);
add(`              {/* SECTION: Contact Info */}`);
add(`              <div style={{fontSize:12,fontWeight:700,color:T.white,marginBottom:12,paddingBottom:6,borderBottom:"1px solid "+T.border}}>Contact Information</div>`);
add(`              <div style={{display:"grid",gridTemplateColumns:"1fr 1fr",gap:12,marginBottom:16}}>`);
add(`                <Field label="Full Name *">`);
add(`                  <FInput value={form.name} onChange={v=>setForm(f=>({...f,name:v}))} placeholder="Client full name" />`);
add(`                </Field>`);
add(`                <Field label="Phone *">`);
add(`                  <FInput value={form.phone} onChange={v=>setForm(f=>({...f,phone:v}))} placeholder="+971 50 XXX XXXX" type="tel" />`);
add(`                </Field>`);
add(`                <Field label="Email">`);
add(`                  <FInput value={form.email} onChange={v=>setForm(f=>({...f,email:v}))} placeholder="email@example.com" type="email" />`);
add(`                </Field>`);
add(`                <Field label="Budget (AED)">`);
add(`                  <FInput value={form.budget} onChange={v=>setForm(f=>({...f,budget:v}))} placeholder="e.g. 2000000" type="number" />`);
add(`                </Field>`);
add(`              </div>`);
add(``);
add(`              {/* SECTION: Request Information (from your screenshot) */}`);
add(`              <div style={{fontSize:12,fontWeight:700,color:T.white,marginBottom:12,paddingBottom:6,borderBottom:"1px solid "+T.border,display:"flex",alignItems:"center",gap:8}}>`);
add(`                <span style={{fontSize:16}}>📋</span> Request Information`);
add(`                <span style={{fontSize:10,color:T.textMuted,fontWeight:400}}>Project and classification details</span>`);
add(`              </div>`);
add(`              <div style={{display:"grid",gridTemplateColumns:"1fr 1fr",gap:12,marginBottom:16}}>`);
add(`                <Field label="Request Type">`);
add(`                  <FSelect value={form.requestType} onChange={v=>setForm(f=>({...f,requestType:v}))}>`);
add(`                    <option value="">Select request type</option>`);
add(`                    {REQUEST_TYPES.map(t => <option key={t} value={t}>{t}</option>)}`);
add(`                  </FSelect>`);
add(`                </Field>`);
add(`                <Field label="Service Type">`);
add(`                  <FSelect value={form.serviceType} onChange={v=>setForm(f=>({...f,serviceType:v}))}>`);
add(`                    <option value="">Select service type</option>`);
add(`                    {SERVICE_TYPES.map(t => <option key={t} value={t}>{t}</option>)}`);
add(`                  </FSelect>`);
add(`                </Field>`);
add(`                <Field label="Project">`);
add(`                  <FInput value={form.project} onChange={v=>setForm(f=>({...f,project:v}))} placeholder="Select project" />`);
add(`                </Field>`);
add(`                <Field label="Lead Source">`);
add(`                  <FSelect value={form.source} onChange={v=>setForm(f=>({...f,source:v}))}>`);
add(`                    <option value="">Select lead source</option>`);
add(`                    {LEAD_SOURCES.map(s => <option key={s} value={s}>{s}</option>)}`);
add(`                  </FSelect>`);
add(`                </Field>`);
add(`                <Field label="Channels">`);
add(`                  <FSelect value={form.channel} onChange={v=>setForm(f=>({...f,channel:v}))}>`);
add(`                    <option value="">Select channel</option>`);
add(`                    {CHANNELS.map(c => <option key={c} value={c}>{c}</option>)}`);
add(`                  </FSelect>`);
add(`                </Field>`);
add(`                <Field label="Campaign">`);
add(`                  <FInput value={form.campaign} onChange={v=>setForm(f=>({...f,campaign:v}))} placeholder="Select campaign" />`);
add(`                </Field>`);
add(`              </div>`);
add(`              <Field label="Comment">`);
add(`                <textarea value={form.comment} onChange={e=>setForm(f=>({...f,comment:e.target.value}))} placeholder="Enter comments or additional notes..." rows={3}`);
add(`                  style={{width:"100%",padding:"8px 10px",background:"rgba(255,255,255,0.04)",border:"1px solid "+T.border,borderRadius:7,color:T.white,fontSize:12,outline:"none",resize:"none",boxSizing:"border-box",marginBottom:16}} />`);
add(`              </Field>`);
add(``);
add(`              {/* SECTION: Agent Information (from your screenshot) */}`);
add(`              <div style={{fontSize:12,fontWeight:700,color:T.white,marginBottom:12,paddingBottom:6,borderBottom:"1px solid "+T.border,display:"flex",alignItems:"center",gap:8}}>`);
add(`                <span style={{fontSize:16}}>👤</span> Agent Information`);
add(`                <span style={{fontSize:10,color:T.textMuted,fontWeight:400}}>Assign to a sales agent</span>`);
add(`              </div>`);
add(`              <div style={{display:"grid",gridTemplateColumns:"1fr 1fr",gap:12,marginBottom:16}}>`);
add(`                <Field label="Agent Name">`);
add(`                  <FInput value={form.agentName} onChange={v=>setForm(f=>({...f,agentName:v}))} placeholder="Agent name" />`);
add(`                </Field>`);
add(`                <Field label="Pipeline Status">`);
add(`                  <FSelect value={form.status} onChange={v=>setForm(f=>({...f,status:v}))}>`);
add(`                    {Object.keys(PIPELINE).map(s => <option key={s} value={s}>{s}</option>)}`);
add(`                  </FSelect>`);
add(`                </Field>`);
add(`              </div>`);
add(``);
add(`              {/* SECTION: Additional Details */}`);
add(`              <div style={{fontSize:12,fontWeight:700,color:T.white,marginBottom:12,paddingBottom:6,borderBottom:"1px solid "+T.border}}>Additional Details</div>`);
add(`              <div style={{display:"grid",gridTemplateColumns:"1fr 1fr 1fr",gap:12,marginBottom:20}}>`);
add(`                <Field label="Nationality">`);
add(`                  <FSelect value={form.nationality} onChange={v=>setForm(f=>({...f,nationality:v}))}>`);
add(`                    <option value="">Select...</option>`);
add(`                    {NATS.map(n => <option key={n} value={n}>{n}</option>)}`);
add(`                  </FSelect>`);
add(`                </Field>`);
add(`                <Field label="Community">`);
add(`                  <FInput value={form.community} onChange={v=>setForm(f=>({...f,community:v}))} placeholder="e.g. Dubai Marina" />`);
add(`                </Field>`);
add(`                <Field label="Timeline">`);
add(`                  <FSelect value={form.timeline} onChange={v=>setForm(f=>({...f,timeline:v}))}>`);
add(`                    <option value="">Select...</option>`);
add(`                    <option value="Immediate">Immediate</option>`);
add(`                    <option value="1-3 months">1-3 Months</option>`);
add(`                    <option value="3-6 months">3-6 Months</option>`);
add(`                    <option value="6-12 months">6-12 Months</option>`);
add(`                    <option value="Just browsing">Just Browsing</option>`);
add(`                  </FSelect>`);
add(`                </Field>`);
add(`              </div>`);
add(``);
add(`              {/* Duplicate warning */}`);
add(`              {form.phone && isDuplicate(form.phone) && (`);
add(`                <div style={{padding:"8px 12px",background:"rgba(239,68,68,0.1)",border:"1px solid rgba(239,68,68,0.25)",borderRadius:7,marginBottom:12,fontSize:11,color:"#EF4444"}}>⚠ A lead with this phone number already exists in the system</div>`);
add(`              )}`);
add(``);
add(`              {/* Save button */}`);
add(`              <button type="button"`);
add(`                disabled={!form.name || !form.phone || saving}`);
add(`                onClick={async()=>{`);
add(`                  if(!form.name||!form.phone) return;`);
add(`                  setSaving(true);`);
add(`                  try {`);
add(`                    if(typeof leadAddSaving !== "undefined") {`);
add(`                      setLeadAddName(form.name); setLeadAddPhone(form.phone);`);
add(`                      setLeadAddEmail(form.email); setLeadAddBudget(form.budget);`);
add(`                      setLeadAddSource(form.source||"Manual");`);
add(`                      setLeadAddType(form.serviceType||"Buy");`);
add(`                      setLeadAddStatus(form.status||"New Lead");`);
add(`                      setLeadAddComm(form.community);`);
add(`                      setLeadAddNat(form.nationality);`);
add(`                      setLeadAddTimeline(form.timeline);`);
add(`                    }`);
add(`                    setShowAddForm(false);`);
add(`                    setForm({name:"",phone:"",email:"",budget:"",requestType:"",serviceType:"Buyer",project:"",source:"Manual",channel:"WhatsApp",campaign:"",status:"New Lead",nationality:"",language:"",timeline:"",community:"",bedrooms:"",comment:"",agentName:"",agentId:""});`);
add(`                  } catch(e){console.error(e);}`);
add(`                  setSaving(false);`);
add(`                }}`);
add(`                style={{width:"100%",padding:"12px",borderRadius:8,border:"none",background:(!form.name||!form.phone)?"rgba(212,168,67,0.3)":"linear-gradient(135deg,#D4A843,#B8902E)",color:"#0A0E1A",fontSize:14,fontWeight:700,cursor:(!form.name||!form.phone)?"not-allowed":"pointer",fontFamily:"'Outfit',sans-serif"}}>`);
add(`                {saving ? "Saving..." : "Save Lead"}`);
add(`              </button>`);
add(`            </div>`);
add(`          </div>`);
add(`        </div>`);
add(`      )}`);
add(``);
add(`      {/* ════════════════════════════════════════════════════════════`);
add(`           WA TEMPLATES MODAL`);
add(`         ════════════════════════════════════════════════════════════ */}`);
add(`      {showWATemplates && (`);
add(`        <div style={{position:"fixed",inset:0,background:"rgba(4,9,15,0.9)",zIndex:2100,display:"flex",alignItems:"center",justifyContent:"center",padding:"20px"}} onClick={e=>{if(e.target===e.currentTarget)setShowWATemplates(false);}}>`);
add(`          <div style={{background:"#0D1117",borderRadius:14,border:"1px solid "+T.border,width:"100%",maxWidth:500,padding:"22px"}} onClick={e=>e.stopPropagation()}>`);
add(`            <div style={{display:"flex",justifyContent:"space-between",alignItems:"center",marginBottom:16}}>`);
add(`              <div>`);
add(`                <div style={{fontFamily:"'Fraunces',serif",fontSize:15,fontWeight:900,color:T.white}}>WhatsApp Templates</div>`);
add(`                <div style={{fontSize:11,color:T.textMuted,marginTop:2}}>{filtered.filter(l=>l.phone).length} leads with phone in current filter</div>`);
add(`              </div>`);
add(`              <button type="button" onClick={()=>setShowWATemplates(false)} style={{background:"rgba(255,255,255,0.06)",border:"1px solid "+T.border,borderRadius:8,color:T.textMuted,width:30,height:30,cursor:"pointer",display:"flex",alignItems:"center",justifyContent:"center",fontSize:16}}>×</button>`);
add(`            </div>`);
add(`            <div style={{fontSize:11,color:T.textMuted,marginBottom:14,padding:"7px 10px",background:"rgba(37,211,102,0.06)",border:"1px solid rgba(37,211,102,0.15)",borderRadius:7}}>`);
add(`              Opens WhatsApp pre-filled for the first lead in your current filter`);
add(`            </div>`);
add(`            <div style={{display:"flex",flexDirection:"column",gap:8}}>`);
add(`              {WA_TEMPLATES.map((tmpl,i) => (`);
add(`                <button key={i} type="button"`);
add(`                  onClick={()=>{`);
add(`                    const lead = filtered.find(l=>l.phone);`);
add(`                    if (lead) sendWA(tmpl, lead);`);
add(`                    else { alert("No leads with phone numbers in current filter."); setShowWATemplates(false); }`);
add(`                  }}`);
add(`                  style={{padding:"12px 14px",borderRadius:9,border:"1px solid rgba(37,211,102,0.2)",background:"rgba(37,211,102,0.05)",textAlign:"left",cursor:"pointer",transition:"background 0.15s"}}`);
add(`                  onMouseEnter={e=>(e.currentTarget.style.background="rgba(37,211,102,0.1)")}`);
add(`                  onMouseLeave={e=>(e.currentTarget.style.background="rgba(37,211,102,0.05)")}`);
add(`                >`);
add(`                  <div style={{fontSize:12,fontWeight:700,color:"#25D366",marginBottom:4}}>{tmpl.label}</div>`);
add(`                  <div style={{fontSize:10,color:T.textMuted,lineHeight:1.5}}>{tmpl.body.slice(0,100)}...</div>`);
add(`                </button>`);
add(`              ))}`);
add(`            </div>`);
add(`          </div>`);
add(`        </div>`);
add(`      )}`);
add(``);
add(`    </div>`);
add(`  );`);
add(`}`);
add(``);
add(`export default MyLeadsTab;`);

fs.writeFileSync("src/tabs/MyLeadsTab.jsx", lines.join("\n"), "utf8");
console.log("Done. Lines:", lines.length);