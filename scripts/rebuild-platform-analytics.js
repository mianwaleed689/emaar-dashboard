const fs = require("fs");
const lines = [];
const a = s => lines.push(s);

a(`/* eslint-disable */`);
a(`/*`);
a(`  DXB ANALYTICS — PLATFORM ANALYTICS`);
a(`  Session 13 — Replaces PlatformLeadsTab B2B CRM`);
a(`  Shows aggregate platform metrics — NO individual agency lead data`);
a(`  Admin only`);
a(`*/`);
a(``);
a(`import React, { useState, useEffect, useMemo } from "react";`);
a(`import { collection, onSnapshot, query, orderBy } from "firebase/firestore";`);
a(`import { db } from "../firebase";`);
a(``);
a(`const T = {`);
a(`  bg:         "#0B0F14",`);
a(`  surface:    "#131821",`);
a(`  surfaceAlt: "#1A2028",`);
a(`  border:     "rgba(148,163,184,0.15)",`);
a(`  gold:       "#D4A843",`);
a(`  white:      "#FFFFFF",`);
a(`  textMuted:  "#94A3B8",`);
a(`  textDim:    "#64748B",`);
a(`  green:      "#10B981",`);
a(`  amber:      "#F59E0B",`);
a(`  red:        "#EF4444",`);
a(`  blue:       "#3B82F6",`);
a(`  purple:     "#A855F7",`);
a(`};`);
a(``);
a(`const fmtCur = n => { if(!n) return "AED 0"; if(n>=1e6) return "AED "+(n/1e6).toFixed(1)+"M"; if(n>=1e3) return "AED "+(n/1e3).toFixed(0)+"K"; return "AED "+n.toLocaleString(); };`);
a(`const fmtDate = d => { if(!d) return "—"; try { return new Date(d).toLocaleDateString("en-AE",{day:"2-digit",month:"short",year:"numeric"}); } catch(e){return "—";} };`);
a(`const daysAgo = d => !d?999:Math.floor((Date.now()-new Date(d).getTime())/86400000);`);
a(``);
a(`const PLAN_PRICE = 299; // AED per month`);
a(``);
a(`const KPICard = ({label,value,sub,color,bg}) => (`);
a(`  <div style={{background:T.surface,border:"1px solid "+T.border,borderRadius:12,padding:"16px 18px",position:"relative",overflow:"hidden"}}>`);
a(`    <div style={{position:"absolute",top:0,left:0,right:0,height:3,background:color,opacity:0.8}}/>`);
a(`    <div style={{fontSize:10,fontWeight:700,color:T.textMuted,textTransform:"uppercase",letterSpacing:0.8,marginBottom:8}}>{label}</div>`);
a(`    <div style={{fontSize:26,fontWeight:900,color:color,fontFamily:"'Fraunces',serif",lineHeight:1,marginBottom:4}}>{value}</div>`);
a(`    {sub&&<div style={{fontSize:11,color:T.textMuted}}>{sub}</div>}`);
a(`  </div>`);
a(`);`);
a(``);
a(`export default function PlatformLeadsTab() {`);
a(`  const [orgs,     setOrgs]     = useState([]);`);
a(`  const [users,    setUsers]    = useState([]);`);
a(`  const [loading,  setLoading]  = useState(true);`);
a(`  const [period,   setPeriod]   = useState("all"); // all | 30 | 7`);
a(``);
a(`  useEffect(() => {`);
a(`    const unsubs = [];`);
a(`    unsubs.push(onSnapshot(collection(db,"organisations"), snap => {`);
a(`      setOrgs(snap.docs.map(d=>({id:d.id,...d.data()})));`);
a(`      setLoading(false);`);
a(`    }));`);
a(`    unsubs.push(onSnapshot(collection(db,"users"), snap => {`);
a(`      setUsers(snap.docs.map(d=>({id:d.id,...d.data()})));`);
a(`    }));`);
a(`    return () => unsubs.forEach(u=>u());`);
a(`  },[]);`);
a(``);
a(`  // ── Derived metrics ────────────────────────────────────────`);
a(`  const cutoff = useMemo(()=>{ const d=new Date(); if(period==="30") d.setDate(d.getDate()-30); else if(period==="7") d.setDate(d.getDate()-7); else return null; return d; },[period]);`);
a(``);
a(`  const filtered = useMemo(()=>cutoff?orgs.filter(o=>new Date(o.createdAt)>=cutoff):orgs,[orgs,cutoff]);`);
a(``);
a(`  const totalOrgs    = orgs.length;`);
a(`  const activeOrgs   = orgs.filter(o=>o.status==="active"||o.paid===true).length;`);
a(`  const pendingOrgs  = orgs.filter(o=>o.status==="pending").length;`);
a(`  const suspendedOrgs= orgs.filter(o=>o.status==="suspended").length;`);
a(`  const mrr          = activeOrgs * PLAN_PRICE;`);
a(`  const arr          = mrr * 12;`);
a(``);
a(`  const totalUsers   = users.length;`);
a(`  const totalAgents  = users.filter(u=>u.orgRole==="agent").length;`);
a(`  const totalManagers= users.filter(u=>u.orgRole==="manager"||u.orgRole==="director"||u.orgRole==="owner").length;`);
a(`  const suspended    = users.filter(u=>u.status==="suspended").length;`);
a(``);
a(`  // New signups by month (last 6 months)`);
a(`  const monthlySignups = useMemo(()=>{`);
a(`    const months = [];`);
a(`    for(let i=5;i>=0;i--){`);
a(`      const d = new Date();`);
a(`      d.setMonth(d.getMonth()-i);`);
a(`      const label = d.toLocaleDateString("en-AE",{month:"short",year:"2-digit"});`);
a(`      const count = orgs.filter(o=>{`);
a(`        if(!o.createdAt) return false;`);
a(`        const od = new Date(o.createdAt);`);
a(`        return od.getMonth()===d.getMonth()&&od.getFullYear()===d.getFullYear();`);
a(`      }).length;`);
a(`      months.push({label,count});`);
a(`    }`);
a(`    return months;`);
a(`  },[orgs]);`);
a(``);
a(`  const maxSignups = Math.max(...monthlySignups.map(m=>m.count),1);`);
a(``);
a(`  // City breakdown`);
a(`  const cityBreakdown = useMemo(()=>{`);
a(`    const m={};`);
a(`    orgs.forEach(o=>{ const c=o.city||"Unknown"; m[c]=(m[c]||0)+1; });`);
a(`    return Object.entries(m).map(([city,count])=>({city,count})).sort((a,b)=>b.count-a.count);`);
a(`  },[orgs]);`);
a(``);
a(`  // Type breakdown`);
a(`  const typeBreakdown = useMemo(()=>{`);
a(`    const m={};`);
a(`    orgs.forEach(o=>{ const t=o.type||"Agency"; m[t]=(m[t]||0)+1; });`);
a(`    return Object.entries(m).map(([type,count])=>({type,count})).sort((a,b)=>b.count-a.count);`);
a(`  },[orgs]);`);
a(``);
a(`  const TYPE_COLORS = {"Agency":"#10B981","Developer":"#A855F7","Brokerage":"#F59E0B","Boutique":"#06B6D4","Property Management":"#EC4899"};`);
a(``);
a(`  if(loading) return <div style={{padding:40,color:T.textMuted,fontFamily:"'Outfit',sans-serif"}}>Loading platform analytics...</div>;`);
a(``);
a(`  return (`);
a(`    <div style={{padding:"20px 28px 60px",background:T.bg,minHeight:"100vh",fontFamily:"'Outfit',sans-serif"}}>`);
a(``);
a(`      {/* ── Header ── */}`);
a(`      <div style={{display:"flex",alignItems:"center",justifyContent:"space-between",marginBottom:24,flexWrap:"wrap",gap:10}}>`);
a(`        <div>`);
a(`          <h1 style={{margin:0,fontSize:24,color:T.gold,fontFamily:"'Fraunces',serif",fontWeight:700}}>Platform Analytics</h1>`);
a(`          <p style={{margin:"4px 0 0",fontSize:12,color:T.textMuted}}>Aggregate metrics · No individual agency lead data · Admin only</p>`);
a(`        </div>`);
a(`        <div style={{display:"flex",gap:6}}>`);
a(`          {[{k:"7",l:"Last 7 days"},{k:"30",l:"Last 30 days"},{k:"all",l:"All time"}].map(v=>(`);
a(`            <button key={v.k} type="button" onClick={()=>setPeriod(v.k)}`);
a(`              style={{padding:"7px 14px",borderRadius:7,border:"1px solid "+(period===v.k?T.gold:T.border),background:period===v.k?"rgba(212,168,67,0.1)":"transparent",color:period===v.k?T.gold:T.textMuted,fontSize:11,fontWeight:600,cursor:"pointer",fontFamily:"'Outfit',sans-serif"}}>`);
a(`              {v.l}`);
a(`            </button>`);
a(`          ))}`);
a(`        </div>`);
a(`      </div>`);
a(``);
a(`      {/* ── KPI Row ── */}`);
a(`      <div style={{display:"grid",gridTemplateColumns:"repeat(auto-fill,minmax(160px,1fr))",gap:12,marginBottom:24}}>`);
a(`        <KPICard label="Total Agencies"  value={totalOrgs}        sub={pendingOrgs+" pending approval"} color={T.gold}/>`);
a(`        <KPICard label="Active Paid"     value={activeOrgs}       sub={"AED "+PLAN_PRICE+"/mo each"}    color={T.green}/>`);
a(`        <KPICard label="MRR"             value={fmtCur(mrr)}      sub="Monthly recurring revenue"       color={T.green}/>`);
a(`        <KPICard label="ARR"             value={fmtCur(arr)}      sub="Annual recurring revenue"        color={T.green}/>`);
a(`        <KPICard label="Total Users"     value={totalUsers}       sub={totalAgents+" agents · "+totalManagers+" managers"} color={T.blue}/>`);
a(`        <KPICard label="Suspended"       value={suspendedOrgs}    sub={suspended+" users suspended"}    color={suspendedOrgs>0?T.red:T.textMuted}/>`);
a(`      </div>`);
a(``);
a(`      {/* ── Main grid ── */}`);
a(`      <div style={{display:"grid",gridTemplateColumns:"1fr 1fr",gap:16,marginBottom:16}}>`);
a(``);
a(`        {/* Monthly signups chart */}`);
a(`        <div style={{background:T.surface,border:"1px solid "+T.border,borderRadius:12,padding:"18px 20px"}}>`);
a(`          <div style={{fontSize:13,fontWeight:700,color:T.white,marginBottom:4}}>Monthly signups</div>`);
a(`          <div style={{fontSize:11,color:T.textMuted,marginBottom:20}}>New agency registrations per month</div>`);
a(`          <div style={{display:"flex",alignItems:"flex-end",gap:8,height:120}}>`);
a(`            {monthlySignups.map((m,i)=>(`);
a(`              <div key={i} style={{flex:1,display:"flex",flexDirection:"column",alignItems:"center",gap:6}}>`);
a(`                <div style={{fontSize:11,fontWeight:700,color:T.gold}}>{m.count||""}</div>`);
a(`                <div style={{width:"100%",background:m.count>0?"rgba(212,168,67,0.7)":"rgba(255,255,255,0.05)",borderRadius:"4px 4px 0 0",height:m.count>0?(m.count/maxSignups)*100+"%":"8px",minHeight:8,transition:"height 0.6s"}} />`);
a(`                <div style={{fontSize:9,color:T.textDim||T.textMuted,textAlign:"center"}}>{m.label}</div>`);
a(`              </div>`);
a(`            ))}`);
a(`          </div>`);
a(`        </div>`);
a(``);
a(`        {/* Revenue projection */}`);
a(`        <div style={{background:T.surface,border:"1px solid "+T.border,borderRadius:12,padding:"18px 20px"}}>`);
a(`          <div style={{fontSize:13,fontWeight:700,color:T.white,marginBottom:4}}>Revenue overview</div>`);
a(`          <div style={{fontSize:11,color:T.textMuted,marginBottom:20}}>Based on AED {PLAN_PRICE}/mo per active agency</div>`);
a(`          {[`);
a(`            {label:"MRR",          value:fmtCur(mrr),        color:T.green,  pct:100},`);
a(`            {label:"Quarterly",    value:fmtCur(mrr*3),      color:T.green,  pct:75},`);
a(`            {label:"ARR",          value:fmtCur(arr),        color:T.gold,   pct:50},`);
a(`            {label:"Per agency/yr",value:fmtCur(PLAN_PRICE*12),color:T.blue, pct:25},`);
a(`          ].map((r,i)=>(`);
a(`            <div key={i} style={{marginBottom:14}}>`);
a(`              <div style={{display:"flex",justifyContent:"space-between",marginBottom:5}}>`);
a(`                <span style={{fontSize:12,color:T.textMuted}}>{r.label}</span>`);
a(`                <span style={{fontSize:13,fontWeight:700,color:r.color,fontFamily:"'Fraunces',serif"}}>{r.value}</span>`);
a(`              </div>`);
a(`              <div style={{height:4,borderRadius:2,background:"rgba(255,255,255,0.05)"}}>`);
a(`                <div style={{height:"100%",width:r.pct+"%",background:r.color,borderRadius:2,opacity:0.7}}/>`);
a(`              </div>`);
a(`            </div>`);
a(`          ))}`);
a(`        </div>`);
a(`      </div>`);
a(``);
a(`      {/* ── Bottom row ── */}`);
a(`      <div style={{display:"grid",gridTemplateColumns:"1fr 1fr 1fr",gap:16,marginBottom:16}}>`);
a(``);
a(`        {/* City breakdown */}`);
a(`        <div style={{background:T.surface,border:"1px solid "+T.border,borderRadius:12,overflow:"hidden"}}>`);
a(`          <div style={{padding:"14px 18px",borderBottom:"1px solid "+T.border}}>`);
a(`            <div style={{fontSize:13,fontWeight:700,color:T.white}}>By city</div>`);
a(`          </div>`);
a(`          <div>`);
a(`            {cityBreakdown.length===0&&<div style={{padding:"20px",textAlign:"center",fontSize:12,color:T.textMuted}}>No data</div>}`);
a(`            {cityBreakdown.map((c,i)=>(`);
a(`              <div key={c.city} style={{display:"flex",alignItems:"center",justifyContent:"space-between",padding:"10px 18px",borderBottom:i<cityBreakdown.length-1?"1px solid "+T.border+"40":""}}>`);
a(`                <span style={{fontSize:12,color:T.white}}>{c.city}</span>`);
a(`                <div style={{display:"flex",alignItems:"center",gap:10}}>`);
a(`                  <div style={{width:60,height:4,borderRadius:2,background:"rgba(255,255,255,0.05)"}}>`);
a(`                    <div style={{height:"100%",width:(c.count/totalOrgs*100)+"%",background:T.gold,borderRadius:2}}/>`);
a(`                  </div>`);
a(`                  <span style={{fontSize:12,fontWeight:700,color:T.gold,minWidth:20,textAlign:"right"}}>{c.count}</span>`);
a(`                </div>`);
a(`              </div>`);
a(`            ))}`);
a(`          </div>`);
a(`        </div>`);
a(``);
a(`        {/* Type breakdown */}`);
a(`        <div style={{background:T.surface,border:"1px solid "+T.border,borderRadius:12,overflow:"hidden"}}>`);
a(`          <div style={{padding:"14px 18px",borderBottom:"1px solid "+T.border}}>`);
a(`            <div style={{fontSize:13,fontWeight:700,color:T.white}}>By type</div>`);
a(`          </div>`);
a(`          <div>`);
a(`            {typeBreakdown.length===0&&<div style={{padding:"20px",textAlign:"center",fontSize:12,color:T.textMuted}}>No data</div>}`);
a(`            {typeBreakdown.map((t,i)=>(`);
a(`              <div key={t.type} style={{display:"flex",alignItems:"center",justifyContent:"space-between",padding:"10px 18px",borderBottom:i<typeBreakdown.length-1?"1px solid "+T.border+"40":""}}>`);
a(`                <div style={{display:"flex",alignItems:"center",gap:8}}>`);
a(`                  <div style={{width:8,height:8,borderRadius:"50%",background:TYPE_COLORS[t.type]||T.textMuted}}/>`);
a(`                  <span style={{fontSize:12,color:T.white}}>{t.type}</span>`);
a(`                </div>`);
a(`                <span style={{fontSize:12,fontWeight:700,color:TYPE_COLORS[t.type]||T.gold}}>{t.count}</span>`);
a(`              </div>`);
a(`            ))}`);
a(`          </div>`);
a(`        </div>`);
a(``);
a(`        {/* User breakdown */}`);
a(`        <div style={{background:T.surface,border:"1px solid "+T.border,borderRadius:12,overflow:"hidden"}}>`);
a(`          <div style={{padding:"14px 18px",borderBottom:"1px solid "+T.border}}>`);
a(`            <div style={{fontSize:13,fontWeight:700,color:T.white}}>Platform users</div>`);
a(`          </div>`);
a(`          <div>`);
a(`            {[`);
a(`              {label:"Total users",    value:totalUsers,   color:T.blue},`);
a(`              {label:"Agents",         value:totalAgents,  color:T.gold},`);
a(`              {label:"Managers",       value:totalManagers,color:T.green},`);
a(`              {label:"Pending approval",value:pendingOrgs, color:T.amber},`);
a(`              {label:"Suspended users",value:suspended,    color:suspended>0?T.red:T.textMuted},`);
a(`            ].map((r,i)=>(`);
a(`              <div key={i} style={{display:"flex",alignItems:"center",justifyContent:"space-between",padding:"10px 18px",borderBottom:i<4?"1px solid "+T.border+"40":""}}>`);
a(`                <span style={{fontSize:12,color:T.textMuted}}>{r.label}</span>`);
a(`                <span style={{fontSize:13,fontWeight:700,color:r.color,fontFamily:"'Fraunces',serif"}}>{r.value}</span>`);
a(`              </div>`);
a(`            ))}`);
a(`          </div>`);
a(`        </div>`);
a(`      </div>`);
a(``);
a(`      {/* ── Agencies table ── */}`);
a(`      <div style={{background:T.surface,border:"1px solid "+T.border,borderRadius:12,overflow:"hidden"}}>`);
a(`        <div style={{padding:"14px 18px",borderBottom:"1px solid "+T.border,display:"flex",justifyContent:"space-between",alignItems:"center"}}>`);
a(`          <div style={{fontSize:13,fontWeight:700,color:T.white}}>All agencies</div>`);
a(`          <div style={{fontSize:11,color:T.textMuted}}>{orgs.length} registered</div>`);
a(`        </div>`);
a(`        <div style={{overflowX:"auto"}}>`);
a(`          <div style={{display:"grid",gridTemplateColumns:"2fr 1fr 1fr 1fr 100px 100px",minWidth:600,padding:"8px 18px",fontSize:9,fontWeight:700,color:T.textMuted,textTransform:"uppercase",letterSpacing:0.8,borderBottom:"1px solid "+T.border}}>`);
a(`            {["Agency","Type","City","Owner","Registered","Status"].map((h,i)=>(<div key={i}>{h}</div>))}`);
a(`          </div>`);
a(`          {orgs.length===0&&<div style={{padding:"40px",textAlign:"center",fontSize:12,color:T.textMuted}}>No agencies registered yet</div>}`);
a(`          {orgs.sort((a,b)=>new Date(b.createdAt||0)-new Date(a.createdAt||0)).map((org,i)=>(`);
a(`            <div key={org.id} style={{display:"grid",gridTemplateColumns:"2fr 1fr 1fr 1fr 100px 100px",minWidth:600,padding:"12px 18px",alignItems:"center",borderBottom:i<orgs.length-1?"1px solid "+T.border+"30":"",background:i%2===0?"transparent":"rgba(255,255,255,0.01)"}}>`);
a(`              <div>`);
a(`                <div style={{fontSize:12,fontWeight:600,color:T.white}}>{org.name||"Unnamed"}</div>`);
a(`                {org.reraNo&&<div style={{fontSize:10,color:T.textMuted}}>RERA: {org.reraNo}</div>}`);
a(`              </div>`);
a(`              <div style={{fontSize:11,color:TYPE_COLORS[org.type]||T.textMuted,fontWeight:600}}>{org.type||"Agency"}</div>`);
a(`              <div style={{fontSize:11,color:T.textMuted}}>{org.city||"Dubai"}</div>`);
a(`              <div style={{fontSize:11,color:T.textMuted,overflow:"hidden",textOverflow:"ellipsis",whiteSpace:"nowrap"}}>{org.ownerEmail||"—"}</div>`);
a(`              <div style={{fontSize:11,color:T.textMuted}}>{fmtDate(org.createdAt)}</div>`);
a(`              <div>`);
a(`                <span style={{fontSize:10,padding:"3px 8px",borderRadius:8,fontWeight:700,`);
a(`                  background:org.status==="active"||org.paid?"rgba(16,185,129,0.1)":org.status==="pending"?"rgba(245,158,11,0.1)":"rgba(239,68,68,0.1)",`);
a(`                  color:org.status==="active"||org.paid?"#10B981":org.status==="pending"?"#F59E0B":"#EF4444"}}>`);
a(`                  {org.status==="active"||org.paid?"Active":org.status==="pending"?"Pending":"Suspended"}`);
a(`                </span>`);
a(`              </div>`);
a(`            </div>`);
a(`          ))}`);
a(`        </div>`);
a(`      </div>`);
a(``);
a(`    </div>`);
a(`  );`);
a(`}`);

fs.writeFileSync("src/admin/PlatformLeadsTab.jsx", lines.join("\n"), "utf8");
console.log("Done. Lines:", lines.length);