const fs = require("fs");

// Write the new OverviewTab piece by piece to avoid template literal issues
const lines = [];

const add = (s) => lines.push(s);

add(`/* eslint-disable */`);
add(`/* DXB ANALYTICS - OVERVIEW TAB (World Class Rebuild) - Session 7 April 2026 */`);
add(``);
add(`import React, { useState, useMemo } from "react";`);
add(`import { T } from "../data";`);
add(`import { SvgIcons } from "../components/Icons";`);
add(`import { useOverviewKpis, useMarketKpis } from "../hooks/useMarketMetrics";`);
add(``);
add(`const TAB_NAV = [`);
add(`  { section: "Market Intelligence", color: "#D4A843", tabs: [`);
add(`    { name: "Market", desc: "Dubai macro: AED 917B 2025 · +31% Q1 2026 · ValuStrat · REIDIN", icon: "📊" },`);
add(`    { name: "DLD Volumes", desc: "Transaction volumes by community · off-plan vs secondary mix", icon: "📋" },`);
add(`    { name: "Price History", desc: "PPSF trends 2020-2026 · ValuStrat VPI · community momentum", icon: "📈" },`);
add(`    { name: "Neighbourhoods", desc: "152 communities · yields · metro · schools · supply risk", icon: "🏘" },`);
add(`    { name: "Launch Calendar", desc: "Upcoming off-plan launches · EOI open · developer pipeline", icon: "🗓" },`);
add(`    { name: "Currency", desc: "AED vs major currencies · live exchange rates", icon: "💱" },`);
add(`  ]},`);
add(`  { section: "Property Explorer", color: "#63B3ED", tabs: [`);
add(`    { name: "Projects", desc: "Off-plan database · payment plans · handover dates · escrow", icon: "🏗" },`);
add(`    { name: "Map", desc: "Community map · yield heat layer · PPSF layer · volume layer", icon: "🗺" },`);
add(`    { name: "Handover", desc: "Project completion tracker · DLD handover timeline", icon: "🔑" },`);
add(`    { name: "Service Charges", desc: "RERA service charge rates · by community · per sqft", icon: "📄" },`);
add(`  ]},`);
add(`  { section: "Investment Tools", color: "#68D391", tabs: [`);
add(`    { name: "Yields", desc: "Gross/net yield by community · top performers · Bayut data", icon: "💰" },`);
add(`    { name: "STR vs LTR", desc: "Short-term vs long-term rental comparison", icon: "⚖️" },`);
add(`    { name: "Mortgage", desc: "UAE bank rates · EIBOR 3.59% · repayment calculator", icon: "🏦" },`);
add(`    { name: "Investment Score", desc: "DXB Analytics composite score · 99 communities ranked", icon: "⭐" },`);
add(`    { name: "Flip", desc: "Off-plan flip calculator · resale premium · transaction costs", icon: "🔄" },`);
add(`    { name: "DXB Estimate", desc: "AVM valuation · 3-method cross-check · DLD PPSF", icon: "🎯" },`);
add(`    { name: "Portfolio", desc: "Track your properties · ROI · equity · rental income", icon: "📁" },`);
add(`    { name: "Golden Visa", desc: "AED 2M threshold checker · 10-year residency", icon: "🛂" },`);
add(`    { name: "Risk", desc: "Supply risk · price cycle risk · community risk radar", icon: "⚠️" },`);
add(`  ]},`);
add(`  { section: "Developer Intelligence", color: "#FC8181", tabs: [`);
add(`    { name: "Financials", desc: "Developer P&L · revenue trends · Emaar vs DAMAC vs Sobha", icon: "📉" },`);
add(`    { name: "Developer Health", desc: "DXB composite health score · delivery rate", icon: "🏢" },`);
add(`    { name: "Competitors", desc: "Market share · launches · pricing positioning", icon: "🥊" },`);
add(`    { name: "Banking", desc: "UAE bank comparison · mortgage products · rates", icon: "🏛" },`);
add(`  ]},`);
add(`  { section: "CRM & Agency", color: "#9F7AEA", tabs: [`);
add(`    { name: "My Leads", desc: "Lead pipeline · follow-ups · conversion tracking", icon: "👥" },`);
add(`    { name: "Pipeline", desc: "Deal stages · commission tracker · revenue forecast", icon: "🎰" },`);
add(`    { name: "Listings", desc: "Your active listings · performance · lead conversion", icon: "📌" },`);
add(`    { name: "Marketing", desc: "Lead generation · channel performance · AI listing copy", icon: "📢" },`);
add(`    { name: "Team", desc: "Agent performance · team leaderboard · org management", icon: "🤝" },`);
add(`    { name: "Agency", desc: "Org profile · RERA card · commission splits", icon: "🏬" },`);
add(`  ]},`);
add(`];`);
add(``);
add(`const Signal = ({ label, value, color }) => (`);
add(`  <div style={{ display: "flex", alignItems: "center", gap: 6, padding: "4px 10px", borderRadius: 20, background: (color || "#D4A843") + "15", border: "1px solid " + (color || "#D4A843") + "30" }}>`);
add(`    <span style={{ fontSize: 9, color: color || "#D4A843" }}>●</span>`);
add(`    <span style={{ fontSize: 10, color: T.textSecondary }}>{label}</span>`);
add(`    <span style={{ fontSize: 10, fontWeight: 700, color: color || "#D4A843" }}>{value}</span>`);
add(`  </div>`);
add(`);`);
add(``);
add(`const QuickStat = ({ label, value, change, note, color, onClick }) => (`);
add(`  <div onClick={onClick} style={{ background: "rgba(255,255,255,0.03)", border: "1px solid " + T.border, borderRadius: 10, padding: "12px 14px", cursor: onClick ? "pointer" : "default" }}`);
add(`    onMouseEnter={e => onClick && (e.currentTarget.style.borderColor = (color || T.gold) + "60")}`);
add(`    onMouseLeave={e => onClick && (e.currentTarget.style.borderColor = T.border)}`);
add(`  >`);
add(`    <div style={{ fontSize: 10, color: T.textMuted, fontWeight: 700, textTransform: "uppercase", letterSpacing: 0.8, marginBottom: 7 }}>{label}</div>`);
add(`    <div style={{ fontFamily: "'Fraunces',serif", fontSize: 18, fontWeight: 800, color: color || T.white, lineHeight: 1.1, marginBottom: 4 }}>{value || "—"}</div>`);
add(`    {change && <div style={{ fontSize: 10, color: T.green }}>{change}</div>}`);
add(`    {note && <div style={{ fontSize: 10, color: T.textMuted, marginTop: 3 }}>{note}</div>}`);
add(`  </div>`);
add(`);`);
add(``);
add(`const ActivityItem = ({ icon, label, count, color, onClick }) => (`);
add(`  <div onClick={onClick} style={{ display: "flex", alignItems: "center", justifyContent: "space-between", padding: "10px 12px", borderRadius: 8, background: T.surfaceAlt, cursor: onClick ? "pointer" : "default", marginBottom: 8 }}`);
add(`    onMouseEnter={e => onClick && (e.currentTarget.style.background = "rgba(255,255,255,0.05)")}`);
add(`    onMouseLeave={e => onClick && (e.currentTarget.style.background = T.surfaceAlt)}`);
add(`  >`);
add(`    <div style={{ display: "flex", alignItems: "center", gap: 10 }}>`);
add(`      <span style={{ fontSize: 14 }}>{icon}</span>`);
add(`      <span style={{ fontSize: 12, color: T.textSecondary }}>{label}</span>`);
add(`    </div>`);
add(`    <div style={{ display: "flex", alignItems: "center", gap: 8 }}>`);
add(`      <span style={{ fontFamily: "'Fraunces',serif", fontSize: 16, fontWeight: 800, color: count > 0 ? color || T.gold : T.textMuted }}>{count}</span>`);
add(`      {onClick && <svg width="10" height="10" viewBox="0 0 24 24" fill="none" stroke={T.textMuted} strokeWidth="2"><polyline points="9 18 15 12 9 6"/></svg>}`);
add(`    </div>`);
add(`  </div>`);
add(`);`);
add(``);
add(`function OverviewTab({`);
add(`  liveMarketData, liveDLDVolumes, liveDevHealth, liveMortgageRates, liveYields,`);
add(`  allDevelopers, deals, listings, myLeads, myPortfolio, watchlist,`);
add(`  aiInsights, gDeveloper, lastDataSync, globalFilters = {}, handleTabChange,`);
add(`}) {`);
add(`  const [navExpanded, setNavExpanded] = useState(null);`);
add(`  const { data: firestoreOverviewKpis = [] } = useOverviewKpis();`);
add(`  const { data: firestoreMarketKpis = [] } = useMarketKpis();`);
add(`  const syncTime = lastDataSync ? lastDataSync.toLocaleTimeString("en-AE", { hour: "2-digit", minute: "2-digit" }) : null;`);
add(`  const allKpis = useMemo(() => {`);
add(`    const live = (liveMarketData || []).filter(d => d.metric && d.value);`);
add(`    return live.length > 0 ? live : firestoreOverviewKpis.length > 0 ? firestoreOverviewKpis : firestoreMarketKpis;`);
add(`  }, [liveMarketData, firestoreOverviewKpis, firestoreMarketKpis]);`);
add(`  const getKpi = (m) => allKpis.find(d => d.metric === m)?.value || "—";`);
add(`  const eibor3m = liveMortgageRates?.[0]?.eibor3m;`);
add(`  const eiborDisplay = eibor3m ? eibor3m.toFixed(2) + "%" : "3.59%";`);
add(`  const leadsCount = myLeads?.length || 0;`);
add(`  const dealsCount = deals?.length || 0;`);
add(`  const listingsCount = listings?.length || 0;`);
add(`  const portfolioCount = myPortfolio?.length || 0;`);
add(`  const watchlistCount = watchlist?.length || 0;`);
add(`  const devFilterLabel = gDeveloper && gDeveloper !== "all"`);
add(`    ? (allDevelopers || []).find(d => String(d.id).toLowerCase() === String(gDeveloper).toLowerCase())?.name || gDeveloper`);
add(`    : null;`);
add(``);
add(`  return (`);
add(`    <div style={{ paddingTop: 4, paddingBottom: 60 }}>`);
add(``);
add(`      {/* LIVE MARKET BANNER */}`);
add(`      <div style={{ background: "linear-gradient(135deg, rgba(212,168,67,0.08) 0%, rgba(99,179,237,0.04) 100%)", border: "1px solid rgba(212,168,67,0.2)", borderRadius: 14, padding: "16px 20px", marginBottom: 20 }}>`);
add(`        <div style={{ display: "flex", justifyContent: "space-between", alignItems: "flex-start", flexWrap: "wrap", gap: 12, marginBottom: 12 }}>`);
add(`          <div>`);
add(`            <div style={{ fontFamily: "'Fraunces',serif", fontSize: 18, fontWeight: 800, color: T.white, marginBottom: 4 }}>Dubai Real Estate · April 2026</div>`);
add(`            <div style={{ fontSize: 11, color: T.textSecondary }}>Q1 2026: AED 252B total transactions · +31% YoY · 60,303 deals · Month 57 of growth cycle</div>`);
add(`          </div>`);
add(`          <div style={{ display: "flex", alignItems: "center", gap: 8 }}>`);
add(`            <div style={{ width: 6, height: 6, borderRadius: "50%", background: T.green, animation: "pulse 2s infinite" }} />`);
add(`            <span style={{ fontSize: 10, color: T.textMuted }}>{syncTime ? "Synced " + syncTime : "Live · DXB Analytics"}</span>`);
add(`            {devFilterLabel && <span style={{ fontSize: 10, padding: "2px 8px", borderRadius: 10, background: "rgba(212,168,67,0.1)", color: T.gold, border: "1px solid rgba(212,168,67,0.2)" }}>{devFilterLabel}</span>}`);
add(`          </div>`);
add(`        </div>`);
add(`        <div style={{ display: "flex", flexWrap: "wrap", gap: 8 }}>`);
add(`          <Signal label="Q1 2026" value="AED 252B" color={T.gold} />`);
add(`          <Signal label="YoY Growth" value="+31%" color={T.green} />`);
add(`          <Signal label="Off-Plan" value="70-80%" color={T.gold} />`);
add(`          <Signal label="Avg PPSF" value="AED 1,759" color="#63B3ED" />`);
add(`          <Signal label="EIBOR 3M" value={eiborDisplay} color="#9F7AEA" />`);
add(`          <Signal label="Health" value="72/100 Growing" color={T.green} />`);
add(`          <Signal label="Cycle" value="Month 57" color={T.gold} />`);
add(`        </div>`);
add(`      </div>`);
add(``);
add(`      {/* WORKSPACE + MARKET PULSE */}`);
add(`      <div style={{ display: "grid", gridTemplateColumns: "280px 1fr", gap: 16, marginBottom: 24 }}>`);
add(`        <div style={{ background: "rgba(255,255,255,0.02)", border: "1px solid " + T.border, borderRadius: 12, padding: "18px" }}>`);
add(`          <div style={{ fontSize: 11, fontWeight: 700, color: T.textMuted, textTransform: "uppercase", letterSpacing: 0.8, marginBottom: 14 }}>Your Workspace</div>`);
add(`          <ActivityItem icon="👥" label="Active Leads" count={leadsCount} color="#63B3ED" onClick={() => handleTabChange?.("My Leads")} />`);
add(`          <ActivityItem icon="🤝" label="Active Deals" count={dealsCount} color={T.gold} onClick={() => handleTabChange?.("Pipeline")} />`);
add(`          <ActivityItem icon="📌" label="My Listings" count={listingsCount} color={T.green} onClick={() => handleTabChange?.("Listings")} />`);
add(`          <ActivityItem icon="🏠" label="Portfolio" count={portfolioCount} color="#FC8181" onClick={() => handleTabChange?.("Portfolio")} />`);
add(`          <ActivityItem icon="⭐" label="Watchlist" count={watchlistCount} color="#9F7AEA" />`);
add(`          {leadsCount === 0 && dealsCount === 0 && listingsCount === 0 && (`);
add(`            <div style={{ marginTop: 12, padding: "12px 14px", background: "rgba(212,168,67,0.05)", borderRadius: 8, border: "1px solid rgba(212,168,67,0.1)" }}>`);
add(`              <div style={{ fontSize: 11, color: T.textMuted, lineHeight: 1.6 }}><span style={{ color: T.gold, fontWeight: 700 }}>Get started:</span> Add your first lead in My Leads, create a listing, or track a deal in Pipeline.</div>`);
add(`            </div>`);
add(`          )}`);
add(`        </div>`);
add(`        <div>`);
add(`          <div style={{ fontSize: 11, fontWeight: 700, color: T.textMuted, textTransform: "uppercase", letterSpacing: 0.8, marginBottom: 12 }}>Market Pulse — FY2025 · Q1 2026 Update</div>`);
add(`          <div style={{ display: "grid", gridTemplateColumns: "repeat(auto-fill,minmax(155px,1fr))", gap: 10 }}>`);
add(`            <QuickStat label="Total Market Value" value="AED 917B" change="+20% YoY · DLD 2025" note="Q1 2026: AED 252B (+31%)" color={T.gold} onClick={() => handleTabChange?.("Market")} />`);
add(`            <QuickStat label="Total Transactions" value="270,000+" change="+20% YoY · DLD 2025" note="Q1 2026: 60,303 (+6%)" onClick={() => handleTabChange?.("DLD Volumes")} />`);
add(`            <QuickStat label="Avg PPSF" value="AED 1,863" change="+6% YoY · FY2025" note="Q1 2026: AED 1,759 (+12.5%)" color="#63B3ED" onClick={() => handleTabChange?.("Price History")} />`);
add(`            <QuickStat label="Avg Gross Yield" value="6.55%" change="Apts 7.03% · Villas 4.63%" note="REIDIN Dec 2025" color={T.green} onClick={() => handleTabChange?.("Yields")} />`);
add(`            <QuickStat label="EIBOR 3M" value={eiborDisplay} change="Falling · Fed easing cycle" note="CBUAE · Mortgage: ~4-5%" color="#9F7AEA" onClick={() => handleTabChange?.("Mortgage")} />`);
add(`            <QuickStat label="Off-Plan Share" value="65-80%" change="Q1 2026: 70-80%" note="FY2025: 65% · Growing" onClick={() => handleTabChange?.("Projects")} />`);
add(`            <QuickStat label="Active Developers" value="228" change="+40% from 163 in 2024" note="RERA registered · DLD" onClick={() => handleTabChange?.("Developer Health")} />`);
add(`            <QuickStat label="Units Launched" value="131,504" change="By Oct 2025 · DLD" note="~98K units forecast 2026" onClick={() => handleTabChange?.("Launch Calendar")} />`);
add(`          </div>`);
add(`        </div>`);
add(`      </div>`);
add(``);
add(`      {/* INTELLIGENCE PANEL */}`);
add(`      <div style={{ fontSize: 11, fontWeight: 700, color: T.textMuted, textTransform: "uppercase", letterSpacing: 0.8, marginBottom: 12 }}>Intelligence Panel — {devFilterLabel ? devFilterLabel : "All Dubai"}</div>`);
add(`      <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr 1fr", gap: 14, marginBottom: 24 }}>`);
add(``);
add(`        {/* Top Yield Communities */}`);
add(`        <div style={{ background: "rgba(255,255,255,0.02)", border: "1px solid " + T.border, borderRadius: 12, padding: "16px 18px" }}>`);
add(`          <div style={{ fontSize: 11, fontWeight: 700, color: T.textMuted, textTransform: "uppercase", letterSpacing: 0.8, marginBottom: 12 }}>Top Communities — Yield</div>`);
add(`          {liveYields?.length > 0`);
add(`            ? [...liveYields].sort((a, b) => (parseFloat(b.gross) || 0) - (parseFloat(a.gross) || 0)).slice(0, 6).map((y, i) => (`);
add(`                <div key={i} style={{ display: "flex", justifyContent: "space-between", alignItems: "center", padding: "6px 0", borderBottom: i < 5 ? "1px solid " + T.border : "none" }}>`);
add(`                  <div><div style={{ fontSize: 12, color: T.white }}>{y.community || "—"}</div><div style={{ fontSize: 10, color: T.textMuted }}>{y.tenantProfile || "Apartment"}</div></div>`);
add(`                  <div style={{ fontFamily: "'Fraunces',serif", fontSize: 15, fontWeight: 700, color: parseFloat(y.gross) >= 7 ? T.green : T.gold }}>{parseFloat(y.gross || 0).toFixed(1)}%</div>`);
add(`                </div>`);
add(`              ))`);
add(`            : [`);
add(`                { community: "International City", yield: "9.2%", profile: "Mixed" },`);
add(`                { community: "Dubai South", yield: "8.8%", profile: "Mixed" },`);
add(`                { community: "Discovery Gardens", yield: "8.5%", profile: "Professionals" },`);
add(`                { community: "Al Furjan", yield: "8.2%", profile: "Families" },`);
add(`                { community: "JLT", yield: "8.1%", profile: "Professionals" },`);
add(`                { community: "JVC", yield: "7.8%", profile: "Professionals" },`);
add(`              ].map((y, i) => (`);
add(`                <div key={i} style={{ display: "flex", justifyContent: "space-between", alignItems: "center", padding: "6px 0", borderBottom: i < 5 ? "1px solid " + T.border : "none" }}>`);
add(`                  <div><div style={{ fontSize: 12, color: T.white }}>{y.community}</div><div style={{ fontSize: 10, color: T.textMuted }}>{y.profile}</div></div>`);
add(`                  <div style={{ fontFamily: "'Fraunces',serif", fontSize: 15, fontWeight: 700, color: T.green }}>{y.yield}</div>`);
add(`                </div>`);
add(`              ))`);
add(`          }`);
add(`          <button type="button" onClick={() => handleTabChange?.("Yields")} style={{ width: "100%", marginTop: 12, padding: "7px 0", background: "rgba(212,168,67,0.06)", border: "1px solid " + T.border, borderRadius: 8, color: T.gold, fontSize: 11, fontWeight: 600, cursor: "pointer", fontFamily: "'Outfit',sans-serif" }}>View All Yields →</button>`);
add(`        </div>`);
add(``);
add(`        {/* DLD Volume */}`);
add(`        <div style={{ background: "rgba(255,255,255,0.02)", border: "1px solid " + T.border, borderRadius: 12, padding: "16px 18px" }}>`);
add(`          <div style={{ fontSize: 11, fontWeight: 700, color: T.textMuted, textTransform: "uppercase", letterSpacing: 0.8, marginBottom: 12 }}>DLD Transaction Volume</div>`);
add(`          {(() => {`);
add(`            const data = liveDLDVolumes?.length > 0 ? liveDLDVolumes : [`);
add(`              { community: "JVC", transactions: 18782 }, { community: "Business Bay", transactions: 12450 },`);
add(`              { community: "Dubai Marina", transactions: 11200 }, { community: "Downtown Dubai", transactions: 8900 },`);
add(`              { community: "Dubai Hills Estate", transactions: 8200 }, { community: "Sobha Hartland", transactions: 6800 },`);
add(`            ];`);
add(`            const sorted = [...data].sort((a, b) => (b.transactions || 0) - (a.transactions || 0)).slice(0, 6);`);
add(`            const max = sorted[0]?.transactions || 1;`);
add(`            return sorted.map((d, i) => (`);
add(`              <div key={i} style={{ marginBottom: 10 }}>`);
add(`                <div style={{ display: "flex", justifyContent: "space-between", marginBottom: 4 }}>`);
add(`                  <span style={{ fontSize: 11, color: T.textSecondary }}>{d.community}</span>`);
add(`                  <span style={{ fontSize: 11, color: T.white, fontWeight: 600 }}>{(d.transactions || 0).toLocaleString()}</span>`);
add(`                </div>`);
add(`                <div style={{ height: 4, borderRadius: 2, background: T.border }}>`);
add(`                  <div style={{ height: "100%", width: Math.round((d.transactions / max) * 100) + "%", borderRadius: 2, background: "linear-gradient(90deg, " + T.gold + ", #63B3ED)", transition: "width 0.8s" }} />`);
add(`                </div>`);
add(`              </div>`);
add(`            ));`);
add(`          })()}`);
add(`          {!liveDLDVolumes?.length && <div style={{ fontSize: 10, color: T.textMuted, marginTop: 4, fontStyle: "italic" }}>DXB Analytics · DLD 2025</div>}`);
add(`          <button type="button" onClick={() => handleTabChange?.("DLD Volumes")} style={{ width: "100%", marginTop: 12, padding: "7px 0", background: "rgba(212,168,67,0.06)", border: "1px solid " + T.border, borderRadius: 8, color: T.gold, fontSize: 11, fontWeight: 600, cursor: "pointer", fontFamily: "'Outfit',sans-serif" }}>View DLD Volumes →</button>`);
add(`        </div>`);
add(``);
add(`        {/* AI Insight + Dev Health */}`);
add(`        <div style={{ display: "flex", flexDirection: "column", gap: 12 }}>`);
add(`          <div style={{ background: "rgba(255,255,255,0.02)", border: "1px solid " + T.border, borderRadius: 12, padding: "16px 18px", flex: 1 }}>`);
add(`            <div style={{ display: "flex", alignItems: "center", gap: 8, marginBottom: 10 }}>`);
add(`              <div style={{ width: 6, height: 6, borderRadius: "50%", background: T.gold, animation: "pulse 2s infinite" }} />`);
add(`              <span style={{ fontSize: 11, fontWeight: 700, color: T.textMuted, textTransform: "uppercase", letterSpacing: 0.8 }}>AI Market Insight</span>`);
add(`            </div>`);
add(`            {aiInsights?.length > 0`);
add(`              ? <div style={{ fontSize: 12, color: T.textSecondary, lineHeight: 1.7 }}>{aiInsights[0]?.text || aiInsights[0]}</div>`);
add(`              : <div style={{ fontSize: 11, color: T.textSecondary, lineHeight: 1.7 }}><span style={{ color: T.gold, fontWeight: 700 }}>Q1 2026 signal:</span> AED 252B in Q1 despite regional uncertainty. Off-plan share deepened to 80%+. Value growth (+31%) outpacing volume (+6%) = market maturing. Buyers more selective on location and developer track record.</div>`);
add(`            }`);
add(`            <div style={{ marginTop: 10, fontSize: 10, color: T.textMuted }}>{aiInsights?.length > 0 ? "Powered by Claude · Updated this week" : "Research · DLD Q1 2026 · Edwards & Towers"}</div>`);
add(`          </div>`);
add(`          <div style={{ background: "rgba(255,255,255,0.02)", border: "1px solid " + T.border, borderRadius: 12, padding: "16px 18px" }}>`);
add(`            <div style={{ fontSize: 11, fontWeight: 700, color: T.textMuted, textTransform: "uppercase", letterSpacing: 0.8, marginBottom: 10 }}>Developer Health</div>`);
add(`            {liveDevHealth?.length > 0`);
add(`              ? [...liveDevHealth].slice(0, 4).map((d, i) => (`);
add(`                  <div key={i} style={{ display: "flex", justifyContent: "space-between", alignItems: "center", padding: "5px 0", borderBottom: i < 3 ? "1px solid " + T.border : "none" }}>`);
add(`                    <span style={{ fontSize: 11, color: T.textSecondary }}>{d.developer || d.name}</span>`);
add(`                    <span style={{ fontSize: 11, fontWeight: 700, padding: "2px 8px", borderRadius: 20, background: (d.score || 0) >= 75 ? "rgba(104,211,145,0.15)" : "rgba(212,168,67,0.15)", color: (d.score || 0) >= 75 ? T.green : T.gold }}>{d.score || "—"}</span>`);
add(`                  </div>`);
add(`                ))`);
add(`              : <div style={{ fontSize: 11, color: T.textMuted }}>Health scores load from Admin → Developer Health</div>`);
add(`            }`);
add(`            <button type="button" onClick={() => handleTabChange?.("Developer Health")} style={{ width: "100%", marginTop: 10, padding: "6px 0", background: "rgba(212,168,67,0.06)", border: "1px solid " + T.border, borderRadius: 8, color: T.gold, fontSize: 11, fontWeight: 600, cursor: "pointer", fontFamily: "'Outfit',sans-serif" }}>View All →</button>`);
add(`          </div>`);
add(`        </div>`);
add(`      </div>`);
add(``);
add(`      {/* PLATFORM NAVIGATOR */}`);
add(`      <div style={{ fontSize: 11, fontWeight: 700, color: T.textMuted, textTransform: "uppercase", letterSpacing: 0.8, marginBottom: 12 }}>Platform Navigator — All 33 Modules</div>`);
add(`      <div style={{ display: "flex", flexDirection: "column", gap: 10, marginBottom: 24 }}>`);
add(`        {TAB_NAV.map(section => (`);
add(`          <div key={section.section}>`);
add(`            <div onClick={() => setNavExpanded(navExpanded === section.section ? null : section.section)}`);
add(`              style={{ display: "flex", justifyContent: "space-between", alignItems: "center", padding: "10px 14px", background: "rgba(255,255,255,0.02)", border: "1px solid " + T.border, borderRadius: navExpanded === section.section ? "10px 10px 0 0" : 10, cursor: "pointer" }}`);
add(`              onMouseEnter={e => (e.currentTarget.style.background = "rgba(255,255,255,0.04)")}`);
add(`              onMouseLeave={e => (e.currentTarget.style.background = "rgba(255,255,255,0.02)")}`);
add(`            >`);
add(`              <div style={{ display: "flex", alignItems: "center", gap: 10 }}>`);
add(`                <div style={{ width: 8, height: 8, borderRadius: 2, background: section.color }} />`);
add(`                <span style={{ fontSize: 12, fontWeight: 700, color: T.white }}>{section.section}</span>`);
add(`                <span style={{ fontSize: 10, color: T.textMuted }}>· {section.tabs.length} modules</span>`);
add(`              </div>`);
add(`              <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke={T.textMuted} strokeWidth="2"><polyline points={navExpanded === section.section ? "18 15 12 9 6 15" : "6 9 12 15 18 15"} /></svg>`);
add(`            </div>`);
add(`            {navExpanded === section.section && (`);
add(`              <div style={{ border: "1px solid " + T.border, borderTop: "none", borderRadius: "0 0 10px 10px", padding: "8px", display: "grid", gridTemplateColumns: "repeat(auto-fill,minmax(200px,1fr))", gap: 6 }}>`);
add(`                {section.tabs.map(tab => (`);
add(`                  <div key={tab.name} onClick={() => handleTabChange?.(tab.name)}`);
add(`                    style={{ display: "flex", alignItems: "flex-start", gap: 10, padding: "10px 12px", borderRadius: 8, cursor: "pointer" }}`);
add(`                    onMouseEnter={e => (e.currentTarget.style.background = "rgba(255,255,255,0.04)")}`);
add(`                    onMouseLeave={e => (e.currentTarget.style.background = "transparent")}`);
add(`                  >`);
add(`                    <span style={{ fontSize: 16, flexShrink: 0, marginTop: 1 }}>{tab.icon}</span>`);
add(`                    <div>`);
add(`                      <div style={{ fontSize: 12, fontWeight: 600, color: T.white, marginBottom: 2 }}>{tab.name}</div>`);
add(`                      <div style={{ fontSize: 10, color: T.textMuted, lineHeight: 1.4 }}>{tab.desc}</div>`);
add(`                    </div>`);
add(`                  </div>`);
add(`                ))}`);
add(`              </div>`);
add(`            )}`);
add(`          </div>`);
add(`        ))}`);
add(`      </div>`);
add(``);
add(`      {/* FOOTER */}`);
add(`      <div style={{ paddingTop: 16, borderTop: "1px solid " + T.border, display: "flex", flexWrap: "wrap", gap: 8, alignItems: "center", justifyContent: "space-between" }}>`);
add(`        <div style={{ display: "flex", flexWrap: "wrap", gap: 6, alignItems: "center" }}>`);
add(`          <span style={{ fontSize: 10, color: T.textMuted }}>Sources:</span>`);
add(`          {[`);
add(`            { label: "DLD Q1 2026", url: "https://mediaoffice.ae/en/news/2026/april/09-04/dubai-real-estate-transactions-surge-31-to-reach-aed252-billion-in-q1-2026" },`);
add(`            { label: "DLD FY2025", url: "https://mediaoffice.ae/en/news/2026/january/12-01/dubais-real-estate-market-records-new-historic-milestone" },`);
add(`            { label: "REIDIN Dec 2025", url: "https://reidin.com" },`);
add(`            { label: "CBUAE EIBOR", url: "https://www.centralbank.ae/en/forex-eibor/eibor-rates/" },`);
add(`            { label: "Edwards & Towers Q1 2026", url: "https://edwardsandtowers.com/dubai-real-estate-market-q1-2026-analysis/" },`);
add(`          ].map(s => (`);
add(`            <a key={s.label} href={s.url} target="_blank" rel="noopener noreferrer" style={{ textDecoration: "none" }}>`);
add(`              <span style={{ fontSize: 10, color: T.textMuted, padding: "2px 8px", borderRadius: 10, border: "1px solid " + T.border, background: T.surfaceAlt, cursor: "pointer" }}>{s.label}</span>`);
add(`            </a>`);
add(`          ))}`);
add(`        </div>`);
add(`        <div style={{ fontSize: 10, color: T.textMuted }}>Session 7 · April 2026 · DXB Analytics</div>`);
add(`      </div>`);
add(``);
add(`    </div>`);
add(`  );`);
add(`}`);
add(``);
add(`export default OverviewTab;`);

fs.writeFileSync("src/tabs/OverviewTab.jsx", lines.join("\n"), "utf8");
console.log("Done. Lines:", lines.length);