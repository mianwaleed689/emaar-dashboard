const fs = require("fs");
const lines = [];
const a = s => lines.push(s);

a(`/* eslint-disable */`);
a(`/*`);
a(`  DXB ANALYTICS — TEAM TAB`);
a(`  Session 12 — Agent Account Creation + Deactivation`);
a(`  Manager creates agents directly from dashboard`);
a(`*/`);
a(``);
a(`import React, { useState } from "react";`);
a(`import { createUserWithEmailAndPassword } from "firebase/auth";`);
a(`import { doc, setDoc, updateDoc, collection, query, where, getDocs, arrayUnion } from "firebase/firestore";`);
a(`import { auth, db } from "../firebase";`);
a(`import { T } from "../data";`);
a(`import { cleanPhone } from "../utils";`);
a(`import { GOLDEN_VISA_THRESHOLD } from "../utils/constants";`);
a(``);
a(`const SOURCE_COLORS = {`);
a(`  "Property Finder":"#00C08B","Bayut":"#FF6B35","Dubizzle":"#E8003D","Meta/Facebook":"#1877F2",`);
a(`  "Instagram":"#E1306C","WhatsApp":"#25D366","Google Ads":"#4285F4","Referral":"#8B5CF6",`);
a(`  "Website":"#14B8A6","Manual":"#94A3B8","Cold Call":"#F59E0B","Email":"#6366F1"`);
a(`};`);
a(``);
a(`const fmtB = v => { const n=parseFloat(v||0); if(!n) return "—"; return n>=1e6?"AED "+(n/1e6).toFixed(1)+"M":"AED "+n.toLocaleString(); };`);
a(``);
a(`export default function TeamTab({ teamMembers=[], teamMembersLoading, myLeads=[], deals=[], orgRole, orgId, firebaseUser, orgName }) {`);
a(``);
a(`  const canManage = orgRole==="owner"||orgRole==="director"||orgRole==="manager";`);
a(`  if (!canManage) return (`);
a(`    <div style={{display:"flex",flexDirection:"column",alignItems:"center",justifyContent:"center",padding:"80px 20px",textAlign:"center"}}>`);
a(`      <svg width="48" height="48" viewBox="0 0 24 24" fill="none" stroke={T.textMuted} strokeWidth="1.5" strokeLinecap="round" style={{marginBottom:16}}><path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="M23 21v-2a4 4 0 0 0-3-3.87"/><path d="M16 3.13a4 4 0 0 1 0 7.75"/></svg>`);
a(`      <div style={{fontSize:16,fontWeight:700,color:T.white,marginBottom:6}}>Manager access only</div>`);
a(`      <div style={{fontSize:12,color:T.textMuted}}>Contact your administrator to request manager access</div>`);
a(`    </div>`);
a(`  );`);
a(``);
a(`  // ── State ──────────────────────────────────────────────────`);
a(`  const [showCreate,  setShowCreate]  = useState(false);`);
a(`  const [showDeact,   setShowDeact]   = useState(null);  // agent being deactivated`);
a(`  const [toast,       setToast]       = useState(null);`);
a(`  const [creating,    setCreating]    = useState(false);`);
a(`  const [deacting,    setDeacting]    = useState(false);`);
a(`  const [form, setForm] = useState({name:"",email:"",phone:"",password:""});`);
a(`  const F = (k,v) => setForm(p=>({...p,[k]:v}));`);
a(``);
a(`  const notify = (msg, type="success") => {`);
a(`    setToast({msg,type});`);
a(`    setTimeout(()=>setToast(null),3500);`);
a(`  };`);
a(``);
a(`  // ── Create agent account ───────────────────────────────────`);
a(`  const createAgent = async () => {`);
a(`    if(!form.name.trim())    { notify("Agent name is required","error"); return; }`);
a(`    if(!form.email.trim())   { notify("Email is required","error"); return; }`);
a(`    if(form.password.length<8){ notify("Password must be at least 8 characters","error"); return; }`);
a(`    if(!/[A-Z]/.test(form.password)){ notify("Password needs at least 1 uppercase letter","error"); return; }`);
a(`    if(!/[0-9]/.test(form.password)){ notify("Password needs at least 1 number","error"); return; }`);
a(`    setCreating(true);`);
a(`    try {`);
a(`      // Save current manager session before creating new user`);
a(`      const managerEmail    = firebaseUser?.email || "";`);
a(`      const managerUid      = firebaseUser?.uid   || "";`);
a(`      const managerPassword = null; // we don't have it — use Admin SDK in production`);
a(``);
a(`      // Create Firebase Auth account`);
a(`      const cred = await createUserWithEmailAndPassword(auth, form.email.trim(), form.password);`);
a(`      const agentUid = cred.user.uid;`);
a(``);
a(`      // Create users doc`);
a(`      await setDoc(doc(db,"users",agentUid),{`);
a(`        name:         form.name.trim(),`);
a(`        email:        form.email.trim(),`);
a(`        phone:        form.phone.trim(),`);
a(`        role:         "user",`);
a(`        orgRole:      "agent",`);
a(`        orgId:        orgId||"",`);
a(`        managerId:    managerUid,`);
a(`        paid:         true,`);
a(`        status:       "active",`);
a(`        onboardingComplete: false,`);
a(`        createdAt:    new Date().toISOString(),`);
a(`        createdBy:    managerUid,`);
a(`      });`);
a(``);
a(`      // Add to organisations/members subcollection`);
a(`      if(orgId){`);
a(`        await setDoc(doc(db,"organisations",orgId,"members",agentUid),{`);
a(`          uid:     agentUid,`);
a(`          name:    form.name.trim(),`);
a(`          email:   form.email.trim(),`);
a(`          phone:   form.phone.trim(),`);
a(`          orgRole: "agent",`);
a(`          orgId,`);
a(`          managerId: managerUid,`);
a(`          status:  "active",`);
a(`          createdAt: new Date().toISOString(),`);
a(`        },{merge:true});`);
a(`      }`);
a(``);
a(`      setForm({name:"",email:"",phone:"",password:""});`);
a(`      setShowCreate(false);`);
a(`      notify("Agent account created successfully! They can now log in.");`);
a(`    } catch(e) {`);
a(`      console.error(e);`);
a(`      if(e.code==="auth/email-already-in-use") notify("This email is already registered","error");`);
a(`      else if(e.code==="auth/invalid-email")   notify("Invalid email address","error");`);
a(`      else notify("Failed to create account: "+e.message,"error");`);
a(`    }`);
a(`    setCreating(false);`);
a(`  };`);
a(``);
a(`  // ── Deactivate agent ───────────────────────────────────────`);
a(`  const deactivateAgent = async (agent) => {`);
a(`    setDeacting(true);`);
a(`    try {`);
a(`      const managerUid = firebaseUser?.uid||"";`);
a(`      const now = new Date().toISOString();`);
a(``);
a(`      // 1. Suspend the agent`);
a(`      await updateDoc(doc(db,"users",agent.uid||agent.id),{`);
a(`        status:      "suspended",`);
a(`        suspendedAt: now,`);
a(`        suspendedBy: managerUid,`);
a(`      });`);
a(``);
a(`      // 2. Find all leads assigned to this agent`);
a(`      const leadsSnap = await getDocs(`);
a(`        query(collection(db,"leads"),`);
a(`          where("assignedTo","==",agent.uid||agent.id),`);
a(`          where("orgId","==",orgId||"")`);
a(`        )`);
a(`      );`);
a(``);
a(`      // 3. Return each lead to manager pool with tag`);
a(`      const batch = [];`);
a(`      leadsSnap.forEach(d => {`);
a(`        const lead = d.data();`);
a(`        const daysAssigned = lead.assignedAt`);
a(`          ? Math.floor((Date.now()-new Date(lead.assignedAt).getTime())/86400000)`);
a(`          : 0;`);
a(`        const tag = {`);
a(`          previousAgent:     agent.uid||agent.id,`);
a(`          previousAgentName: agent.name||agent.email||"Agent",`);
a(`          previousAgentDate: now,`);
a(`          previousAgentDays: daysAssigned,`);
a(`        };`);
a(`        const entry = {`);
a(`          text: "Lead returned to pool — previously assigned to "+`);
a(`                (agent.name||agent.email||"Agent")+`);
a(`                " for "+daysAssigned+" days",`);
a(`          type: "Note",`);
a(`          by:   "System",`);
a(`          at:   now,`);
a(`        };`);
a(`        batch.push(updateDoc(doc(db,"leads",d.id),{`);
a(`          assignedTo:       "",`);
a(`          assignedToName:   "",`);
a(`          updatedAt:        now,`);
a(`          notes_log:        arrayUnion(entry),`);
a(`          ...tag,`);
a(`        }));`);
a(`      });`);
a(``);
a(`      await Promise.all(batch);`);
a(`      setShowDeact(null);`);
a(`      notify("Agent deactivated. "+leadsSnap.size+" leads returned to pool.");`);
a(`    } catch(e) {`);
a(`      console.error(e);`);
a(`      notify("Deactivation failed: "+e.message,"error");`);
a(`    }`);
a(`    setDeacting(false);`);
a(`  };`);
a(``);
a(`  // ── Reactivate agent ───────────────────────────────────────`);
a(`  const reactivateAgent = async (agent) => {`);
a(`    try {`);
a(`      await updateDoc(doc(db,"users",agent.uid||agent.id),{`);
a(`        status:        "active",`);
a(`        reactivatedAt: new Date().toISOString(),`);
a(`        reactivatedBy: firebaseUser?.uid||"",`);
a(`      });`);
a(`      notify(agent.name+" reactivated successfully");`);
a(`    } catch(e) { notify("Reactivation failed","error"); }`);
a(`  };`);
a(``);
a(`  // ── Derived metrics ────────────────────────────────────────`);
a(`  const agents   = teamMembers.filter(u=>u.orgRole==="agent"||u.role==="agent");`);
a(`  const weekAgo  = new Date(Date.now()-7*24*60*60*1000);`);
a(``);
a(`  const agentStats = agents.map(agent => {`);
a(`    const uid        = agent.uid||agent.id;`);
a(`    const aLeads     = myLeads.filter(l=>l.assignedTo===uid);`);
a(`    const aDeals     = deals.filter(d=>d.agentId===uid);`);
a(`    const closed     = aDeals.filter(d=>d.stage==="Completed");`);
a(`    const totalVal   = aDeals.reduce((s,d)=>s+parseFloat(d.price||0),0);`);
a(`    const conv       = aLeads.length>0?((closed.length/aLeads.length)*100).toFixed(1):"0.0";`);
a(`    const newWeek    = aLeads.filter(l=>new Date(l.createdAt)>=weekAgo).length;`);
a(`    const overdue    = aLeads.filter(l=>l.updatedAt&&(Date.now()-new Date(l.updatedAt))>3*86400000&&["New Lead","Potential","No Answer"].includes(l.status)).length;`);
a(`    return {...agent,uid,aLeads,aDeals,closed,totalVal,conv,newWeek,overdue};`);
a(`  }).sort((a,b)=>b.closed.length-a.closed.length);`);
a(``);
a(`  const teamLeads  = myLeads.length;`);
a(`  const teamDeals  = deals.length;`);
a(`  const teamClosed = deals.filter(d=>d.stage==="Completed").length;`);
a(`  const teamVal    = deals.reduce((s,d)=>s+parseFloat(d.price||0),0);`);
a(`  const teamOverdue= myLeads.filter(l=>l.updatedAt&&(Date.now()-new Date(l.updatedAt))>3*86400000&&["New Lead","Potential","No Answer"].includes(l.status));`);
a(``);
a(`  const srcStats = [...new Set(myLeads.map(l=>l.source).filter(Boolean))].map(src=>{`);
a(`    const srcLeads = myLeads.filter(l=>l.source===src);`);
a(`    const srcDeals = deals.filter(d=>srcLeads.some(l=>l.assignedTo===d.agentId));`);
a(`    const cls = srcDeals.filter(d=>d.stage==="Completed").length;`);
a(`    return {src,leads:srcLeads.length,closed:cls,conv:srcLeads.length>0?((cls/srcLeads.length)*100).toFixed(1):"0.0",color:SOURCE_COLORS[src]||T.textMuted};`);
a(`  }).sort((a,b)=>parseFloat(b.conv)-parseFloat(a.conv));`);
a(``);
a(`  const inp = {width:"100%",padding:"8px 10px",background:"rgba(255,255,255,0.04)",border:"1px solid "+T.border,borderRadius:7,color:T.white,fontSize:12,outline:"none",boxSizing:"border-box",fontFamily:"'Outfit',sans-serif"};`);
a(``);
a(`  return (`);
a(`    <div style={{paddingBottom:60}}>`);
a(``);
a(`      {/* ══ HEADER ══════════════════════════════════════════════ */}`);
a(`      <div style={{display:"flex",alignItems:"center",justifyContent:"space-between",marginBottom:20,flexWrap:"wrap",gap:12}}>`);
a(`        <div>`);
a(`          <h1 style={{fontFamily:"'Fraunces',serif",fontSize:22,fontWeight:900,color:T.white,margin:0}}>Team Dashboard</h1>`);
a(`          <p style={{fontSize:12,color:T.textMuted,margin:"4px 0 0"}}>`);
a(`            {agents.length} agents · {teamLeads} leads · {teamDeals} deals · Live`);
a(`          </p>`);
a(`        </div>`);
a(`        <button type="button" onClick={()=>setShowCreate(true)}`);
a(`          style={{padding:"9px 18px",borderRadius:8,border:"none",background:"linear-gradient(135deg,#D4A843,#B8902E)",color:"#0A0E1A",fontSize:12,fontWeight:700,cursor:"pointer",fontFamily:"'Outfit',sans-serif",display:"flex",alignItems:"center",gap:6}}>`);
a(`          <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2.5"><line x1="12" y1="5" x2="12" y2="19"/><line x1="5" y1="12" x2="19" y2="12"/></svg>`);
a(`          Add Agent`);
a(`        </button>`);
a(`      </div>`);
a(``);
a(`      {/* ══ KPI BAR ══════════════════════════════════════════════ */}`);
a(`      <div style={{display:"grid",gridTemplateColumns:"repeat(5,1fr)",gap:10,marginBottom:20}}>`);
a(`        {[`);
a(`          {label:"Total Leads",    value:teamLeads,   color:T.gold},`);
a(`          {label:"Active Deals",   value:teamDeals-teamClosed, color:"#14B8A6"},`);
a(`          {label:"Deals Closed",   value:teamClosed,  color:"#10B981"},`);
a(`          {label:"Pipeline",       value:fmtB(teamVal), color:"#8B5CF6"},`);
a(`          {label:"Overdue",        value:teamOverdue.length, color:teamOverdue.length>0?"#EF4444":"#10B981"},`);
a(`        ].map((k,i)=>(`);
a(`          <div key={i} style={{background:T.card||"rgba(255,255,255,0.03)",border:"1px solid "+T.border,borderRadius:12,padding:"14px 16px",position:"relative",overflow:"hidden"}}>`);
a(`            <div style={{position:"absolute",top:0,left:0,right:0,height:2,background:k.color,opacity:0.8}}/>`);
a(`            <div style={{fontSize:10,fontWeight:700,color:T.textMuted,textTransform:"uppercase",letterSpacing:0.8,marginBottom:8}}>{k.label}</div>`);
a(`            <div style={{fontSize:22,fontWeight:900,color:k.color,fontFamily:"'Fraunces',serif",lineHeight:1}}>{k.value}</div>`);
a(`          </div>`);
a(`        ))}`);
a(`      </div>`);
a(``);
a(`      {/* ══ AGENT TABLE ══════════════════════════════════════════ */}`);
a(`      <div style={{background:"rgba(255,255,255,0.02)",border:"1px solid "+T.border,borderRadius:14,overflow:"hidden",marginBottom:16}}>`);
a(`        <div style={{padding:"14px 18px",borderBottom:"1px solid "+T.border,display:"flex",alignItems:"center",justifyContent:"space-between"}}>`);
a(`          <div style={{fontSize:13,fontWeight:700,color:T.white}}>Agent Roster</div>`);
a(`          <div style={{fontSize:11,color:T.textMuted}}>{agents.length} agents</div>`);
a(`        </div>`);
a(`        <div style={{overflowX:"auto"}}>`);
a(`          <div style={{display:"grid",gridTemplateColumns:"minmax(140px,2fr) 70px 70px 70px 90px 80px 100px 80px",minWidth:680,gap:8,padding:"8px 16px",fontSize:9,fontWeight:700,color:T.textMuted,textTransform:"uppercase",letterSpacing:0.8,borderBottom:"1px solid "+T.border}}>`);
a(`            {["Agent","Leads","New/wk","Hot","Closed","Pipeline","Conv%","Action"].map((h,i)=>(`);
a(`              <div key={i} style={{textAlign:i>0?"center":"left"}}>{h}</div>`);
a(`            ))}`);
a(`          </div>`);
a(`          {teamMembersLoading&&<div style={{padding:"40px",textAlign:"center",color:T.textMuted,fontSize:12}}>Loading team...</div>}`);
a(`          {!teamMembersLoading&&agentStats.length===0&&(`);
a(`            <div style={{padding:"40px",textAlign:"center"}}>`);
a(`              <div style={{fontSize:13,color:T.textMuted,marginBottom:8}}>No agents yet</div>`);
a(`              <button type="button" onClick={()=>setShowCreate(true)} style={{padding:"8px 20px",borderRadius:7,border:"none",background:"linear-gradient(135deg,#D4A843,#B8902E)",color:"#0A0E1A",fontSize:12,fontWeight:700,cursor:"pointer"}}>+ Add First Agent</button>`);
a(`            </div>`);
a(`          )}`);
a(`          {agentStats.map((agent,i)=>{`);
a(`            const isSuspended = agent.status==="suspended";`);
a(`            const rankColor = i===0?"#F59E0B":i===1?"#94A3B8":i===2?"#B45309":T.textMuted;`);
a(`            return (`);
a(`              <div key={agent.uid||i} style={{display:"grid",gridTemplateColumns:"minmax(140px,2fr) 70px 70px 70px 90px 80px 100px 80px",gap:8,padding:"12px 16px",alignItems:"center",borderBottom:i<agentStats.length-1?"1px solid "+T.border+"40":"none",background:isSuspended?"rgba(239,68,68,0.03)":i%2===0?"transparent":"rgba(255,255,255,0.01)",opacity:isSuspended?0.6:1}}>`);
a(`                {/* Agent info */}`);
a(`                <div style={{display:"flex",alignItems:"center",gap:8,minWidth:0}}>`);
a(`                  <div style={{width:28,height:28,borderRadius:"50%",background:isSuspended?"rgba(239,68,68,0.12)":"rgba(212,168,67,0.12)",display:"flex",alignItems:"center",justifyContent:"center",fontSize:10,fontWeight:700,color:isSuspended?"#EF4444":T.gold,flexShrink:0}}>`);
a(`                    {(agent.name||agent.email||"?").slice(0,2).toUpperCase()}`);
a(`                  </div>`);
a(`                  <div style={{minWidth:0}}>`);
a(`                    <div style={{fontSize:12,fontWeight:600,color:isSuspended?T.textMuted:T.white,overflow:"hidden",textOverflow:"ellipsis",whiteSpace:"nowrap"}}>`);
a(`                      {agent.name||agent.email?.split("@")[0]||"Agent"}`);
a(`                      {isSuspended&&<span style={{marginLeft:6,fontSize:9,color:"#EF4444",fontWeight:700}}>SUSPENDED</span>}`);
a(`                    </div>`);
a(`                    <div style={{fontSize:10,color:T.textMuted,overflow:"hidden",textOverflow:"ellipsis",whiteSpace:"nowrap"}}>{agent.email||""}</div>`);
a(`                  </div>`);
a(`                </div>`);
a(`                {/* Leads */}`);
a(`                <div style={{fontSize:12,fontWeight:600,color:T.white,textAlign:"center"}}>{agent.aLeads.length}</div>`);
a(`                {/* New this week */}`);
a(`                <div style={{fontSize:12,color:T.gold,fontWeight:600,textAlign:"center"}}>{agent.newWeek}</div>`);
a(`                {/* Hot */}`);
a(`                <div style={{fontSize:12,color:"#10B981",fontWeight:600,textAlign:"center"}}>`);
a(`                  {agent.aLeads.filter(l=>l.status==="Hot Case").length}`);
a(`                </div>`);
a(`                {/* Closed */}`);
a(`                <div style={{textAlign:"center"}}>`);
a(`                  <span style={{fontSize:11,fontWeight:700,padding:"2px 8px",borderRadius:5,background:agent.closed.length>0?"rgba(16,185,129,0.1)":"rgba(255,255,255,0.04)",color:agent.closed.length>0?"#10B981":T.textMuted}}>`);
a(`                    {agent.closed.length}`);
a(`                  </span>`);
a(`                </div>`);
a(`                {/* Pipeline */}`);
a(`                <div style={{fontSize:11,fontWeight:600,color:agent.totalVal>0?T.gold:T.textMuted,textAlign:"center"}}>{fmtB(agent.totalVal)}</div>`);
a(`                {/* Conv % */}`);
a(`                <div style={{textAlign:"center"}}>`);
a(`                  <span style={{fontSize:11,fontWeight:700,color:parseFloat(agent.conv)>5?"#10B981":parseFloat(agent.conv)>0?T.gold:T.textMuted}}>`);
a(`                    {agent.conv}%`);
a(`                  </span>`);
a(`                </div>`);
a(`                {/* Action */}`);
a(`                <div style={{display:"flex",gap:5,justifyContent:"center"}}>`);
a(`                  {agent.phone&&<a href={"https://wa.me/"+cleanPhone(agent.phone||"")} target="_blank" rel="noopener noreferrer" style={{padding:"3px 7px",borderRadius:5,background:"rgba(37,211,102,0.1)",border:"1px solid rgba(37,211,102,0.2)",color:"#25D366",textDecoration:"none",fontSize:9,fontWeight:700}}>WA</a>}`);
a(`                  {!isSuspended`);
a(`                    ?<button type="button" onClick={()=>setShowDeact(agent)} style={{padding:"3px 9px",borderRadius:5,background:"rgba(239,68,68,0.08)",border:"1px solid rgba(239,68,68,0.2)",color:"#EF4444",fontSize:9,fontWeight:700,cursor:"pointer",fontFamily:"'Outfit',sans-serif"}}>Deactivate</button>`);
a(`                    :<button type="button" onClick={()=>reactivateAgent(agent)} style={{padding:"3px 9px",borderRadius:5,background:"rgba(16,185,129,0.08)",border:"1px solid rgba(16,185,129,0.2)",color:"#10B981",fontSize:9,fontWeight:700,cursor:"pointer",fontFamily:"'Outfit',sans-serif"}}>Reactivate</button>`);
a(`                  }`);
a(`                </div>`);
a(`              </div>`);
a(`            );`);
a(`          })}`);
a(`        </div>`);
a(`      </div>`);
a(``);
a(`      {/* ══ BOTTOM ROW ════════════════════════════════════════════ */}`);
a(`      <div style={{display:"grid",gridTemplateColumns:"1fr 1fr",gap:16}}>`);
a(``);
a(`        {/* Source ROI */}`);
a(`        <div style={{background:"rgba(255,255,255,0.02)",border:"1px solid "+T.border,borderRadius:14,overflow:"hidden"}}>`);
a(`          <div style={{padding:"14px 18px",borderBottom:"1px solid "+T.border,display:"flex",alignItems:"center",gap:10}}>`);
a(`            <div style={{fontSize:13,fontWeight:700,color:T.white}}>Source ROI</div>`);
a(`            <div style={{marginLeft:"auto",fontSize:10,color:T.textMuted}}>By conversion rate</div>`);
a(`          </div>`);
a(`          <div>`);
a(`            <div style={{display:"grid",gridTemplateColumns:"1fr 60px 60px 70px",gap:8,padding:"8px 16px",fontSize:9,fontWeight:700,color:T.textMuted,textTransform:"uppercase",letterSpacing:0.8,borderBottom:"1px solid "+T.border}}>`);
a(`              <div>Source</div><div style={{textAlign:"center"}}>Leads</div><div style={{textAlign:"center"}}>Closed</div><div style={{textAlign:"right"}}>Conv%</div>`);
a(`            </div>`);
a(`            {srcStats.length===0&&<div style={{padding:"24px 16px",textAlign:"center",fontSize:12,color:T.textMuted}}>No source data yet</div>}`);
a(`            {srcStats.map(({src,leads,closed,conv,color},i)=>(`);
a(`              <div key={src} style={{display:"grid",gridTemplateColumns:"1fr 60px 60px 70px",gap:8,padding:"10px 16px",alignItems:"center",borderBottom:i<srcStats.length-1?"1px solid "+T.border+"40":"none"}}>`);
a(`                <div style={{display:"flex",alignItems:"center",gap:8}}><div style={{width:8,height:8,borderRadius:"50%",background:color,flexShrink:0}}/><span style={{fontSize:11,fontWeight:600,color:T.white,overflow:"hidden",textOverflow:"ellipsis",whiteSpace:"nowrap"}}>{src}</span></div>`);
a(`                <div style={{fontSize:11,fontWeight:600,color:T.white,textAlign:"center"}}>{leads}</div>`);
a(`                <div style={{fontSize:11,fontWeight:600,color:closed>0?"#10B981":T.textMuted,textAlign:"center"}}>{closed}</div>`);
a(`                <div style={{textAlign:"right"}}><span style={{fontSize:11,fontWeight:700,color:parseFloat(conv)>5?"#10B981":parseFloat(conv)>0?T.gold:T.textMuted}}>{conv}%</span></div>`);
a(`              </div>`);
a(`            ))}`);
a(`          </div>`);
a(`        </div>`);
a(``);
a(`        {/* Overdue Follow-ups */}`);
a(`        <div style={{background:"rgba(255,255,255,0.02)",border:"1px solid "+T.border,borderRadius:14,overflow:"hidden"}}>`);
a(`          <div style={{padding:"14px 18px",borderBottom:"1px solid "+T.border,display:"flex",alignItems:"center",gap:10}}>`);
a(`            <div style={{fontSize:13,fontWeight:700,color:T.white}}>Overdue Follow-ups</div>`);
a(`            {teamOverdue.length>0&&<span style={{marginLeft:"auto",fontSize:10,fontWeight:700,padding:"2px 8px",borderRadius:10,background:"rgba(239,68,68,0.12)",color:"#EF4444"}}>{teamOverdue.length} overdue</span>}`);
a(`          </div>`);
a(`          <div style={{maxHeight:320,overflowY:"auto"}}>`);
a(`            {teamOverdue.length===0&&(`);
a(`              <div style={{padding:"40px 20px",textAlign:"center"}}>`);
a(`                <div style={{fontSize:13,fontWeight:600,color:"#10B981"}}>All caught up</div>`);
a(`                <div style={{fontSize:11,color:T.textMuted,marginTop:4}}>No overdue follow-ups</div>`);
a(`              </div>`);
a(`            )}`);
a(`            {teamOverdue.map((l,i)=>{`);
a(`              const days = l.updatedAt?Math.floor((Date.now()-new Date(l.updatedAt))/86400000):"?";`);
a(`              const agent = teamMembers.find(u=>u.uid===l.assignedTo);`);
a(`              return (`);
a(`                <div key={l.id||i} style={{display:"flex",alignItems:"center",justifyContent:"space-between",padding:"11px 18px",borderBottom:i<teamOverdue.length-1?"1px solid "+T.border:""}}>`);
a(`                  <div style={{flex:1,minWidth:0}}>`);
a(`                    <div style={{fontSize:12,fontWeight:600,color:T.white,overflow:"hidden",textOverflow:"ellipsis",whiteSpace:"nowrap"}}>{l.name||l.phone||"Unnamed"}</div>`);
a(`                    <div style={{fontSize:10,color:T.textMuted,marginTop:2}}>{agent?(agent.name||agent.email?.split("@")[0]):"Unassigned"}{l.source?" · "+l.source:""}</div>`);
a(`                  </div>`);
a(`                  <div style={{display:"flex",alignItems:"center",gap:8,flexShrink:0}}>`);
a(`                    <span style={{fontSize:10,fontWeight:700,padding:"3px 8px",borderRadius:5,background:"rgba(239,68,68,0.1)",color:"#EF4444"}}>{days}d ago</span>`);
a(`                    {l.phone&&<a href={"https://wa.me/"+cleanPhone(l.phone||"")} target="_blank" rel="noopener noreferrer" style={{display:"flex",alignItems:"center",justifyContent:"center",width:28,height:28,borderRadius:6,border:"1px solid rgba(37,211,102,0.3)",background:"rgba(37,211,102,0.08)",color:"#25D366",textDecoration:"none"}}>`);
a(`                      <svg width="13" height="13" viewBox="0 0 24 24" fill="currentColor"><path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 0 1-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 0 1-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 0 1 2.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0 0 12.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 0 0 5.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 0 0-3.48-8.413z"/></svg>`);
a(`                    </a>}`);
a(`                  </div>`);
a(`                </div>`);
a(`              );`);
a(`            })}`);
a(`          </div>`);
a(`        </div>`);
a(`      </div>`);
a(``);
a(`      {/* ══ CREATE AGENT MODAL ════════════════════════════════════ */}`);
a(`      {showCreate&&(`);
a(`        <div style={{position:"fixed",inset:0,background:"rgba(4,9,15,0.9)",zIndex:2000,display:"flex",alignItems:"center",justifyContent:"center",padding:20}} onClick={e=>{if(e.target===e.currentTarget)setShowCreate(false);}}>`);
a(`          <div style={{background:"#0D1117",borderRadius:14,border:"1px solid rgba(212,168,67,0.3)",width:"100%",maxWidth:440,padding:24}} onClick={e=>e.stopPropagation()}>`);
a(`            <div style={{display:"flex",justifyContent:"space-between",alignItems:"center",marginBottom:20}}>`);
a(`              <div>`);
a(`                <div style={{fontFamily:"'Fraunces',serif",fontSize:16,fontWeight:900,color:T.white}}>Create Agent Account</div>`);
a(`                <div style={{fontSize:11,color:T.textMuted,marginTop:3}}>Agent will receive login credentials by email</div>`);
a(`              </div>`);
a(`              <button type="button" onClick={()=>setShowCreate(false)} style={{background:"rgba(255,255,255,0.06)",border:"1px solid "+T.border,borderRadius:7,color:T.textMuted,width:28,height:28,cursor:"pointer",display:"flex",alignItems:"center",justifyContent:"center",fontSize:16}}>×</button>`);
a(`            </div>`);
a(`            <div style={{display:"flex",flexDirection:"column",gap:12}}>`);
a(`              {[`);
a(`                {k:"name",    label:"Full Name *",      placeholder:"Ahmed Al-Mansouri",   type:"text"},`);
a(`                {k:"email",   label:"Work Email *",     placeholder:"ahmed@agency.ae",     type:"email"},`);
a(`                {k:"phone",   label:"Phone Number",     placeholder:"+971 50 XXX XXXX",    type:"tel"},`);
a(`                {k:"password",label:"Temporary Password *", placeholder:"Min 8 chars, 1 uppercase, 1 number", type:"password"},`);
a(`              ].map(({k,label,placeholder,type})=>(`);
a(`                <div key={k}>`);
a(`                  <div style={{fontSize:10,color:T.textMuted,marginBottom:4,fontWeight:600,textTransform:"uppercase",letterSpacing:0.5}}>{label}</div>`);
a(`                  <input type={type} value={form[k]||""} onChange={e=>F(k,e.target.value)} placeholder={placeholder}`);
a(`                    style={{...inp}} />`);
a(`                </div>`);
a(`              ))}`);
a(`              <div style={{padding:"10px 12px",background:"rgba(212,168,67,0.06)",border:"1px solid rgba(212,168,67,0.2)",borderRadius:8,fontSize:11,color:T.textMuted,lineHeight:1.6}}>`);
a(`                The agent will log in with these credentials. They can change their password anytime from settings.`);
a(`              </div>`);
a(`              <button type="button" onClick={createAgent} disabled={creating||!form.name||!form.email||!form.password}`);
a(`                style={{width:"100%",padding:"11px",borderRadius:8,border:"none",background:(!form.name||!form.email||!form.password||creating)?"rgba(212,168,67,0.3)":"linear-gradient(135deg,#D4A843,#B8902E)",color:"#0A0E1A",fontSize:13,fontWeight:700,cursor:(!form.name||!form.email||!form.password)?"not-allowed":"pointer",fontFamily:"'Outfit',sans-serif"}}>`);
a(`                {creating?"Creating account...":"Create Agent Account"}`);
a(`              </button>`);
a(`            </div>`);
a(`          </div>`);
a(`        </div>`);
a(`      )}`);
a(``);
a(`      {/* ══ DEACTIVATE CONFIRM MODAL ═════════════════════════════ */}`);
a(`      {showDeact&&(`);
a(`        <div style={{position:"fixed",inset:0,background:"rgba(4,9,15,0.9)",zIndex:2000,display:"flex",alignItems:"center",justifyContent:"center",padding:20}} onClick={e=>{if(e.target===e.currentTarget)setShowDeact(null);}}>`);
a(`          <div style={{background:"#0D1117",borderRadius:14,border:"1px solid rgba(239,68,68,0.3)",width:"100%",maxWidth:400,padding:24}} onClick={e=>e.stopPropagation()}>`);
a(`            <div style={{fontSize:16,fontWeight:700,color:T.white,marginBottom:6,fontFamily:"'Fraunces',serif"}}>Deactivate Agent</div>`);
a(`            <div style={{fontSize:13,color:T.textMuted,marginBottom:16}}>Are you sure you want to deactivate <strong style={{color:T.white}}>{showDeact.name||showDeact.email}</strong>?</div>`);
a(`            <div style={{padding:"12px 14px",background:"rgba(239,68,68,0.06)",border:"1px solid rgba(239,68,68,0.2)",borderRadius:9,marginBottom:20}}>`);
a(`              <div style={{fontSize:12,color:T.white,fontWeight:600,marginBottom:6}}>What happens:</div>`);
a(`              <div style={{fontSize:11,color:T.textMuted,lineHeight:1.7}}>`);
a(`                • Account will be suspended (not deleted)<br/>`);
a(`                • All assigned leads return to your unassigned pool<br/>`);
a(`                • Each lead will show tag: "Previously: {showDeact.name||"Agent"}"<br/>`);
a(`                • Full activity history is preserved<br/>`);
a(`                • You can reactivate this agent at any time`);
a(`              </div>`);
a(`            </div>`);
a(`            <div style={{display:"flex",gap:10}}>`);
a(`              <button type="button" onClick={()=>setShowDeact(null)} style={{flex:1,padding:"10px",borderRadius:8,border:"1px solid "+T.border,background:"transparent",color:T.textMuted,fontSize:12,cursor:"pointer",fontFamily:"'Outfit',sans-serif"}}>Cancel</button>`);
a(`              <button type="button" onClick={()=>deactivateAgent(showDeact)} disabled={deacting}`);
a(`                style={{flex:1,padding:"10px",borderRadius:8,border:"none",background:deacting?"rgba(239,68,68,0.3)":"rgba(239,68,68,0.85)",color:"#fff",fontSize:12,fontWeight:700,cursor:deacting?"not-allowed":"pointer",fontFamily:"'Outfit',sans-serif"}}>`);
a(`                {deacting?"Deactivating...":"Deactivate Agent"}`);
a(`              </button>`);
a(`            </div>`);
a(`          </div>`);
a(`        </div>`);
a(`      )}`);
a(``);
a(`      {/* ══ TOAST ════════════════════════════════════════════════ */}`);
a(`      {toast&&<div style={{position:"fixed",bottom:24,right:24,padding:"11px 18px",background:toast.type==="error"?"rgba(239,68,68,0.15)":"rgba(16,185,129,0.15)",border:"1px solid "+(toast.type==="error"?"#EF4444":"#10B981"),borderRadius:9,color:toast.type==="error"?"#EF4444":"#10B981",fontSize:12,fontWeight:600,zIndex:9999,boxShadow:"0 8px 32px rgba(0,0,0,0.4)"}}>{toast.msg}</div>}`);
a(``);
a(`    </div>`);
a(`  );`);
a(`}`);

fs.writeFileSync("src/tabs/TeamTab.jsx", lines.join("\n"), "utf8");
console.log("Done. Lines:", lines.length);