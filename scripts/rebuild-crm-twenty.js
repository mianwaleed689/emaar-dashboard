const fs = require("fs");
const lines = [];
const add = (s) => lines.push(s);

add(`/* eslint-disable */`);
add(`/*`);
add(`  DXB ANALYTICS — MY LEADS CRM`);
add(`  Built from scratch — Twenty CRM UX patterns in DXB dark theme`);
add(`  April 2026`);
add(`*/`);
add(``);
add(`import React, { useState, useMemo, useCallback, useRef } from "react";`);
add(`import { T } from "../data";`);
add(`import { GOLDEN_VISA_THRESHOLD } from "../utils/constants";`);
add(`import Papa from "papaparse";`);
add(``);
add(`// ─── PIPELINE STAGES (your exact status names from screenshots) ─────────────`);
add(`const PIPELINE_STAGES = [`);
add(`  { key: "Hot Case",        color: "#EF4444", desc: "High intent leads" },`);
add(`  { key: "Low Budget",      color: "#8B5CF6", desc: "Budget below threshold" },`);
add(`  { key: "No Answer",       color: "#F59E0B", desc: "Cannot be reached" },`);
add(`  { key: "New Lead",        color: "#3B82F6", desc: "Just came in" },`);
add(`  { key: "Non Potential",   color: "#6B7280", desc: "Not qualified" },`);
add(`  { key: "Potential",       color: "#10B981", desc: "Qualified and interested" },`);
add(`  { key: "Resale/buy/Rent", color: "#14B8A6", desc: "Secondary market" },`);
add(`  { key: "Whats app",       color: "#25D366", desc: "WhatsApp only contact" },`);
add(`  { key: "Closed Deal",     color: "#10B981", desc: "Successfully closed" },`);
add(`  { key: "Closed Outside",  color: "#EF4444", desc: "Closed with competitor" },`);
add(`  { key: "EOI",             color: "#D4A843", desc: "Expression of Interest" },`);
add(`];`);
add(``);
add(`// ─── SMART VIEWS (top tabs like Twenty CRM) ─────────────────────────────────`);
add(`const SMART_VIEWS = [`);
add(`  { key: "all",          label: "All" },`);
add(`  { key: "today",        label: "Today" },`);
add(`  { key: "my_leads",     label: "My Leads" },`);
add(`  { key: "uncontacted",  label: "Uncontacted" },`);
add(`  { key: "hot",          label: "Hot" },`);
add(`  { key: "stale",        label: "Stale" },`);
add(`  { key: "overdue",      label: "Overdue" },`);
add(`  { key: "unassigned",   label: "Unassigned" },`);
add(`  { key: "golden_visa",  label: "Golden Visa" },`);
add(`];`);
add(``);
add(`const SERVICE_TYPES = ["Buyer", "Seller", "Tenant"];`);
add(`const REQUEST_TYPES = ["Off-Plan", "Ready", "Resale", "Rental", "Investment", "Commercial"];`);
add(`const LEAD_SOURCES  = ["Property Finder","Bayut","Dubizzle","Meta/Facebook","Instagram","WhatsApp","Google Ads","Referral","Website","Cold Call","TikTok","LinkedIn","Email Campaign","Walk In","Manual"];`);
add(`const CHANNELS      = ["WhatsApp","Phone Call","Email","In Person","Video Call","Social DM"];`);
add(`const NATS          = ["Indian","British","Russian","Chinese","French","Pakistani","Saudi","Egyptian","German","American","Italian","Canadian","Australian","Japanese","Korean","Emirati","Filipino","Lebanese","Jordanian","Other"];`);
add(``);
add(`const SRC_COLOR = {`);
add(`  "Property Finder":"#00C08B","Bayut":"#FF6B35","Dubizzle":"#E8003D",`);
add(`  "Meta/Facebook":"#1877F2","Instagram":"#E1306C","WhatsApp":"#25D366",`);
add(`  "Google Ads":"#4285F4","Referral":"#8B5CF6","Website":"#14B8A6",`);
add(`  "Cold Call":"#F59E0B","TikTok":"#010101","LinkedIn":"#0A66C2",`);
add(`  "Email Campaign":"#EA4335","Walk In":"#6B7280","Manual":"#94A3B8",`);
add(`};`);
add(``);
add(`const BEST_TIME = {`);
add(`  "Property Finder":"Evening 6-9pm","Bayut":"Evening 6-9pm","Dubizzle":"Evening 6-8pm",`);
add(`  "WhatsApp":"Morning 9-11am","Instagram":"Afternoon 2-5pm","Meta/Facebook":"Afternoon 2-5pm",`);
add(`  "Google Ads":"Morning 10am-12pm","Referral":"Anytime","Cold Call":"Morning 9-11am",`);
add(`  "Website":"Morning 9-11am","TikTok":"Evening 7-10pm","Manual":"Anytime",`);
add(`};`);
add(``);
add(`const WA_TEMPLATES = [`);
add(`  { label:"First Contact",   body:"Hello {name}, I came across your property enquiry. I am {agent} from The Address Holding. We have excellent options matching your requirements in Dubai. When would be a good time to connect?" },`);
add(`  { label:"Follow Up",       body:"Hello {name}, following up on your property search. We have new listings matching your budget. Would you like to schedule a viewing this week?" },`);
add(`  { label:"Viewing Confirm", body:"Hello {name}, confirming your property viewing. Please let me know if you need to reschedule. Looking forward to meeting you!" },`);
add(`  { label:"Offer Made",      body:"Hello {name}, great news! We have submitted your offer. I will update you as soon as we hear back." },`);
add(`  { label:"Golden Visa",     body:"Hello {name}, based on your budget you qualify for UAE Golden Visa (AED 2M+ investment). This gives you 10-year residency. Shall I send you more details?" },`);
add(`  { label:"Market Update",   body:"Hello {name}, quick update — prices in your preferred area have moved. I have great options within your budget. Shall we connect?" },`);
add(`  { label:"EOI Follow Up",   body:"Hello {name}, thank you for your Expression of Interest. We have reviewed your request and have excellent options to present. When can we connect?" },`);
add(`];`);
add(``);
add(`// ─── HELPERS ─────────────────────────────────────────────────────────────────`);
add(`const clnPhone  = (p) => String(p||"").replace(/[^0-9]/g,"");`);
add(`const maskPhone = (p) => { if(!p) return "—"; const c=clnPhone(p); return c.slice(0,3)+"••••••"+c.slice(-2); };`);
add(`const fmtB      = (b) => { const n=parseFloat(b||0); if(!n) return "—"; return n>=1e6?"AED "+(n/1e6).toFixed(1)+"M":"AED "+n.toLocaleString(); };`);
add(`const fmtD      = (d) => { if(!d) return "—"; return new Date(d).toLocaleDateString("en-AE",{day:"2-digit",month:"short",year:"2-digit"}); };`);
add(`const daysAgo   = (d) => !d ? 999 : Math.floor((Date.now()-new Date(d).getTime())/86400000);`);
add(`const escCSV    = (v) => '"'+String(v==null?"":v).replace(/"/g,'""')+'"';`);
add(`const GV_THRESHOLD = typeof GOLDEN_VISA_THRESHOLD!=="undefined" ? GOLDEN_VISA_THRESHOLD : 2000000;`);
add(``);
add(`// ─── AI SCORE ────────────────────────────────────────────────────────────────`);
add(`function aiScore(l) {`);
add(`  let s=0;`);
add(`  if(l.phone&&l.email) s+=25; else if(l.phone||l.email) s+=10;`);
add(`  const b=parseFloat(l.budget||0);`);
add(`  if(b>=5000000) s+=20; else if(b>=GV_THRESHOLD) s+=15; else if(b>=1000000) s+=10;`);
add(`  const d=daysAgo(l.createdAt);`);
add(`  if(d<1) s+=20; else if(d<3) s+=15; else if(d<7) s+=10;`);
add(`  const n=(l.notes_log||[]).length; if(n>=3) s+=10; else if(n>=1) s+=5;`);
add(`  if(l.timeline==="Immediate") s+=15; else if(l.timeline==="1-3 months") s+=10;`);
add(`  if(l.nationality) s+=5;`);
add(`  const score=Math.min(100,s);`);
add(`  return { score, color:score>=70?"#10B981":score>=40?"#D4A843":"#EF4444", label:score>=70?"Hot":score>=40?"Warm":"Cold" };`);
add(`}`);
add(``);
add(`// ─── SMALL COMPONENTS ────────────────────────────────────────────────────────`);
add(`const StatusDot = ({ status }) => {`);
add(`  const stage = PIPELINE_STAGES.find(s=>s.key===status);`);
add(`  const color = stage?.color || "#94A3B8";`);
add(`  return (`);
add(`    <span style={{display:"inline-flex",alignItems:"center",gap:5,fontSize:11,color,fontWeight:600}}>`);
add(`      <span style={{width:7,height:7,borderRadius:"50%",background:color,display:"inline-block",flexShrink:0}} />`);
add(`      {status||"New Lead"}`);
add(`    </span>`);
add(`  );`);
add(`};`);
add(``);
add(`const CopyIcon = ({ text }) => (`);
add(`  <button type="button" onClick={e=>{e.stopPropagation();navigator.clipboard?.writeText(text||"");}} title="Copy"`);
add(`    style={{background:"none",border:"none",cursor:"pointer",padding:"2px 4px",color:T.textMuted,opacity:0.6,display:"inline-flex",alignItems:"center"}}>`);
add(`    <svg width="11" height="11" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2"><rect x="9" y="9" width="13" height="13" rx="2"/><path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"/></svg>`);
add(`  </button>`);
add(`);`);
add(``);
add(`const EyeReveal = ({ value, masked }) => {`);
add(`  const [show, setShow] = useState(false);`);
add(`  return (`);
add(`    <span style={{display:"inline-flex",alignItems:"center",gap:4}}>`);
add(`      <span style={{fontSize:11,color:T.textSecondary,fontFamily:"monospace"}}>{show ? value : masked}</span>`);
add(`      <button type="button" onClick={e=>{e.stopPropagation();setShow(v=>!v);}} style={{background:"none",border:"none",cursor:"pointer",color:T.textMuted,padding:"1px 3px",display:"inline-flex"}}>`);
add(`        <svg width="11" height="11" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2">{show?<><path d="M17.94 17.94A10.07 10.07 0 0 1 12 20c-7 0-11-8-11-8a18.45 18.45 0 0 1 5.06-5.94"/><path d="M9.9 4.24A9.12 9.12 0 0 1 12 4c7 0 11 8 11 8a18.5 18.5 0 0 1-2.16 3.19"/><line x1="1" y1="1" x2="23" y2="23"/></>:<><path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z"/><circle cx="12" cy="12" r="3"/></>}</svg>`);
add(`      </button>`);
add(`    </span>`);
add(`  );`);
add(`};`);
add(``);
add(`// ─── MAIN ────────────────────────────────────────────────────────────────────`);
add(`function MyLeadsTab({`);
add(`  myLeads, liveLeads, orgRole, userRole, orgId, orgName, listings,`);
add(`  leadSearch, setLeadSearch,`);
add(`  leadStatusFilter, setLeadStatusFilter,`);
add(`  leadTypeFilter, setLeadTypeFilter,`);
add(`  leadSourceFilter, setLeadSourceFilter,`);
add(`  leadBudgetFilter, setLeadBudgetFilter,`);
add(`  leadNatFilter, setLeadNatFilter,`);
add(`  leadTagFilter, setLeadTagFilter,`);
add(`  leadSortBy, setLeadSortBy,`);
add(`  selectedLead, setSelectedLead,`);
add(`  leadDrawerTab, setLeadDrawerTab,`);
add(`  leadShowAdd, setLeadShowAdd,`);
add(`  leadAddName, setLeadAddName,`);
add(`  leadAddPhone, setLeadAddPhone,`);
add(`  leadAddEmail, setLeadAddEmail,`);
add(`  leadAddNat, setLeadAddNat,`);
add(`  leadAddLang, setLeadAddLang,`);
add(`  leadAddSource, setLeadAddSource,`);
add(`  leadAddType, setLeadAddType,`);
add(`  leadAddBudget, setLeadAddBudget,`);
add(`  leadAddBedrooms, setLeadAddBedrooms,`);
add(`  leadAddPurpose, setLeadAddPurpose,`);
add(`  leadAddTimeline, setLeadAddTimeline,`);
add(`  leadAddStatus, setLeadAddStatus,`);
add(`  leadAddComm, setLeadAddComm,`);
add(`  leadAddRef, setLeadAddRef,`);
add(`  leadAddFollowUp, setLeadAddFollowUp,`);
add(`  leadAddSaving, setLeadAddSaving,`);
add(`  noteText, setNoteText,`);
add(`  noteType, setNoteType,`);
add(`  noteLoading, setNoteLoading,`);
add(`  taskText, setTaskText,`);
add(`  taskDue, setTaskDue,`);
add(`  showMLAnalytics, setShowMLAnalytics,`);
add(`  showMLTemplates, setShowMLTemplates,`);
add(`  showQuickCapture, setShowQuickCapture,`);
add(`  handleTabChange,`);
add(`}) {`);
add(``);
add(`  const [smartView, setSmartView]     = useState("all");`);
add(`  const [activeStage, setActiveStage] = useState("all");`);
add(`  const [view, setView]               = useState("table");`);
add(`  const [showAdd, setShowAdd]         = useState(false);`);
add(`  const [showWA, setShowWA]           = useState(false);`);
add(`  const [drawerTab, setDrawerTab]     = useState("profile");`);
add(`  const [revealPhone, setRevealPhone] = useState({});`);
add(`  const [aiSearch, setAiSearch]       = useState("");`);
add(`  const [filterSource, setFilterSource] = useState("all");`);
add(`  const [filterBudget, setFilterBudget] = useState("all");`);
add(`  const [filterService, setFilterService] = useState("all");`);
add(``);
add(`  // form state`);
add(`  const emptyForm = { name:"",phone:"",email:"",budget:"",requestType:"",serviceType:"Buyer",project:"",source:"Manual",channel:"WhatsApp",campaign:"",status:"New Lead",nationality:"",language:"",timeline:"",community:"",bedrooms:"",comment:"",agentName:"" };`);
add(`  const [form, setForm] = useState(emptyForm);`);
add(`  const [saving, setSaving] = useState(false);`);
add(`  const [openSection, setOpenSection] = useState({ contact:true, request:true, agent:false, extra:false });`);
add(``);
add(`  // role`);
add(`  const isAgent      = orgRole==="agent";`);
add(`  const isManager    = orgRole==="manager";`);
add(`  const isSuperAdmin = userRole==="admin"||userRole==="superAdmin";`);
add(`  const canSee       = isAgent||isManager||isSuperAdmin;`);
add(`  const allLeads     = isSuperAdmin ? (liveLeads||[]) : (myLeads||[]);`);
add(``);
add(`  // duplicate check`);
add(`  const isDupe = useCallback((p) => {`);
add(`    if(!p) return false;`);
add(`    const c=clnPhone(p);`);
add(`    return allLeads.some(l=>clnPhone(l.phone)===c);`);
add(`  },[allLeads]);`);
add(``);
add(`  // stage counts`);
add(`  const stageCounts = useMemo(() => {`);
add(`    const c={};`);
add(`    PIPELINE_STAGES.forEach(s=>{ c[s.key]=allLeads.filter(l=>(l.status||"New Lead")===s.key).length; });`);
add(`    return c;`);
add(`  },[allLeads]);`);
add(``);
add(`  // smart view filter`);
add(`  const smartFiltered = useMemo(() => {`);
add(`    let a = allLeads;`);
add(`    if(smartView==="today")       a=a.filter(l=>daysAgo(l.createdAt)<1);`);
add(`    if(smartView==="hot")         a=a.filter(l=>aiScore(l).score>=70);`);
add(`    if(smartView==="stale")       a=a.filter(l=>!["Closed Deal","Closed Outside"].includes(l.status)&&daysAgo(l.updatedAt||l.createdAt)>7);`);
add(`    if(smartView==="overdue")     a=a.filter(l=>l.followUpDate&&new Date(l.followUpDate)<new Date()&&!["Closed Deal","Closed Outside"].includes(l.status));`);
add(`    if(smartView==="uncontacted") a=a.filter(l=>!(l.notes_log||[]).some(n=>["Call","WhatsApp","Email"].includes(n.type)));`);
add(`    if(smartView==="unassigned")  a=a.filter(l=>!l.agentId&&!l.assignedTo);`);
add(`    if(smartView==="golden_visa") a=a.filter(l=>parseFloat(l.budget||0)>=GV_THRESHOLD);`);
add(`    return a;`);
add(`  },[allLeads,smartView]);`);
add(``);
add(`  // main filter`);
add(`  const filtered = useMemo(() => {`);
add(`    let a = smartFiltered;`);
add(`    if(activeStage!=="all") a=a.filter(l=>(l.status||"New Lead")===activeStage);`);
add(`    if(filterService!=="all") a=a.filter(l=>(l.serviceType||l.type||"")===filterService);`);
add(`    if(filterSource!=="all") a=a.filter(l=>l.source===filterSource);`);
add(`    if(filterBudget!=="all") {`);
add(`      const b=l=>parseFloat(l.budget||0);`);
add(`      if(filterBudget==="under1m")  a=a.filter(l=>b(l)<1e6);`);
add(`      if(filterBudget==="1to3m")    a=a.filter(l=>b(l)>=1e6&&b(l)<3e6);`);
add(`      if(filterBudget==="3to5m")    a=a.filter(l=>b(l)>=3e6&&b(l)<5e6);`);
add(`      if(filterBudget==="5to10m")   a=a.filter(l=>b(l)>=5e6&&b(l)<1e7);`);
add(`      if(filterBudget==="above10m") a=a.filter(l=>b(l)>=1e7);`);
add(`    }`);
add(`    if(aiSearch.trim()) {`);
add(`      const q=aiSearch.trim().toLowerCase();`);
add(`      a=a.filter(l=>(l.name||"").toLowerCase().includes(q)||(l.phone||"").includes(q)||(l.email||"").toLowerCase().includes(q)||(l.community||"").toLowerCase().includes(q)||(l.source||"").toLowerCase().includes(q));`);
add(`    }`);
add(`    if(leadSortBy==="score")  return [...a].sort((a,b)=>aiScore(b).score-aiScore(a).score);`);
add(`    if(leadSortBy==="budget") return [...a].sort((a,b)=>parseFloat(b.budget||0)-parseFloat(a.budget||0));`);
add(`    if(leadSortBy==="name")   return [...a].sort((a,b)=>(a.name||"").localeCompare(b.name||""));`);
add(`    return [...a].sort((a,b)=>new Date(b.createdAt||0)-new Date(a.createdAt||0));`);
add(`  },[smartFiltered,activeStage,filterService,filterSource,filterBudget,aiSearch,leadSortBy]);`);
add(``);
add(`  // kpis`);
add(`  const kpiTotal    = allLeads.length;`);
add(`  const kpiToday    = allLeads.filter(l=>daysAgo(l.createdAt)<1).length;`);
add(`  const kpiHot      = allLeads.filter(l=>aiScore(l).score>=70&&!["Closed Deal","Closed Outside"].includes(l.status)).length;`);
add(`  const kpiClosed   = allLeads.filter(l=>l.status==="Closed Deal").length;`);
add(`  const kpiStale    = allLeads.filter(l=>!["Closed Deal","Closed Outside","Non Potential"].includes(l.status)&&daysAgo(l.updatedAt||l.createdAt)>7).length;`);
add(`  const kpiPipeline = allLeads.reduce((s,l)=>s+parseFloat(l.budget||0),0);`);
add(``);
add(`  // source analytics`);
add(`  const srcStats = useMemo(()=>{`);
add(`    const m={};`);
add(`    allLeads.forEach(l=>{const s=l.source||"Manual";if(!m[s])m[s]={t:0,c:0};m[s].t++;if(l.status==="Closed Deal")m[s].c++;});`);
add(`    return Object.entries(m).map(([s,d])=>({src:s,total:d.t,closed:d.c,rate:d.t>0?Math.round(d.c*100/d.t):0})).sort((a,b)=>b.total-a.total);`);
add(`  },[allLeads]);`);
add(``);
add(`  // property matching`);
add(`  const matchedListings = useMemo(()=>{`);
add(`    if(!selectedLead||!listings) return [];`);
add(`    const b=parseFloat(selectedLead.budget||0);`);
add(`    return (listings||[]).filter(l=>{`);
add(`      const lp=parseFloat(l.price||l.priceMin||0);`);
add(`      return (b>0?lp<=b*1.25&&lp>=b*0.6:true)&&(selectedLead.community?(l.community||"").toLowerCase().includes((selectedLead.community||"").toLowerCase()):true);`);
add(`    }).slice(0,4);`);
add(`  },[selectedLead,listings]);`);
add(``);
add(`  // export`);
add(`  const doExport = useCallback(()=>{`);
add(`    const h=["ID","Name","Phone","Email","Budget","Service Type","Request Type","Status","Source","Channel","AD Name","AD Account","Nationality","Language","Timeline","Community","Bedrooms","Project","Campaign","Score","Created"];`);
add(`    const rows=filtered.map(l=>[l.id||"",l.name||"",l.phone||"",l.email||"",l.budget||"",l.serviceType||l.type||"",l.requestType||"",l.status||"",l.source||"",l.channel||"",l.adName||"",l.adAccount||"",l.nationality||"",l.language||"",l.timeline||"",l.community||"",l.bedrooms||"",l.project||"",l.campaign||"",aiScore(l).score,l.createdAt?new Date(l.createdAt).toLocaleDateString("en-GB"):""].map(v=>escCSV(v)));`);
add(`    const csv=[h.join(","),...rows.map(r=>r.join(","))].join("\\n");`);
add(`    const blob=new Blob([csv],{type:"text/csv"});`);
add(`    const url=URL.createObjectURL(blob);`);
add(`    const a=document.createElement("a");`);
add(`    a.href=url;a.download="DXB_Leads_"+new Date().toISOString().slice(0,10)+".csv";a.click();URL.revokeObjectURL(url);`);
add(`  },[filtered]);`);
add(``);
add(`  // send WA`);
add(`  const sendWA = useCallback((tmpl,lead)=>{`);
add(`    if(!lead?.phone) return;`);
add(`    const msg=tmpl.body.replace("{name}",lead.name||"").replace("{agent}","Agent").replace("{budget}",fmtB(lead.budget)).replace("{community}",lead.community||"Dubai").replace("{type}",lead.serviceType||"property").replace("{date}","the scheduled time");`);
add(`    window.open("https://wa.me/"+clnPhone(lead.phone)+"?text="+encodeURIComponent(msg),"_blank");`);
add(`    setShowWA(false);`);
add(`  },[]);`);
add(``);
add(`  if(!canSee) return (`);
add(`    <div style={{display:"flex",flexDirection:"column",alignItems:"center",justifyContent:"center",height:400,gap:12}}>`);
add(`      <div style={{fontSize:32}}>🔒</div>`);
add(`      <div style={{fontSize:16,fontWeight:700,color:T.white}}>CRM Access Required</div>`);
add(`      <div style={{fontSize:12,color:T.textMuted,marginTop:4}}>Contact your agency manager to enable CRM access</div>`);
add(`    </div>`);
add(`  );`);
add(``);
add(`  return (`);
add(`    <div style={{paddingBottom:80,marginLeft:-4,marginRight:-4}}>`);
add(``);
add(`      {/* ══ SMART VIEW TABS (Twenty CRM top bar pattern) ══════════════════════ */}`);
add(`      <div style={{display:"flex",alignItems:"center",gap:0,marginBottom:0,overflowX:"auto",borderBottom:"1px solid "+T.border,paddingLeft:4}}>`);
add(`        {SMART_VIEWS.map(v => {`);
add(`          const active = smartView===v.key;`);
add(`          return (`);
add(`            <button key={v.key} type="button" onClick={()=>{setSmartView(v.key);setActiveStage("all");}}`);
add(`              style={{padding:"10px 14px",border:"none",borderBottom:active?"2px solid "+T.gold:"2px solid transparent",background:"transparent",color:active?T.white:T.textMuted,fontSize:12,fontWeight:active?600:400,cursor:"pointer",whiteSpace:"nowrap",fontFamily:"'Outfit',sans-serif",transition:"color 0.15s"}}>`);
add(`              {v.label}`);
add(`            </button>`);
add(`          );`);
add(`        })}`);
add(`      </div>`);
add(``);
add(`      {/* ══ TOOLBAR (Twenty CRM second row) ════════════════════════════════════ */}`);
add(`      <div style={{display:"flex",alignItems:"center",gap:10,padding:"10px 4px",borderBottom:"1px solid "+T.border,flexWrap:"wrap"}}>`);
add(`        {/* Lead count */}`);
add(`        <div style={{display:"flex",alignItems:"center",gap:8}}>`);
add(`          <span style={{fontSize:13,fontWeight:700,color:T.white}}>Leads</span>`);
add(`          <span style={{fontSize:13,fontWeight:700,color:T.textMuted}}>{kpiTotal.toLocaleString()}</span>`);
add(`        </div>`);
add(`        {/* AI Search bar — Twenty CRM style ⌘K */}`);
add(`        <div style={{flex:"1 1 260px",position:"relative"}}>`);
add(`          <div style={{display:"flex",alignItems:"center",gap:8,padding:"6px 12px",background:"rgba(255,255,255,0.04)",border:"1px solid "+(aiSearch?T.gold:T.border),borderRadius:8,cursor:"text"}} onClick={()=>document.getElementById("crm-ai-search")?.focus()}>`);
add(`            <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke={T.textMuted} strokeWidth="2"><circle cx="11" cy="11" r="8"/><line x1="21" y1="21" x2="16.65" y2="16.65"/></svg>`);
add(`            <input id="crm-ai-search" value={aiSearch} onChange={e=>setAiSearch(e.target.value)} placeholder="Ask AI to filter leads..." style={{flex:1,background:"none",border:"none",outline:"none",color:T.white,fontSize:12,fontFamily:"'Outfit',sans-serif"}} />`);
add(`            <span style={{fontSize:10,color:T.textMuted,padding:"2px 6px",borderRadius:4,border:"1px solid "+T.border,fontFamily:"monospace"}}>⌘K</span>`);
add(`          </div>`);
add(`        </div>`);
add(`        {/* Right actions */}`);
add(`        <div style={{display:"flex",gap:6,marginLeft:"auto",alignItems:"center"}}>`);
add(`          {/* View toggles — Twenty CRM icon style */}`);
add(`          <div style={{display:"flex",gap:2,background:"rgba(255,255,255,0.03)",border:"1px solid "+T.border,borderRadius:7,padding:2}}>`);
add(`            {[`);
add(`              {k:"table",   icon:<svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2"><rect x="3" y="3" width="18" height="18" rx="2"/><line x1="3" y1="9" x2="21" y2="9"/><line x1="3" y1="15" x2="21" y2="15"/><line x1="9" y1="3" x2="9" y2="21"/></svg>},`);
add(`              {k:"kanban",  icon:<svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2"><rect x="3" y="3" width="5" height="18" rx="1"/><rect x="10" y="3" width="5" height="14" rx="1"/><rect x="17" y="3" width="5" height="10" rx="1"/></svg>},`);
add(`              {k:"analytics",icon:<svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2"><line x1="18" y1="20" x2="18" y2="10"/><line x1="12" y1="20" x2="12" y2="4"/><line x1="6" y1="20" x2="6" y2="14"/></svg>},`);
add(`            ].map(v=>(`);
add(`              <button key={v.k} type="button" onClick={()=>setView(v.k)} style={{padding:"5px 8px",borderRadius:5,border:view===v.k?"1px solid "+T.gold:"1px solid transparent",background:view===v.k?"rgba(212,168,67,0.15)":"transparent",color:view===v.k?T.gold:T.textMuted,cursor:"pointer",display:"flex",alignItems:"center"}}>{v.icon}</button>`);
add(`            ))}`);
add(`          </div>`);
add(`          <button type="button" onClick={doExport} style={{padding:"6px 11px",borderRadius:7,border:"1px solid "+T.border,background:"transparent",color:T.textSecondary,fontSize:11,fontWeight:600,cursor:"pointer",fontFamily:"'Outfit',sans-serif"}}>Export</button>`);
add(`          <button type="button" onClick={()=>setShowWA(true)} style={{padding:"6px 11px",borderRadius:7,border:"1px solid rgba(37,211,102,0.3)",background:"rgba(37,211,102,0.07)",color:"#25D366",fontSize:11,fontWeight:600,cursor:"pointer",fontFamily:"'Outfit',sans-serif"}}>Bulk WA</button>`);
add(`          <button type="button" onClick={()=>setShowAdd(true)} style={{padding:"7px 16px",borderRadius:7,border:"none",background:"linear-gradient(135deg,#D4A843,#B8902E)",color:"#0A0E1A",fontSize:12,fontWeight:700,cursor:"pointer",fontFamily:"'Outfit',sans-serif",display:"flex",alignItems:"center",gap:5}}>`);
add(`            <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2.5"><line x1="12" y1="5" x2="12" y2="19"/><line x1="5" y1="12" x2="19" y2="12"/></svg>`);
add(`            Add`);
add(`          </button>`);
add(`        </div>`);
add(`      </div>`);
add(``);
add(`      {/* ══ PIPELINE STAGE PILLS ROW (Twenty CRM colored dots row) ════════════ */}`);
add(`      <div style={{display:"flex",alignItems:"center",gap:0,overflowX:"auto",padding:"0 4px",borderBottom:"1px solid "+T.border,background:"rgba(255,255,255,0.01)"}}>`);
add(`        <button type="button" onClick={()=>setActiveStage("all")}`);
add(`          style={{padding:"8px 12px",border:"none",borderBottom:activeStage==="all"?"2px solid "+T.gold:"2px solid transparent",background:"transparent",color:activeStage==="all"?T.white:T.textMuted,fontSize:11,fontWeight:activeStage==="all"?600:400,cursor:"pointer",whiteSpace:"nowrap",fontFamily:"'Outfit',sans-serif"}}>All</button>`);
add(`        {PIPELINE_STAGES.map(stage => {`);
add(`          const count = stageCounts[stage.key]||0;`);
add(`          const active = activeStage===stage.key;`);
add(`          return (`);
add(`            <button key={stage.key} type="button" onClick={()=>setActiveStage(active?"all":stage.key)}`);
add(`              style={{padding:"8px 12px",border:"none",borderBottom:active?"2px solid "+stage.color:"2px solid transparent",background:"transparent",color:active?stage.color:T.textMuted,fontSize:11,fontWeight:active?600:400,cursor:"pointer",whiteSpace:"nowrap",fontFamily:"'Outfit',sans-serif",display:"flex",alignItems:"center",gap:5,transition:"color 0.15s"}}>`);
add(`              <span style={{width:7,height:7,borderRadius:"50%",background:stage.color,flexShrink:0}} />`);
add(`              {stage.key}`);
add(`              <span style={{fontSize:10,color:active?stage.color:T.textMuted}}>{count}</span>`);
add(`            </button>`);
add(`          );`);
add(`        })}`);
add(`      </div>`);
add(``);
add(`      {/* ══ FILTER ROW ══════════════════════════════════════════════════════════ */}`);
add(`      <div style={{display:"flex",gap:8,padding:"8px 4px",borderBottom:"1px solid "+T.border,flexWrap:"wrap",alignItems:"center"}}>`);
add(`        <select value={filterService} onChange={e=>setFilterService(e.target.value)} style={{padding:"5px 8px",background:"rgba(255,255,255,0.04)",border:"1px solid "+T.border,borderRadius:6,color:T.textSecondary,fontSize:11,outline:"none"}}>`);
add(`          <option value="all">All Types</option>`);
add(`          {SERVICE_TYPES.map(t=><option key={t} value={t}>{t}</option>)}`);
add(`        </select>`);
add(`        <select value={filterSource} onChange={e=>setFilterSource(e.target.value)} style={{padding:"5px 8px",background:"rgba(255,255,255,0.04)",border:"1px solid "+T.border,borderRadius:6,color:T.textSecondary,fontSize:11,outline:"none"}}>`);
add(`          <option value="all">All Sources</option>`);
add(`          {LEAD_SOURCES.map(s=><option key={s} value={s}>{s}</option>)}`);
add(`        </select>`);
add(`        <select value={filterBudget} onChange={e=>setFilterBudget(e.target.value)} style={{padding:"5px 8px",background:"rgba(255,255,255,0.04)",border:"1px solid "+T.border,borderRadius:6,color:T.textSecondary,fontSize:11,outline:"none"}}>`);
add(`          <option value="all">All Budgets</option>`);
add(`          <option value="under1m">Under AED 1M</option>`);
add(`          <option value="1to3m">AED 1-3M</option>`);
add(`          <option value="3to5m">AED 3-5M</option>`);
add(`          <option value="5to10m">AED 5-10M</option>`);
add(`          <option value="above10m">Above AED 10M</option>`);
add(`        </select>`);
add(`        <select value={leadSortBy||"date"} onChange={e=>setLeadSortBy(e.target.value)} style={{padding:"5px 8px",background:"rgba(255,255,255,0.04)",border:"1px solid "+T.border,borderRadius:6,color:T.textSecondary,fontSize:11,outline:"none"}}>`);
add(`          <option value="date">Latest First</option>`);
add(`          <option value="score">AI Score</option>`);
add(`          <option value="budget">Budget High-Low</option>`);
add(`          <option value="name">Name A-Z</option>`);
add(`        </select>`);
add(`        {(aiSearch||activeStage!=="all"||filterService!=="all"||filterSource!=="all"||filterBudget!=="all")&&(`);
add(`          <div style={{display:"flex",gap:6,alignItems:"center",marginLeft:"auto"}}>`);
add(`            {aiSearch&&<span style={{fontSize:10,padding:"3px 8px",borderRadius:10,background:"rgba(99,179,237,0.15)",color:"#63B3ED",display:"flex",alignItems:"center",gap:4}}>"{aiSearch}" <span style={{cursor:"pointer"}} onClick={()=>setAiSearch("")}>×</span></span>}`);
add(`            {activeStage!=="all"&&<span style={{fontSize:10,padding:"3px 8px",borderRadius:10,background:"rgba(212,168,67,0.15)",color:T.gold,display:"flex",alignItems:"center",gap:4}}>{activeStage} <span style={{cursor:"pointer"}} onClick={()=>setActiveStage("all")}>×</span></span>}`);
add(`            <button type="button" onClick={()=>{setAiSearch("");setActiveStage("all");setFilterService("all");setFilterSource("all");setFilterBudget("all");}} style={{fontSize:10,padding:"3px 10px",borderRadius:10,background:"rgba(252,129,129,0.1)",border:"1px solid rgba(252,129,129,0.3)",color:"#FC8181",cursor:"pointer",fontFamily:"'Outfit',sans-serif"}}>Clear all</button>`);
add(`            <span style={{fontSize:11,color:T.textMuted}}>{filtered.length} results</span>`);
add(`          </div>`);
add(`        )}`);
add(`      </div>`);
add(``);
add(`      {/* ══ MAIN CONTENT ════════════════════════════════════════════════════════ */}`);
add(`      <div style={{display:"grid",gridTemplateColumns:selectedLead?"1fr 360px":"1fr",gap:0,alignItems:"start"}}>`);
add(``);
add(`        {/* ── TABLE VIEW — exact Twenty CRM column layout ── */}`);
add(`        {view==="table"&&(`);
add(`          <div style={{background:"rgba(255,255,255,0.01)"}}>`);
add(`            {/* Column headers — ID · NAME · SERVICE TYPE · AD NAME · AD ACCOUNT · MOBILE · EMAIL */}`);
add(`            <div style={{display:"grid",gridTemplateColumns:"80px 2fr 120px 140px 140px 160px 1fr 40px",padding:"8px 4px",borderBottom:"1px solid "+T.border,background:"rgba(255,255,255,0.02)"}}>`);
add(`              {["ID","NAME","SERVICE TYPE","AD NAME","AD ACCOUNT","MOBILE","EMAIL",""].map((h,i)=>(`);
add(`                <div key={i} style={{fontSize:10,fontWeight:700,color:T.textMuted,textTransform:"uppercase",letterSpacing:0.6,paddingLeft:i===0?4:0}}>{h}</div>`);
add(`              ))}`);
add(`            </div>`);
add(`            {/* Empty state */}`);
add(`            {allLeads.length===0&&(`);
add(`              <div style={{padding:"80px 20px",textAlign:"center"}}>`);
add(`                <div style={{fontSize:40,marginBottom:12}}>📋</div>`);
add(`                <div style={{fontSize:16,fontWeight:700,color:T.white,marginBottom:6}}>No leads yet</div>`);
add(`                <div style={{fontSize:12,color:T.textMuted,marginBottom:16}}>Add your first lead to get started</div>`);
add(`                <button type="button" onClick={()=>setShowAdd(true)} style={{padding:"10px 24px",borderRadius:8,border:"none",background:"linear-gradient(135deg,#D4A843,#B8902E)",color:"#0A0E1A",fontSize:13,fontWeight:700,cursor:"pointer"}}>+ Add First Lead</button>`);
add(`              </div>`);
add(`            )}`);
add(`            {filtered.length===0&&allLeads.length>0&&(`);
add(`              <div style={{padding:"40px 20px",textAlign:"center",color:T.textMuted,fontSize:13}}>No leads match your filters.</div>`);
add(`            )}`);
add(`            {/* Rows */}`);
add(`            {filtered.map((lead,i)=>{`);
add(`              const sc=aiScore(lead);`);
add(`              const stale=!["Closed Deal","Closed Outside","Non Potential"].includes(lead.status)&&daysAgo(lead.updatedAt||lead.createdAt)>7;`);
add(`              const isSel=selectedLead?.id===lead.id;`);
add(`              const stage=PIPELINE_STAGES.find(s=>s.key===lead.status);`);
add(`              return (`);
add(`                <div key={lead.id||i} onClick={()=>setSelectedLead(isSel?null:lead)}`);
add(`                  style={{display:"grid",gridTemplateColumns:"80px 2fr 120px 140px 140px 160px 1fr 40px",padding:"11px 4px",borderBottom:"1px solid "+T.border+"40",cursor:"pointer",background:isSel?"rgba(212,168,67,0.04)":"transparent",transition:"background 0.1s",borderLeft:isSel?"3px solid "+T.gold:"3px solid transparent"}}`);
add(`                  onMouseEnter={e=>!isSel&&(e.currentTarget.style.background="rgba(255,255,255,0.02)")}`);
add(`                  onMouseLeave={e=>!isSel&&(e.currentTarget.style.background="transparent")}`);
add(`                >`);
add(`                  {/* ID */}`);
add(`                  <div style={{display:"flex",alignItems:"center",gap:4,paddingLeft:4}}>`);
add(`                    <span style={{color:T.textMuted,fontSize:10}}>#</span>`);
add(`                    <span style={{fontSize:11,color:T.textMuted}}>{lead.id?.slice(-5)||String(i+1).padStart(4,"0")}</span>`);
add(`                  </div>`);
add(`                  {/* NAME */}`);
add(`                  <div style={{display:"flex",alignItems:"center",gap:6}}>`);
add(`                    <div style={{width:24,height:24,borderRadius:"50%",background:"rgba(255,255,255,0.08)",display:"flex",alignItems:"center",justifyContent:"center",flexShrink:0}}>`);
add(`                      <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke={T.textMuted} strokeWidth="2"><path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"/><circle cx="12" cy="7" r="4"/></svg>`);
add(`                    </div>`);
add(`                    <div>`);
add(`                      <div style={{display:"flex",alignItems:"center",gap:4}}>`);
add(`                        <span style={{fontSize:12,fontWeight:500,color:T.white}}>{lead.name||"Unnamed"}</span>`);
add(`                        <CopyIcon text={lead.name} />`);
add(`                      </div>`);
add(`                      <div style={{display:"flex",gap:4,marginTop:2}}>`);
add(`                        <span style={{fontSize:9,color:sc.color,fontWeight:700}}>{sc.label}</span>`);
add(`                        {parseFloat(lead.budget||0)>=GV_THRESHOLD&&<span style={{fontSize:9,color:T.gold}}>GV</span>}`);
add(`                        {stale&&<span style={{fontSize:9,color:"#EF4444"}}>Stale</span>}`);
add(`                      </div>`);
add(`                    </div>`);
add(`                  </div>`);
add(`                  {/* SERVICE TYPE */}`);
add(`                  <div style={{fontSize:11,color:lead.serviceType||lead.type?"#A78BFA":T.textMuted,fontWeight:lead.serviceType||lead.type?600:400,alignSelf:"center"}}>{lead.serviceType||lead.type||"—"}</div>`);
add(`                  {/* AD NAME */}`);
add(`                  <div style={{fontSize:11,color:T.textMuted,alignSelf:"center",overflow:"hidden",textOverflow:"ellipsis",whiteSpace:"nowrap"}}>{lead.adName||"—"}</div>`);
add(`                  {/* AD ACCOUNT */}`);
add(`                  <div style={{fontSize:11,color:T.textMuted,alignSelf:"center",overflow:"hidden",textOverflow:"ellipsis",whiteSpace:"nowrap"}}>{lead.adAccount||"—"}</div>`);
add(`                  {/* MOBILE */}`);
add(`                  <div style={{alignSelf:"center"}}>`);
add(`                    {lead.phone ? (`);
add(`                      <div style={{display:"flex",alignItems:"center",gap:4}}>`);
add(`                        <svg width="11" height="11" viewBox="0 0 24 24" fill="none" stroke={T.textMuted} strokeWidth="2"><path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07A19.5 19.5 0 0 1 4.69 13a19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 3.6 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"/></svg>`);
add(`                        <EyeReveal value={lead.phone} masked={maskPhone(lead.phone)} />`);
add(`                      </div>`);
add(`                    ) : <span style={{fontSize:11,color:T.textMuted}}>—</span>}`);
add(`                  </div>`);
add(`                  {/* EMAIL */}`);
add(`                  <div style={{fontSize:11,color:T.textMuted,alignSelf:"center",overflow:"hidden",textOverflow:"ellipsis",whiteSpace:"nowrap"}}>{lead.email||"—"}</div>`);
add(`                  {/* ACTIONS */}`);
add(`                  <div style={{display:"flex",alignItems:"center",justifyContent:"center"}}>`);
add(`                    <button type="button" onClick={e=>{e.stopPropagation();}} style={{background:"none",border:"none",color:T.textMuted,cursor:"pointer",padding:"4px",fontSize:16,letterSpacing:1}}>···</button>`);
add(`                  </div>`);
add(`                </div>`);
add(`              );`);
add(`            })}`);
add(`          </div>`);
add(`        )}`);
add(``);
add(`        {/* ── KANBAN VIEW ── */}`);
add(`        {view==="kanban"&&(`);
add(`          <div style={{overflowX:"auto",padding:"12px 0"}}>`);
add(`            <div style={{display:"flex",gap:10,minWidth:"max-content",alignItems:"start"}}>`);
add(`              {PIPELINE_STAGES.map(stage=>{`);
add(`                const cols=allLeads.filter(l=>(l.status||"New Lead")===stage.key);`);
add(`                return (`);
add(`                  <div key={stage.key} style={{width:220,flexShrink:0}}>`);
add(`                    <div style={{display:"flex",justifyContent:"space-between",alignItems:"center",marginBottom:8,padding:"7px 10px",background:"rgba(255,255,255,0.03)",borderRadius:7,border:"1px solid "+stage.color+"25"}}>`);
add(`                      <div style={{display:"flex",alignItems:"center",gap:6}}>`);
add(`                        <div style={{width:8,height:8,borderRadius:"50%",background:stage.color}} />`);
add(`                        <span style={{fontSize:11,fontWeight:600,color:stage.color}}>{stage.key}</span>`);
add(`                      </div>`);
add(`                      <span style={{fontSize:10,color:stage.color,background:stage.color+"15",padding:"1px 6px",borderRadius:8}}>{cols.length}</span>`);
add(`                    </div>`);
add(`                    <div style={{display:"flex",flexDirection:"column",gap:6,maxHeight:560,overflowY:"auto"}}>`);
add(`                      {cols.map((lead,i)=>{`);
add(`                        const sc=aiScore(lead);`);
add(`                        const stale=daysAgo(lead.updatedAt||lead.createdAt)>7;`);
add(`                        return (`);
add(`                          <div key={lead.id||i} onClick={()=>setSelectedLead(selectedLead?.id===lead.id?null:lead)}`);
add(`                            style={{background:"rgba(255,255,255,0.03)",border:"1px solid "+(stale?"rgba(239,68,68,0.25)":T.border),borderRadius:8,padding:"10px 11px",cursor:"pointer",transition:"border-color 0.15s"}}`);
add(`                            onMouseEnter={e=>(e.currentTarget.style.borderColor=stage.color+"50")}`);
add(`                            onMouseLeave={e=>(e.currentTarget.style.borderColor=stale?"rgba(239,68,68,0.25)":T.border)}`);
add(`                          >`);
add(`                            <div style={{display:"flex",justifyContent:"space-between",marginBottom:4}}>`);
add(`                              <span style={{fontSize:12,fontWeight:500,color:T.white,overflow:"hidden",textOverflow:"ellipsis",whiteSpace:"nowrap",flex:1}}>{lead.name||"Unnamed"}</span>`);
add(`                              <div style={{width:7,height:7,borderRadius:"50%",background:sc.color,flexShrink:0,marginTop:3}} />`);
add(`                            </div>`);
add(`                            <div style={{fontSize:12,fontWeight:700,color:T.gold,marginBottom:5}}>{fmtB(lead.budget)}</div>`);
add(`                            <div style={{display:"flex",justifyContent:"space-between",alignItems:"center"}}>`);
add(`                              <span style={{fontSize:10,color:SRC_COLOR[lead.source]||T.textMuted}}>{lead.source||"Manual"}</span>`);
add(`                              <div style={{display:"flex",gap:4}}>`);
add(`                                {parseFloat(lead.budget||0)>=GV_THRESHOLD&&<span style={{fontSize:9,color:T.gold}}>GV</span>}`);
add(`                                {stale&&<span style={{fontSize:9,color:"#EF4444"}}>Stale</span>}`);
add(`                              </div>`);
add(`                            </div>`);
add(`                            {lead.community&&<div style={{fontSize:9,color:T.textMuted,marginTop:4}}>{lead.community}</div>}`);
add(`                          </div>`);
add(`                        );`);
add(`                      })}`);
add(`                      {cols.length===0&&(`);
add(`                        <div style={{padding:"14px",textAlign:"center",color:T.textMuted,fontSize:11,border:"1px dashed "+T.border+"50",borderRadius:7}}>No leads</div>`);
add(`                      )}`);
add(`                    </div>`);
add(`                  </div>`);
add(`                );`);
add(`              })}`);
add(`            </div>`);
add(`          </div>`);
add(`        )}`);
add(``);
add(`        {/* ── ANALYTICS VIEW ── */}`);
add(`        {view==="analytics"&&(`);
add(`          <div style={{padding:"16px 4px",display:"flex",flexDirection:"column",gap:14}}>`);
add(`            {/* KPI row */}`);
add(`            <div style={{display:"grid",gridTemplateColumns:"repeat(6,1fr)",gap:8}}>`);
add(`              {[`);
add(`                {l:"Total",      v:kpiTotal,      c:T.gold},`);
add(`                {l:"New Today",  v:kpiToday,      c:"#0EA5E9"},`);
add(`                {l:"Hot",        v:kpiHot,        c:"#10B981"},`);
add(`                {l:"Closed",     v:kpiClosed,     c:"#10B981"},`);
add(`                {l:"Stale 7d+",  v:kpiStale,      c:"#EF4444"},`);
add(`                {l:"Pipeline",   v:kpiPipeline>=1e9?"AED "+(kpiPipeline/1e9).toFixed(1)+"B":"AED "+(kpiPipeline/1e6).toFixed(1)+"M", c:T.gold},`);
add(`              ].map((k,i)=>(`);
add(`                <div key={i} style={{background:"rgba(255,255,255,0.03)",border:"1px solid "+T.border,borderRadius:9,padding:"10px 12px",position:"relative",overflow:"hidden"}}>`);
add(`                  <div style={{position:"absolute",top:0,left:0,right:0,height:2,background:k.c,opacity:0.8}} />`);
add(`                  <div style={{fontSize:9,fontWeight:700,color:T.textMuted,textTransform:"uppercase",letterSpacing:0.7,marginBottom:3}}>{k.l}</div>`);
add(`                  <div style={{fontSize:18,fontWeight:900,color:k.c,fontFamily:"'Fraunces',serif"}}>{k.v}</div>`);
add(`                </div>`);
add(`              ))}`);
add(`            </div>`);
add(`            {/* Source performance */}`);
add(`            <div style={{background:"rgba(255,255,255,0.02)",border:"1px solid "+T.border,borderRadius:12,padding:"18px"}}>`);
add(`              <div style={{fontSize:13,fontWeight:700,color:T.white,marginBottom:14}}>Source Performance</div>`);
add(`              {allLeads.length===0?<div style={{padding:"20px",textAlign:"center",color:T.textMuted,fontSize:12}}>No leads yet</div>:(`);
add(`                <div style={{display:"grid",gridTemplateColumns:"repeat(auto-fill,minmax(150px,1fr))",gap:10}}>`);
add(`                  {srcStats.slice(0,8).map((s,i)=>(`);
add(`                    <div key={i} style={{background:"rgba(255,255,255,0.03)",borderRadius:9,padding:"11px"}}>`);
add(`                      <div style={{fontSize:11,fontWeight:700,color:SRC_COLOR[s.src]||T.textMuted,marginBottom:5,overflow:"hidden",textOverflow:"ellipsis",whiteSpace:"nowrap"}}>{s.src}</div>`);
add(`                      <div style={{display:"flex",justifyContent:"space-between",alignItems:"flex-end"}}>`);
add(`                        <div><div style={{fontSize:18,fontWeight:900,color:T.white,fontFamily:"'Fraunces',serif",lineHeight:1}}>{s.total}</div><div style={{fontSize:9,color:T.textMuted}}>leads</div></div>`);
add(`                        <div style={{textAlign:"right"}}><div style={{fontSize:14,fontWeight:900,color:s.rate>=10?"#10B981":s.rate>=5?"#D4A843":"#EF4444",fontFamily:"'Fraunces',serif",lineHeight:1}}>{s.rate}%</div><div style={{fontSize:9,color:T.textMuted}}>closed</div></div>`);
add(`                      </div>`);
add(`                      <div style={{height:2,background:T.border,borderRadius:1,marginTop:7}}><div style={{height:"100%",width:Math.min(s.total,100)+"%",background:SRC_COLOR[s.src]||T.gold,borderRadius:1,opacity:0.7}} /></div>`);
add(`                    </div>`);
add(`                  ))}`);
add(`                </div>`);
add(`              )}`);
add(`            </div>`);
add(`            {/* Pipeline distribution */}`);
add(`            <div style={{background:"rgba(255,255,255,0.02)",border:"1px solid "+T.border,borderRadius:12,padding:"18px"}}>`);
add(`              <div style={{fontSize:13,fontWeight:700,color:T.white,marginBottom:14}}>Pipeline Distribution</div>`);
add(`              {PIPELINE_STAGES.map(stage=>{`);
add(`                const count=allLeads.filter(l=>(l.status||"New Lead")===stage.key).length;`);
add(`                const pct=allLeads.length>0?Math.round(count*100/allLeads.length):0;`);
add(`                return(`);
add(`                  <div key={stage.key} style={{marginBottom:8}}>`);
add(`                    <div style={{display:"flex",justifyContent:"space-between",marginBottom:3}}>`);
add(`                      <div style={{display:"flex",alignItems:"center",gap:6}}><div style={{width:7,height:7,borderRadius:"50%",background:stage.color}} /><span style={{fontSize:11,color:T.textSecondary}}>{stage.key}</span></div>`);
add(`                      <span style={{fontSize:11,fontWeight:700,color:stage.color}}>{count} <span style={{color:T.textMuted,fontWeight:400}}>({pct}%)</span></span>`);
add(`                    </div>`);
add(`                    <div style={{height:4,borderRadius:2,background:"rgba(255,255,255,0.06)"}}><div style={{height:"100%",width:pct+"%",borderRadius:2,background:stage.color,opacity:0.85,transition:"width 0.8s"}} /></div>`);
add(`                  </div>`);
add(`                );`);
add(`              })}`);
add(`            </div>`);
add(`          </div>`);
add(`        )}`);
add(``);
add(`        {/* ══ LEAD DETAIL DRAWER ══════════════════════════════════════════════ */}`);
add(`        {selectedLead&&(`);
add(`          <div style={{background:"rgba(255,255,255,0.02)",borderLeft:"1px solid "+T.border,display:"flex",flexDirection:"column",maxHeight:"calc(100vh - 200px)",position:"sticky",top:0,overflowY:"auto"}}>`);
add(`            {/* Header */}`);
add(`            <div style={{padding:"14px 16px",borderBottom:"1px solid "+T.border,background:"rgba(255,255,255,0.02)"}}>`);
add(`              <div style={{display:"flex",justifyContent:"space-between",alignItems:"flex-start",marginBottom:8}}>`);
add(`                <div style={{display:"flex",alignItems:"center",gap:8}}>`);
add(`                  <div style={{width:32,height:32,borderRadius:"50%",background:"rgba(255,255,255,0.08)",display:"flex",alignItems:"center",justifyContent:"center",flexShrink:0}}>`);
add(`                    <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke={T.textMuted} strokeWidth="2"><path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"/><circle cx="12" cy="7" r="4"/></svg>`);
add(`                  </div>`);
add(`                  <div>`);
add(`                    <div style={{fontSize:14,fontWeight:700,color:T.white}}>{selectedLead.name||"Unnamed"}</div>`);
add(`                    <StatusDot status={selectedLead.status||"New Lead"} />`);
add(`                  </div>`);
add(`                </div>`);
add(`                <button type="button" onClick={()=>setSelectedLead(null)} style={{background:"none",border:"none",color:T.textMuted,cursor:"pointer",fontSize:18}}>×</button>`);
add(`              </div>`);
add(`              {parseFloat(selectedLead.budget||0)>=GV_THRESHOLD&&(`);
add(`                <div style={{padding:"4px 9px",borderRadius:6,background:"rgba(212,168,67,0.12)",border:"1px solid rgba(212,168,67,0.25)",fontSize:10,color:T.gold,fontWeight:700,display:"inline-flex",alignItems:"center",gap:4}}>🏆 Golden Visa Eligible</div>`);
add(`              )}`);
add(`            </div>`);
add(`            {/* Drawer tabs */}`);
add(`            <div style={{display:"flex",borderBottom:"1px solid "+T.border}}>`);
add(`              {[{k:"profile",l:"Profile"},{k:"activity",l:"Activity"},{k:"match",l:"Matches"}].map(t=>(`);
add(`                <button key={t.k} type="button" onClick={()=>setDrawerTab(t.k)} style={{flex:1,padding:"8px 0",border:"none",borderBottom:drawerTab===t.k?"2px solid "+T.gold:"2px solid transparent",background:"transparent",color:drawerTab===t.k?T.gold:T.textMuted,fontSize:11,fontWeight:drawerTab===t.k?600:400,cursor:"pointer",fontFamily:"'Outfit',sans-serif"}}>{t.l}</button>`);
add(`              ))}`);
add(`            </div>`);
add(`            {/* Drawer body */}`);
add(`            <div style={{padding:"14px 16px",flex:1}}>`);
add(`              {drawerTab==="profile"&&(()=>{`);
add(`                const sc=aiScore(selectedLead);`);
add(`                return (`);
add(`                  <div>`);
add(`                    <div style={{padding:"10px 12px",borderRadius:8,background:sc.color+"0E",border:"1px solid "+sc.color+"25",marginBottom:14,display:"flex",justifyContent:"space-between",alignItems:"center"}}>`);
add(`                      <div>`);
add(`                        <div style={{fontSize:9,color:T.textMuted,fontWeight:700,textTransform:"uppercase",letterSpacing:0.7,marginBottom:2}}>AI SCORE</div>`);
add(`                        <div style={{fontFamily:"'Fraunces',serif",fontSize:18,fontWeight:800,color:sc.color}}>{sc.score}/100 — {sc.label}</div>`);
add(`                        <div style={{fontSize:10,color:T.textMuted,marginTop:1}}>Best contact: {BEST_TIME[selectedLead.source]||"Anytime"}</div>`);
add(`                      </div>`);
add(`                      <div style={{width:40,height:40,borderRadius:"50%",border:"2px solid "+sc.color,display:"flex",alignItems:"center",justifyContent:"center",background:sc.color+"12"}}>`);
add(`                        <span style={{fontSize:13,fontWeight:800,color:sc.color,fontFamily:"'Fraunces',serif"}}>{sc.score}</span>`);
add(`                      </div>`);
add(`                    </div>`);
add(`                    {[`);
add(`                      {label:"Phone",      value:selectedLead.phone,  link:selectedLead.phone?"tel:"+selectedLead.phone:null},`);
add(`                      {label:"WhatsApp",   value:selectedLead.phone?"Open WA":null, link:selectedLead.phone?"https://wa.me/"+clnPhone(selectedLead.phone):null, ext:true},`);
add(`                      {label:"Email",      value:selectedLead.email,  link:selectedLead.email?"mailto:"+selectedLead.email:null},`);
add(`                      {label:"Budget",     value:fmtB(selectedLead.budget)},`);
add(`                      {label:"Service",    value:selectedLead.serviceType||selectedLead.type||"—"},`);
add(`                      {label:"Request",    value:selectedLead.requestType||"—"},`);
add(`                      {label:"Project",    value:selectedLead.project||"—"},`);
add(`                      {label:"Source",     value:selectedLead.source||"—"},`);
add(`                      {label:"Channel",    value:selectedLead.channel||"—"},`);
add(`                      {label:"Campaign",   value:selectedLead.campaign||"—"},`);
add(`                      {label:"AD Name",    value:selectedLead.adName||"—"},`);
add(`                      {label:"AD Account", value:selectedLead.adAccount||"—"},`);
add(`                      {label:"Nationality",value:selectedLead.nationality||"—"},`);
add(`                      {label:"Community",  value:selectedLead.community||"—"},`);
add(`                      {label:"Timeline",   value:selectedLead.timeline||"—"},`);
add(`                      {label:"Agent",      value:selectedLead.agentName||"—"},`);
add(`                      {label:"Added",      value:fmtD(selectedLead.createdAt)},`);
add(`                    ].map((f,i)=>(`);
add(`                      <div key={i} style={{display:"flex",justifyContent:"space-between",alignItems:"center",padding:"5px 0",borderBottom:i<16?"1px solid "+T.border+"30":"none"}}>`);
add(`                        <span style={{fontSize:11,color:T.textMuted,flexShrink:0}}>{f.label}</span>`);
add(`                        {f.link?<a href={f.link} target={f.ext?"_blank":"_self"} rel="noopener noreferrer" style={{fontSize:11,color:T.gold,fontWeight:600,textDecoration:"none"}}>{f.value}</a>:<span style={{fontSize:11,color:T.white,fontWeight:500,textAlign:"right",maxWidth:180,overflow:"hidden",textOverflow:"ellipsis"}}>{f.value||"—"}</span>}`);
add(`                      </div>`);
add(`                    ))}`);
add(`                    {selectedLead.comment&&(`);
add(`                      <div style={{marginTop:10,padding:"8px 10px",background:"rgba(255,255,255,0.03)",borderRadius:7,fontSize:11,color:T.textSecondary,lineHeight:1.6}}>`);
add(`                        <div style={{fontSize:9,color:T.textMuted,marginBottom:3,fontWeight:700,textTransform:"uppercase",letterSpacing:0.7}}>Comment</div>`);
add(`                        {selectedLead.comment}`);
add(`                      </div>`);
add(`                    )}`);
add(`                  </div>`);
add(`                );`);
add(`              })()}`);
add(`              {drawerTab==="activity"&&(`);
add(`                <div>`);
add(`                  <div style={{display:"flex",flexDirection:"column",gap:6,marginBottom:12,maxHeight:240,overflowY:"auto"}}>`);
add(`                    {(selectedLead.notes_log||[]).length===0&&<div style={{padding:"20px",textAlign:"center",color:T.textMuted,fontSize:11}}>No activity yet</div>}`);
add(`                    {[...(selectedLead.notes_log||[])].reverse().map((n,i)=>(`);
add(`                      <div key={i} style={{padding:"8px 10px",background:"rgba(255,255,255,0.03)",borderRadius:7,borderLeft:"2px solid "+T.gold}}>`);
add(`                        <div style={{fontSize:11,color:T.white,marginBottom:2}}>{n.text}</div>`);
add(`                        <div style={{fontSize:9,color:T.textMuted}}>{n.type||"Note"} · {n.by||"Agent"} · {fmtD(n.at)}</div>`);
add(`                      </div>`);
add(`                    ))}`);
add(`                  </div>`);
add(`                  <div style={{borderTop:"1px solid "+T.border,paddingTop:10}}>`);
add(`                    <select value={noteType||"Note"} onChange={e=>setNoteType(e.target.value)} style={{width:"100%",padding:"6px 8px",background:"rgba(255,255,255,0.04)",border:"1px solid "+T.border,borderRadius:6,color:T.textSecondary,fontSize:11,outline:"none",marginBottom:6}}>`);
add(`                      {["Note","Call","Email","WhatsApp","Viewing","Offer","Follow Up","Status Update"].map(t=><option key={t} value={t}>{t}</option>)}`);
add(`                    </select>`);
add(`                    <textarea value={noteText||""} onChange={e=>setNoteText(e.target.value)} placeholder="Add activity note..." rows={3} style={{width:"100%",padding:"7px 9px",background:"rgba(255,255,255,0.04)",border:"1px solid "+T.border,borderRadius:6,color:T.white,fontSize:12,outline:"none",resize:"none",boxSizing:"border-box"}} />`);
add(`                  </div>`);
add(`                </div>`);
add(`              )}`);
add(`              {drawerTab==="match"&&(`);
add(`                <div>`);
add(`                  <div style={{fontSize:11,color:T.textMuted,marginBottom:10}}>{matchedListings.length>0?"Properties matching budget + area:":"No listings match this lead yet."}</div>`);
add(`                  {matchedListings.map((l,i)=>(`);
add(`                    <div key={i} style={{padding:"9px 11px",background:"rgba(255,255,255,0.03)",borderRadius:7,marginBottom:7,border:"1px solid "+T.border}}>`);
add(`                      <div style={{fontSize:12,fontWeight:600,color:T.white,marginBottom:3}}>{l.title||l.name||"Property"}</div>`);
add(`                      <div style={{display:"flex",justifyContent:"space-between"}}><span style={{fontSize:11,color:T.textMuted}}>{l.community||""}</span><span style={{fontSize:12,fontWeight:700,color:T.gold}}>{fmtB(l.price||l.priceMin)}</span></div>`);
add(`                    </div>`);
add(`                  ))}`);
add(`                  {matchedListings.length===0&&(`);
add(`                    <button type="button" onClick={()=>handleTabChange?.("Projects")} style={{width:"100%",padding:"8px",borderRadius:7,border:"1px solid "+T.border,background:"rgba(212,168,67,0.06)",color:T.gold,fontSize:11,cursor:"pointer",fontFamily:"'Outfit',sans-serif"}}>Browse Projects →</button>`);
add(`                  )}`);
add(`                </div>`);
add(`              )}`);
add(`            </div>`);
add(`          </div>`);
add(`        )}`);
add(`      </div>`);
add(``);
add(`      {/* ══ ADD LEAD MODAL — exact Twenty CRM section pattern ═══════════════ */}`);
add(`      {showAdd&&(`);
add(`        <div style={{position:"fixed",inset:0,background:"rgba(4,9,15,0.88)",zIndex:2000,display:"flex",alignItems:"center",justifyContent:"center",padding:"20px"}} onClick={e=>{if(e.target===e.currentTarget)setShowAdd(false);}}>`);
add(`          <div style={{background:"#0D1117",borderRadius:14,border:"1px solid "+T.border,width:"100%",maxWidth:640,maxHeight:"90vh",overflowY:"auto"}} onClick={e=>e.stopPropagation()}>`);
add(`            {/* Modal header */}`);
add(`            <div style={{padding:"16px 20px",borderBottom:"1px solid "+T.border,display:"flex",justifyContent:"space-between",alignItems:"center",position:"sticky",top:0,background:"#0D1117",zIndex:1}}>`);
add(`              <div style={{fontFamily:"'Fraunces',serif",fontSize:15,fontWeight:900,color:T.white}}>Add Lead</div>`);
add(`              <button type="button" onClick={()=>setShowAdd(false)} style={{background:"rgba(255,255,255,0.06)",border:"1px solid "+T.border,borderRadius:7,color:T.textMuted,width:28,height:28,cursor:"pointer",fontSize:16,display:"flex",alignItems:"center",justifyContent:"center"}}>×</button>`);
add(`            </div>`);
add(`            <div style={{padding:"20px"}}>`);
add(``);
add(`              {/* SECTION: Contact Info */}`);
add(`              <div style={{marginBottom:16}}>`);
add(`                <div style={{display:"grid",gridTemplateColumns:"1fr 1fr",gap:12}}>`);
add(`                  {[{l:"Full Name *",k:"name",ph:"Client name",t:"text"},{l:"Phone *",k:"phone",ph:"+971 50 XXX XXXX",t:"tel"},{l:"Email",k:"email",ph:"email@example.com",t:"email"},{l:"Budget (AED)",k:"budget",ph:"e.g. 2000000",t:"number"}].map(f=>(`);
add(`                    <div key={f.k}>`);
add(`                      <div style={{fontSize:10,color:T.textMuted,marginBottom:3,fontWeight:600,textTransform:"uppercase",letterSpacing:0.5}}>{f.l}</div>`);
add(`                      <input type={f.t} value={form[f.k]||""} onChange={e=>setForm(p=>({...p,[f.k]:e.target.value}))} placeholder={f.ph}`);
add(`                        style={{width:"100%",padding:"8px 10px",background:"rgba(255,255,255,0.04)",border:"1px solid "+T.border,borderRadius:7,color:T.white,fontSize:12,outline:"none",boxSizing:"border-box"}} />`);
add(`                    </div>`);
add(`                  ))}`);
add(`                </div>`);
add(`              </div>`);
add(``);
add(`              {/* SECTION: Request Information — exact from screenshot */}`);
add(`              <div style={{background:"rgba(255,255,255,0.02)",border:"1px solid "+T.border,borderRadius:10,marginBottom:12,overflow:"hidden"}}>`);
add(`                <div style={{padding:"11px 14px",display:"flex",alignItems:"center",gap:10,cursor:"pointer",borderBottom:openSection.request?"1px solid "+T.border:"none"}} onClick={()=>setOpenSection(s=>({...s,request:!s.request}))}>`);
add(`                  <div style={{width:28,height:28,borderRadius:6,background:"rgba(99,102,241,0.15)",display:"flex",alignItems:"center",justifyContent:"center",flexShrink:0}}>`);
add(`                    <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="#818CF8" strokeWidth="2"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/></svg>`);
add(`                  </div>`);
add(`                  <div>`);
add(`                    <div style={{fontSize:13,fontWeight:700,color:T.white}}>Request Information</div>`);
add(`                    <div style={{fontSize:11,color:T.textMuted}}>Project and classification details</div>`);
add(`                  </div>`);
add(`                  <div style={{marginLeft:"auto",color:T.textMuted,fontSize:16,transition:"transform 0.2s",transform:openSection.request?"rotate(90deg)":"rotate(0deg)"}}>{openSection.request?"▾":"▸"}</div>`);
add(`                </div>`);
add(`                {openSection.request&&(`);
add(`                  <div style={{padding:"14px",display:"grid",gridTemplateColumns:"1fr 1fr",gap:12}}>`);
add(`                    {[`);
add(`                      {l:"Request Type",  k:"requestType", opts:["", ...REQUEST_TYPES], ph:"Select request type"},`);
add(`                      {l:"Service Type",  k:"serviceType", opts:["", ...SERVICE_TYPES], ph:"Select service type"},`);
add(`                    ].map(f=>(`);
add(`                      <div key={f.k}>`);
add(`                        <div style={{fontSize:10,color:T.textMuted,marginBottom:3,fontWeight:600,textTransform:"uppercase",letterSpacing:0.5}}>{f.l}</div>`);
add(`                        <select value={form[f.k]||""} onChange={e=>setForm(p=>({...p,[f.k]:e.target.value}))} style={{width:"100%",padding:"8px 10px",background:"rgba(255,255,255,0.04)",border:"1px solid "+T.border,borderRadius:7,color:T.white,fontSize:12,outline:"none"}}>`);
add(`                          {f.opts.map(o=><option key={o} value={o}>{o||f.ph}</option>)}`);
add(`                        </select>`);
add(`                      </div>`);
add(`                    ))}`);
add(`                    <div style={{gridColumn:"1/-1"}}>`);
add(`                      <div style={{fontSize:10,color:T.textMuted,marginBottom:3,fontWeight:600,textTransform:"uppercase",letterSpacing:0.5}}>Project</div>`);
add(`                      <input type="text" value={form.project||""} onChange={e=>setForm(p=>({...p,project:e.target.value}))} placeholder="Select project" style={{width:"100%",padding:"8px 10px",background:"rgba(255,255,255,0.04)",border:"1px solid "+T.border,borderRadius:7,color:T.white,fontSize:12,outline:"none",boxSizing:"border-box"}} />`);
add(`                    </div>`);
add(`                    {[`);
add(`                      {l:"Lead Source", k:"source",   opts:["", ...LEAD_SOURCES], ph:"Select lead source"},`);
add(`                      {l:"Channels",   k:"channel",  opts:["", ...CHANNELS],      ph:"Select channel"},`);
add(`                    ].map(f=>(`);
add(`                      <div key={f.k}>`);
add(`                        <div style={{fontSize:10,color:T.textMuted,marginBottom:3,fontWeight:600,textTransform:"uppercase",letterSpacing:0.5}}>{f.l}</div>`);
add(`                        <select value={form[f.k]||""} onChange={e=>setForm(p=>({...p,[f.k]:e.target.value}))} style={{width:"100%",padding:"8px 10px",background:"rgba(255,255,255,0.04)",border:"1px solid "+T.border,borderRadius:7,color:T.white,fontSize:12,outline:"none"}}>`);
add(`                          {f.opts.map(o=><option key={o} value={o}>{o||f.ph}</option>)}`);
add(`                        </select>`);
add(`                      </div>`);
add(`                    ))}`);
add(`                    <div style={{gridColumn:"1/-1"}}>`);
add(`                      <div style={{fontSize:10,color:T.textMuted,marginBottom:3,fontWeight:600,textTransform:"uppercase",letterSpacing:0.5}}>Campaign</div>`);
add(`                      <input type="text" value={form.campaign||""} onChange={e=>setForm(p=>({...p,campaign:e.target.value}))} placeholder="Select campaign" style={{width:"100%",padding:"8px 10px",background:"rgba(255,255,255,0.04)",border:"1px solid "+T.border,borderRadius:7,color:T.white,fontSize:12,outline:"none",boxSizing:"border-box"}} />`);
add(`                    </div>`);
add(`                    <div style={{gridColumn:"1/-1"}}>`);
add(`                      <div style={{fontSize:10,color:T.textMuted,marginBottom:3,fontWeight:600,textTransform:"uppercase",letterSpacing:0.5}}>Comment</div>`);
add(`                      <textarea value={form.comment||""} onChange={e=>setForm(p=>({...p,comment:e.target.value}))} placeholder="Enter comments or additional notes..." rows={3} style={{width:"100%",padding:"8px 10px",background:"rgba(255,255,255,0.04)",border:"1px solid "+T.border,borderRadius:7,color:T.white,fontSize:12,outline:"none",resize:"none",boxSizing:"border-box"}} />`);
add(`                    </div>`);
add(`                  </div>`);
add(`                )}`);
add(`              </div>`);
add(``);
add(`              {/* SECTION: Agent Information — exact from screenshot */}`);
add(`              <div style={{background:"rgba(255,255,255,0.02)",border:"1px solid "+T.border,borderRadius:10,marginBottom:12,overflow:"hidden"}}>`);
add(`                <div style={{padding:"11px 14px",display:"flex",alignItems:"center",gap:10,cursor:"pointer",borderBottom:openSection.agent?"1px solid "+T.border:"none"}} onClick={()=>setOpenSection(s=>({...s,agent:!s.agent}))}>`);
add(`                  <div style={{width:28,height:28,borderRadius:6,background:"rgba(251,146,60,0.15)",display:"flex",alignItems:"center",justifyContent:"center",flexShrink:0}}>`);
add(`                    <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="#FB923C" strokeWidth="2"><path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"/><circle cx="12" cy="7" r="4"/></svg>`);
add(`                  </div>`);
add(`                  <div>`);
add(`                    <div style={{fontSize:13,fontWeight:700,color:T.white}}>Agent Information</div>`);
add(`                    <div style={{fontSize:11,color:T.textMuted}}>Assign to a sales agent</div>`);
add(`                  </div>`);
add(`                  <div style={{marginLeft:"auto",color:T.textMuted,fontSize:16}}>{openSection.agent?"▾":"▸"}</div>`);
add(`                </div>`);
add(`                {openSection.agent&&(`);
add(`                  <div style={{padding:"14px",display:"grid",gridTemplateColumns:"1fr 1fr",gap:12}}>`);
add(`                    <div>`);
add(`                      <div style={{fontSize:10,color:T.textMuted,marginBottom:3,fontWeight:600,textTransform:"uppercase",letterSpacing:0.5}}>Agent Name</div>`);
add(`                      <input type="text" value={form.agentName||""} onChange={e=>setForm(p=>({...p,agentName:e.target.value}))} placeholder="Agent name" style={{width:"100%",padding:"8px 10px",background:"rgba(255,255,255,0.04)",border:"1px solid "+T.border,borderRadius:7,color:T.white,fontSize:12,outline:"none",boxSizing:"border-box"}} />`);
add(`                    </div>`);
add(`                    <div>`);
add(`                      <div style={{fontSize:10,color:T.textMuted,marginBottom:3,fontWeight:600,textTransform:"uppercase",letterSpacing:0.5}}>Pipeline Status</div>`);
add(`                      <select value={form.status||"New Lead"} onChange={e=>setForm(p=>({...p,status:e.target.value}))} style={{width:"100%",padding:"8px 10px",background:"rgba(255,255,255,0.04)",border:"1px solid "+T.border,borderRadius:7,color:T.white,fontSize:12,outline:"none"}}>`);
add(`                        {PIPELINE_STAGES.map(s=><option key={s.key} value={s.key}>{s.key}</option>)}`);
add(`                      </select>`);
add(`                    </div>`);
add(`                  </div>`);
add(`                )}`);
add(`              </div>`);
add(``);
add(`              {/* Duplicate warning */}`);
add(`              {form.phone&&isDupe(form.phone)&&(`);
add(`                <div style={{padding:"7px 12px",background:"rgba(239,68,68,0.1)",border:"1px solid rgba(239,68,68,0.25)",borderRadius:7,marginBottom:12,fontSize:11,color:"#EF4444"}}>⚠ A lead with this phone already exists in the system</div>`);
add(`              )}`);
add(``);
add(`              <button type="button" disabled={!form.name||!form.phone||saving}`);
add(`                onClick={()=>{`);
add(`                  if(!form.name||!form.phone) return;`);
add(`                  setSaving(true);`);
add(`                  try{`);
add(`                    setLeadAddName&&setLeadAddName(form.name);`);
add(`                    setLeadAddPhone&&setLeadAddPhone(form.phone);`);
add(`                    setLeadAddEmail&&setLeadAddEmail(form.email);`);
add(`                    setLeadAddBudget&&setLeadAddBudget(form.budget);`);
add(`                    setLeadAddSource&&setLeadAddSource(form.source||"Manual");`);
add(`                    setLeadAddType&&setLeadAddType(form.serviceType||"Buy");`);
add(`                    setLeadAddStatus&&setLeadAddStatus(form.status||"New Lead");`);
add(`                    setLeadAddComm&&setLeadAddComm(form.community||"");`);
add(`                    setLeadAddNat&&setLeadAddNat(form.nationality||"");`);
add(`                    setLeadAddTimeline&&setLeadAddTimeline(form.timeline||"");`);
add(`                    setShowAdd(false);`);
add(`                    setForm(emptyForm);`);
add(`                  }catch(e){console.error(e);}`);
add(`                  setSaving(false);`);
add(`                }}`);
add(`                style={{width:"100%",padding:"12px",borderRadius:8,border:"none",background:(!form.name||!form.phone)?"rgba(212,168,67,0.3)":"linear-gradient(135deg,#D4A843,#B8902E)",color:"#0A0E1A",fontSize:14,fontWeight:700,cursor:(!form.name||!form.phone)?"not-allowed":"pointer",fontFamily:"'Outfit',sans-serif"}}>`);
add(`                {saving?"Saving...":"Save Lead"}`);
add(`              </button>`);
add(`            </div>`);
add(`          </div>`);
add(`        </div>`);
add(`      )}`);
add(``);
add(`      {/* ══ WA TEMPLATES MODAL ══════════════════════════════════════════════ */}`);
add(`      {showWA&&(`);
add(`        <div style={{position:"fixed",inset:0,background:"rgba(4,9,15,0.88)",zIndex:2100,display:"flex",alignItems:"center",justifyContent:"center",padding:"20px"}} onClick={e=>{if(e.target===e.currentTarget)setShowWA(false);}}>`);
add(`          <div style={{background:"#0D1117",borderRadius:14,border:"1px solid "+T.border,width:"100%",maxWidth:500,padding:"22px"}} onClick={e=>e.stopPropagation()}>`);
add(`            <div style={{display:"flex",justifyContent:"space-between",alignItems:"center",marginBottom:16}}>`);
add(`              <div>`);
add(`                <div style={{fontFamily:"'Fraunces',serif",fontSize:15,fontWeight:900,color:T.white}}>WhatsApp Templates</div>`);
add(`                <div style={{fontSize:11,color:T.textMuted,marginTop:2}}>{filtered.filter(l=>l.phone).length} leads with phone in current filter</div>`);
add(`              </div>`);
add(`              <button type="button" onClick={()=>setShowWA(false)} style={{background:"rgba(255,255,255,0.06)",border:"1px solid "+T.border,borderRadius:7,color:T.textMuted,width:28,height:28,cursor:"pointer",display:"flex",alignItems:"center",justifyContent:"center",fontSize:16}}>×</button>`);
add(`            </div>`);
add(`            <div style={{display:"flex",flexDirection:"column",gap:8}}>`);
add(`              {WA_TEMPLATES.map((tmpl,i)=>(`);
add(`                <button key={i} type="button"`);
add(`                  onClick={()=>{const lead=filtered.find(l=>l.phone);if(lead)sendWA(tmpl,lead);else{alert("No leads with phone in current filter.");setShowWA(false);}}}`);
add(`                  style={{padding:"11px 13px",borderRadius:8,border:"1px solid rgba(37,211,102,0.2)",background:"rgba(37,211,102,0.04)",textAlign:"left",cursor:"pointer"}}`);
add(`                  onMouseEnter={e=>(e.currentTarget.style.background="rgba(37,211,102,0.1)")}`);
add(`                  onMouseLeave={e=>(e.currentTarget.style.background="rgba(37,211,102,0.04)")}`);
add(`                >`);
add(`                  <div style={{fontSize:12,fontWeight:700,color:"#25D366",marginBottom:3}}>{tmpl.label}</div>`);
add(`                  <div style={{fontSize:10,color:T.textMuted,lineHeight:1.5}}>{tmpl.body.slice(0,100)}...</div>`);
add(`                </button>`);
add(`              ))}`);
add(`            </div>`);
add(`          </div>`);
add(`        </div>`);
add(`      )}`);
add(``);
add(`    </div>`);
add(`  );`);
add(`}`);
add(``);
add(`export default MyLeadsTab;`);

fs.writeFileSync("src/tabs/MyLeadsTab.jsx", lines.join("\n"), "utf8");
console.log("Done. Lines:", lines.length);