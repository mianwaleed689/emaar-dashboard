const fs = require("fs");
const lines = [];
const a = s => lines.push(s);

a(`/* eslint-disable */`);
a(`/* DXB ANALYTICS - YIELDS TAB - Session 15 World Class Rebuild`);
a(`   Data: 259 communities from neighbourhoodScores collection`);
a(`   Real verified yields from Bayut/Driven/Knight Frank Q1 2026 */`);
a(``);
a(`import React, { useState, useMemo } from "react";`);
a(`import { BarChart, Bar, XAxis, YAxis, CartesianGrid, Tooltip, ResponsiveContainer, ReferenceLine } from "recharts";`);
a(`import { T } from "../data";`);
a(``);
a(`const fmtY  = n => n ? parseFloat(n).toFixed(1)+"%" : "--";`);
a(`const fmtP  = n => n ? "AED "+Math.round(n).toLocaleString() : "--";`);
a(`const fmtD  = n => n ? parseFloat(n).toFixed(1)+" km" : "--";`);
a(``);
a(`const YIELD_COLOR = y => parseFloat(y||0)>=8?"#10B981":parseFloat(y||0)>=6?"#84CC16":parseFloat(y||0)>=5?"#D4A843":"#94A3B8";`);
a(``);
a(`export default function YieldsTab({ liveNeighbourhoods=[], handleTabChange, globalFilters={} }) {`);
a(`  const [search,      setSearch]      = useState("");`);
a(`  const [sortBy,      setSortBy]      = useState("gross");`);
a(`  const [typeFilter,  setTypeFilter]  = useState("all");`);
a(`  const [yieldFilter, setYieldFilter] = useState("all");`);
a(`  const [metroOnly,   setMetroOnly]   = useState(false);`);
a(`  const [gvOnly,      setGvOnly]      = useState(false);`);
a(`  const [selected,    setSelected]    = useState(null);`);
a(``);
a(`  // Filter communities with yield data`);
a(`  const withYield = useMemo(() => `);
a(`    liveNeighbourhoods.filter(n => parseFloat(n.grossYield||0) > 0)`);
a(`  , [liveNeighbourhoods]);`);
a(``);
a(`  const filtered = useMemo(() => {`);
a(`    let a = [...withYield];`);
a(`    if(search.trim()) a = a.filter(n=>(n.community||"").toLowerCase().includes(search.toLowerCase()));`);
a(`    if(yieldFilter==="8+") a = a.filter(n=>parseFloat(n.grossYield||0)>=8);`);
a(`    if(yieldFilter==="7+") a = a.filter(n=>parseFloat(n.grossYield||0)>=7);`);
a(`    if(yieldFilter==="6+") a = a.filter(n=>parseFloat(n.grossYield||0)>=6);`);
a(`    if(yieldFilter==="<5") a = a.filter(n=>parseFloat(n.grossYield||0)<5);`);
a(`    if(metroOnly) a = a.filter(n=>n.hasMetro||parseFloat(n.distMetro||99)<=1.5);`);
a(`    if(gvOnly)    a = a.filter(n=>n.goldenVisa);`);
a(`    a.sort((x,y)=>{`);
a(`      if(sortBy==="gross")    return parseFloat(y.grossYield||0)-parseFloat(x.grossYield||0);`);
a(`      if(sortBy==="net")      return parseFloat(y.netYield||0)-parseFloat(x.netYield||0);`);
a(`      if(sortBy==="score")    return (y.investmentScore||0)-(x.investmentScore||0);`);
a(`      if(sortBy==="ppsf_asc") return (x.avgPpsf||0)-(y.avgPpsf||0);`);
a(`      if(sortBy==="liquidity")return (y.dldTransactions||0)-(x.dldTransactions||0);`);
a(`      if(sortBy==="name")     return (x.community||"").localeCompare(y.community||"");`);
a(`      return 0;`);
a(`    });`);
a(`    return a;`);
a(`  }, [withYield,search,sortBy,yieldFilter,metroOnly,gvOnly]);`);
a(``);
a(`  // Stats`);
a(`  const avgGross = withYield.length ? (withYield.reduce((s,n)=>s+parseFloat(n.grossYield||0),0)/withYield.length).toFixed(1) : 0;`);
a(`  const avgNet   = withYield.length ? (withYield.reduce((s,n)=>s+parseFloat(n.netYield||0),0)/withYield.length).toFixed(1) : 0;`);
a(`  const topYield = [...withYield].sort((a,b)=>parseFloat(b.grossYield||0)-parseFloat(a.grossYield||0))[0];`);
a(`  const high8    = withYield.filter(n=>parseFloat(n.grossYield||0)>=8).length;`);
a(`  const high7    = withYield.filter(n=>parseFloat(n.grossYield||0)>=7).length;`);
a(``);
a(`  // Chart data — top 15`);
a(`  const chartData = filtered.slice(0,15).map(n=>({`);
a(`    name: (n.community||"").length>12?(n.community||"").substring(0,12)+"...":n.community,`);
a(`    gross: parseFloat(n.grossYield||0),`);
a(`    net:   parseFloat(n.netYield||0),`);
a(`    fill:  YIELD_COLOR(n.grossYield),`);
a(`  }));`);
a(``);
a(`  const selStyle = {padding:"6px 10px",background:"rgba(255,255,255,0.04)",border:"1px solid "+T.border,borderRadius:7,color:"#CBD5E1",fontSize:11,outline:"none",fontFamily:"'Outfit',sans-serif"};`);
a(`  const Pill = ({active,onClick,label}) => (`);
a(`    <button type="button" onClick={onClick} style={{padding:"5px 10px",borderRadius:7,border:"1px solid "+(active?T.gold:T.border),background:active?"rgba(212,168,67,0.1)":"rgba(255,255,255,0.03)",color:active?T.gold:"#94A3B8",fontSize:11,fontWeight:active?600:400,cursor:"pointer",fontFamily:"'Outfit',sans-serif"}}>`);
a(`      {label}`);
a(`    </button>`);
a(`  );`);
a(``);
a(`  return (`);
a(`    <div style={{paddingBottom:60}}>`);
a(``);
a(`      {/* HEADER */}`);
a(`      <div style={{marginBottom:16}}>`);
a(`        <h2 style={{margin:0,fontSize:20,fontWeight:900,color:T.white,fontFamily:"'Fraunces',serif"}}>Rental Yields</h2>`);
a(`        <p style={{margin:"4px 0 0",fontSize:12,color:"#94A3B8"}}>`);
a(`          {withYield.length} communities tracked · Dubai average {avgGross}% gross · Sources: Bayut 2025, Driven Properties, Knight Frank Q1 2026`);
a(`        </p>`);
a(`      </div>`);
a(``);
a(`      {/* KPI CARDS */}`);
a(`      <div style={{display:"grid",gridTemplateColumns:"repeat(4,1fr)",gap:12,marginBottom:16}}>`);
a(`        {[`);
a(`          {label:"Dubai Avg Gross",  value:avgGross+"%",            color:"#10B981", hint:"All communities"},`);
a(`          {label:"Dubai Avg Net",    value:avgNet+"%",              color:"#84CC16", hint:"After service charges"},`);
a(`          {label:"8%+ High Yield",   value:high8+" communities",    color:T.gold,    hint:"Top performers"},`);
a(`          {label:"Best Yield",       value:topYield?fmtY(topYield.grossYield):"--", color:"#10B981", hint:topYield?.community||""},`);
a(`        ].map((k,i)=>(`);
a(`          <div key={i} style={{background:"rgba(255,255,255,0.02)",border:"1px solid "+T.border,borderRadius:12,padding:"14px 16px",position:"relative",overflow:"hidden"}}>`);
a(`            <div style={{position:"absolute",top:0,left:0,right:0,height:2,background:k.color,opacity:0.8}}/>`);
a(`            <div style={{fontSize:9,fontWeight:700,color:"#64748B",textTransform:"uppercase",letterSpacing:0.8,marginBottom:4}}>{k.label}</div>`);
a(`            <div style={{fontSize:18,fontWeight:900,color:k.color,fontFamily:"'Fraunces',serif",marginBottom:2}}>{k.value}</div>`);
a(`            <div style={{fontSize:10,color:"#64748B"}}>{k.hint}</div>`);
a(`          </div>`);
a(`        ))}`);
a(`      </div>`);
a(``);
a(`      {/* CHART */}`);
a(`      {chartData.length>0&&(`);
a(`        <div style={{background:"rgba(255,255,255,0.02)",border:"1px solid "+T.border,borderRadius:12,padding:"16px",marginBottom:16}}>`);
a(`          <div style={{fontSize:12,fontWeight:700,color:T.white,marginBottom:12}}>Top 15 Communities by Yield</div>`);
a(`          <ResponsiveContainer width="100%" height={200}>`);
a(`            <BarChart data={chartData} margin={{top:5,right:10,left:-20,bottom:40}}>`);
a(`              <CartesianGrid strokeDasharray="3 3" stroke={T.border} vertical={false}/>`);
a(`              <XAxis dataKey="name" tick={{fontSize:8,fill:"#64748B"}} angle={-35} textAnchor="end"/>`);
a(`              <YAxis tick={{fontSize:9,fill:"#64748B"}} domain={[0,12]}/>`);
a(`              <Tooltip contentStyle={{background:T.surface,border:"1px solid "+T.border,borderRadius:8,fontSize:11}} formatter={(v,n)=>[v+"%",n==="gross"?"Gross Yield":"Net Yield"]}/>`);
a(`              <ReferenceLine y={parseFloat(avgGross)} stroke="#D4A843" strokeDasharray="4 4" label={{value:"Dubai Avg",fill:"#D4A843",fontSize:9}}/>`);
a(`              <Bar dataKey="gross" name="Gross Yield" radius={[3,3,0,0]} fill="#10B981">`);
a(`                {chartData.map((d,i)=>(`);
a(`                  <cell key={i} fill={d.fill}/>`);
a(`                ))}`);
a(`              </Bar>`);
a(`              <Bar dataKey="net" name="Net Yield" radius={[3,3,0,0]} fill="#84CC16" opacity={0.6}/>`);
a(`            </BarChart>`);
a(`          </ResponsiveContainer>`);
a(`        </div>`);
a(`      )}`);
a(``);
a(`      {/* TOOLBAR */}`);
a(`      <div style={{display:"flex",gap:8,alignItems:"center",marginBottom:10,flexWrap:"wrap"}}>`);
a(`        <div style={{flex:"1 1 200px",display:"flex",alignItems:"center",gap:8,padding:"7px 12px",background:"rgba(255,255,255,0.03)",border:"1px solid "+(search?T.gold:T.border),borderRadius:8}}>`);
a(`          <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="#94A3B8" strokeWidth="2"><circle cx="11" cy="11" r="8"/><line x1="21" y1="21" x2="16.65" y2="16.65"/></svg>`);
a(`          <input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search community..." style={{flex:1,background:"none",border:"none",outline:"none",color:T.white,fontSize:12,fontFamily:"'Outfit',sans-serif"}}/>`);
a(`          {search&&<button type="button" onClick={()=>setSearch("")} style={{background:"none",border:"none",color:"#94A3B8",cursor:"pointer",fontSize:14}}>x</button>}`);
a(`        </div>`);
a(`        <select value={yieldFilter} onChange={e=>setYieldFilter(e.target.value)} style={selStyle}>`);
a(`          <option value="all">All Yields</option>`);
a(`          <option value="8+">8%+ High Yield</option>`);
a(`          <option value="7+">7%+ Good Yield</option>`);
a(`          <option value="6+">6%+ Mid Yield</option>`);
a(`          <option value="<5">Below 5%</option>`);
a(`        </select>`);
a(`        <select value={sortBy} onChange={e=>setSortBy(e.target.value)} style={selStyle}>`);
a(`          <option value="gross">Highest Gross Yield</option>`);
a(`          <option value="net">Highest Net Yield</option>`);
a(`          <option value="score">Best Score</option>`);
a(`          <option value="ppsf_asc">Most Affordable</option>`);
a(`          <option value="liquidity">Most Liquid</option>`);
a(`          <option value="name">A - Z</option>`);
a(`        </select>`);
a(`      </div>`);
a(``);
a(`      {/* FILTER PILLS */}`);
a(`      <div style={{display:"flex",gap:6,marginBottom:14,flexWrap:"wrap",alignItems:"center"}}>`);
a(`        <Pill active={metroOnly} onClick={()=>setMetroOnly(v=>!v)} label="Metro Access"/>`);
a(`        <Pill active={gvOnly}    onClick={()=>setGvOnly(v=>!v)}    label="Golden Visa"/>`);
a(`        {(metroOnly||gvOnly||yieldFilter!=="all"||search)&&(`);
a(`          <button type="button" onClick={()=>{setMetroOnly(false);setGvOnly(false);setYieldFilter("all");setSearch("");}} style={{fontSize:10,padding:"4px 10px",borderRadius:8,border:"1px solid rgba(239,68,68,0.3)",background:"rgba(239,68,68,0.08)",color:"#EF4444",cursor:"pointer",fontFamily:"'Outfit',sans-serif"}}>Clear</button>`);
a(`        )}`);
a(`        <span style={{fontSize:11,color:"#94A3B8",marginLeft:"auto"}}>{filtered.length} communities</span>`);
a(`      </div>`);
a(``);
a(`      {/* YIELD TABLE */}`);
a(`      <div style={{background:"rgba(255,255,255,0.02)",border:"1px solid "+T.border,borderRadius:14,overflow:"hidden"}}>`);
a(`        {/* Header */}`);
a(`        <div style={{display:"grid",gridTemplateColumns:"28px 2fr 80px 80px 90px 100px 90px 80px",padding:"10px 16px",fontSize:9,fontWeight:700,color:"#64748B",textTransform:"uppercase",letterSpacing:0.8,borderBottom:"1px solid "+T.border,background:"rgba(255,255,255,0.02)"}}>`);
a(`          {["#","Community","Gross","Net","PPSF","Transactions","Score","Metro"].map((h,i)=>(`);
a(`            <div key={i} style={{textAlign:i>1?"center":"left"}}>{h}</div>`);
a(`          ))}`);
a(`        </div>`);
a(``);
a(`        {/* Rows */}`);
a(`        {filtered.slice(0,100).map((n,i)=>(`);
a(`          <div key={n.community||i}`);
a(`            onClick={()=>setSelected(selected?.community===n.community?null:n)}`);
a(`            style={{display:"grid",gridTemplateColumns:"28px 2fr 80px 80px 90px 100px 90px 80px",padding:"11px 16px",alignItems:"center",borderBottom:i<filtered.length-1?"1px solid "+T.border+"30":"none",cursor:"pointer",background:selected?.community===n.community?"rgba(212,168,67,0.04)":"transparent"}}`);
a(`            onMouseEnter={e=>e.currentTarget.style.background="rgba(212,168,67,0.03)"}`);
a(`            onMouseLeave={e=>e.currentTarget.style.background=selected?.community===n.community?"rgba(212,168,67,0.04)":"transparent"}`);
a(`          >`);
a(`            {/* Rank */}`);
a(`            <div style={{fontSize:10,color:"#64748B",fontWeight:600}}>{i+1}</div>`);
a(`            {/* Community */}`);
a(`            <div>`);
a(`              <div style={{fontSize:12,fontWeight:600,color:T.white}}>{n.community}</div>`);
a(`              <div style={{display:"flex",gap:4,marginTop:2,flexWrap:"wrap"}}>`);
a(`                <span style={{fontSize:9,padding:"1px 5px",borderRadius:4,background:n.tier==="verified"?"rgba(16,185,129,0.12)":"rgba(100,116,139,0.12)",color:n.tier==="verified"?"#10B981":"#64748B",fontWeight:600}}>`);
a(`                  {n.tier==="verified"?"Verified":n.tier==="area-data"?"Area Data":"DLD"}`);
a(`                </span>`);
a(`                {n.goldenVisa&&<span style={{fontSize:9,padding:"1px 5px",borderRadius:4,background:"rgba(212,168,67,0.12)",color:T.gold,fontWeight:600}}>GV</span>}`);
a(`                {n.hasMetro&&<span style={{fontSize:9,padding:"1px 5px",borderRadius:4,background:"rgba(16,185,129,0.1)",color:"#10B981",fontWeight:600}}>Metro</span>}`);
a(`              </div>`);
a(`            </div>`);
a(`            {/* Gross Yield */}`);
a(`            <div style={{textAlign:"center"}}>`);
a(`              <span style={{fontSize:13,fontWeight:700,color:YIELD_COLOR(n.grossYield),fontFamily:"'Fraunces',serif"}}>{fmtY(n.grossYield)}</span>`);
a(`            </div>`);
a(`            {/* Net Yield */}`);
a(`            <div style={{textAlign:"center",fontSize:12,fontWeight:600,color:"#CBD5E1"}}>{fmtY(n.netYield)}</div>`);
a(`            {/* PPSF */}`);
a(`            <div style={{textAlign:"center",fontSize:11,color:T.gold}}>{n.avgPpsf?"AED "+Math.round(n.avgPpsf).toLocaleString():"--"}</div>`);
a(`            {/* DLD Transactions */}`);
a(`            <div style={{textAlign:"center"}}>`);
a(`              {n.dldTransactions?(`);
a(`                <div>`);
a(`                  <div style={{fontSize:11,fontWeight:600,color:"#94A3B8"}}>{n.dldTransactions.toLocaleString()}</div>`);
a(`                  <div style={{fontSize:9,color:n.liquidity==="Very High"||n.liquidity==="High"?"#10B981":"#64748B"}}>{n.liquidity||""}</div>`);
a(`                </div>`);
a(`              ):<span style={{fontSize:10,color:"#64748B"}}>--</span>}`);
a(`            </div>`);
a(`            {/* Score */}`);
a(`            <div style={{textAlign:"center"}}>`);
a(`              <div style={{width:32,height:32,borderRadius:"50%",background:"rgba(212,168,67,0.1)",border:"1px solid rgba(212,168,67,0.3)",display:"inline-flex",alignItems:"center",justifyContent:"center"}}>`);
a(`                <span style={{fontSize:10,fontWeight:700,color:T.gold}}>{n.investmentScore||"--"}</span>`);
a(`              </div>`);
a(`            </div>`);
a(`            {/* Metro */}`);
a(`            <div style={{textAlign:"center",fontSize:10,color:"#94A3B8"}}>{n.distMetro?parseFloat(n.distMetro).toFixed(1)+"km":"--"}</div>`);
a(`          </div>`);
a(`        ))}`);
a(`      </div>`);
a(``);
a(`      {/* SELECTED COMMUNITY DETAIL */}`);
a(`      {selected&&(`);
a(`        <div style={{marginTop:12,background:"rgba(212,168,67,0.04)",border:"1px solid rgba(212,168,67,0.2)",borderRadius:14,padding:"20px"}}>`);
a(`          <div style={{display:"flex",justifyContent:"space-between",alignItems:"flex-start",marginBottom:14}}>`);
a(`            <div>`);
a(`              <div style={{fontSize:18,fontWeight:700,color:T.white,fontFamily:"'Fraunces',serif",marginBottom:4}}>{selected.community}</div>`);
a(`              <div style={{fontSize:11,color:"#94A3B8"}}>{selected.nearestMetro||"Dubai"} · {selected.supplyRisk||"Unknown"} Supply Risk</div>`);
a(`            </div>`);
a(`            <div style={{display:"flex",gap:8,alignItems:"center"}}>`);
a(`              <button type="button" onClick={()=>handleTabChange&&handleTabChange("Neighbourhoods")} style={{padding:"8px 14px",borderRadius:8,border:"1px solid "+T.gold,background:"rgba(212,168,67,0.08)",color:T.gold,fontSize:11,fontWeight:600,cursor:"pointer",fontFamily:"'Outfit',sans-serif"}}>View Community</button>`);
a(`              <button type="button" onClick={()=>setSelected(null)} style={{background:"rgba(255,255,255,0.06)",border:"1px solid "+T.border,borderRadius:8,color:"#94A3B8",width:32,height:32,cursor:"pointer",display:"flex",alignItems:"center",justifyContent:"center",fontSize:16}}>x</button>`);
a(`            </div>`);
a(`          </div>`);
a(`          <div style={{display:"grid",gridTemplateColumns:"repeat(4,1fr)",gap:10,marginBottom:14}}>`);
a(`            {[`);
a(`              {label:"Gross Yield",    value:fmtY(selected.grossYield),    color:YIELD_COLOR(selected.grossYield)},`);
a(`              {label:"Net Yield",      value:fmtY(selected.netYield),      color:"#CBD5E1"},`);
a(`              {label:"Avg PPSF",       value:fmtP(selected.avgPpsf),       color:T.gold},`);
a(`              {label:"Service Charge", value:selected.serviceCharge?"AED "+selected.serviceCharge+"/sqft":"--", color:"#94A3B8"},`);
a(`              {label:"DLD Txns",       value:selected.dldTransactions?selected.dldTransactions.toLocaleString():"--", color:"#94A3B8"},`);
a(`              {label:"Liquidity",      value:selected.liquidity||"--",     color:selected.liquidity==="Very High"||selected.liquidity==="High"?"#10B981":"#F59E0B"},`);
a(`              {label:"Inv Score",      value:selected.investmentScore||"--", color:T.gold},`);
a(`              {label:"Supply Risk",    value:selected.supplyRisk||"--",    color:selected.supplyRisk==="Low"?"#10B981":"#F59E0B"},`);
a(`            ].map((m,i)=>(`);
a(`              <div key={i} style={{background:"rgba(255,255,255,0.03)",borderRadius:8,padding:"10px 12px"}}>`);
a(`                <div style={{fontSize:9,color:"#64748B",textTransform:"uppercase",letterSpacing:0.7,marginBottom:3}}>{m.label}</div>`);
a(`                <div style={{fontSize:14,fontWeight:700,color:m.color,fontFamily:"'Fraunces',serif"}}>{m.value}</div>`);
a(`              </div>`);
a(`            ))}`);
a(`          </div>`);
a(`          <div style={{display:"grid",gridTemplateColumns:"repeat(3,1fr)",gap:8}}>`);
a(`            {[`);
a(`              {label:"Metro",    value:selected.nearestMetro,    dist:selected.distMetro},`);
a(`              {label:"Mall",     value:selected.nearestMall,     dist:selected.distMall},`);
a(`              {label:"Hospital", value:selected.nearestHospital, dist:selected.distHospital},`);
a(`            ].map((f,i)=>(`);
a(`              <div key={i} style={{background:"rgba(255,255,255,0.03)",borderRadius:8,padding:"8px 10px"}}>`);
a(`                <div style={{fontSize:9,color:"#64748B",textTransform:"uppercase",letterSpacing:0.7,marginBottom:2}}>{f.label}</div>`);
a(`                <div style={{fontSize:11,fontWeight:600,color:T.white,overflow:"hidden",textOverflow:"ellipsis",whiteSpace:"nowrap"}}>{f.value||"--"}</div>`);
a(`                <div style={{fontSize:10,color:"#94A3B8"}}>{f.dist?parseFloat(f.dist).toFixed(1)+" km":"--"}</div>`);
a(`              </div>`);
a(`            ))}`);
a(`          </div>`);
a(`        </div>`);
a(`      )}`);
a(``);
a(`      {/* EMPTY STATE */}`);
a(`      {liveNeighbourhoods.length===0&&(`);
a(`        <div style={{padding:"60px 20px",textAlign:"center"}}>`);
a(`          <div style={{fontSize:32,marginBottom:12}}>?</div>`);
a(`          <div style={{fontSize:16,fontWeight:700,color:T.white,marginBottom:6}}>Loading yield data...</div>`);
a(`          <div style={{fontSize:12,color:"#94A3B8"}}>Connecting to 259 community database</div>`);
a(`        </div>`);
a(`      )}`);
a(``);
a(`      {/* SOURCES */}`);
a(`      <div style={{marginTop:16,paddingTop:12,borderTop:"1px solid "+T.border,display:"flex",gap:6,flexWrap:"wrap",alignItems:"center"}}>`);
a(`        <span style={{fontSize:10,color:"#64748B"}}>Sources:</span>`);
a(`        {["Bayut H1 2025","Driven Properties Q1 2026","Knight Frank Q1 2025","Dubai Land Department","GuestReady STR Data"].map((s,i)=>(`);
a(`          <span key={i} style={{fontSize:10,color:"#64748B",padding:"2px 8px",borderRadius:8,border:"1px solid rgba(255,255,255,0.06)"}}>{s}</span>`);
a(`        ))}`);
a(`      </div>`);
a(``);
a(`    </div>`);
a(`  );`);
a(`}`);

const content = lines.join("\n");
require("fs").writeFileSync("src/tabs/YieldsTab.jsx", content, "utf8");
console.log("Done. Lines:", lines.length);
console.log("Non-ASCII:", (content.match(/[^\x00-\x7F]/g)||[]).length);